# v1270 运行证据说明

## 结论

v1270 将高频修改的 `ci_workflow_hygiene.py` 从单文件 523 行拆成入口、检查、摘要和类型四个职责层。
固定输入的 canonical JSON SHA-256 在拆分前后完全一致：
`298db00f23faa43be167566fb46e9f6851962364777b1693a29ad631cb72f126`。

## 需求-证据矩阵

| 需求 | 实现 | 证据 | 状态 |
| --- | --- | --- | --- |
| 降低高频入口职责宽度 | 80 行编排入口 + 3 个组件模块 | line census | 完整 |
| 所有 report 字段和顺序保持不变 | 固定 generated_at canonical JSON 比较 | before/after SHA-256 相同 | 完整 |
| check ID、summary key 不丢失 | 组件重组测试 | 63 checks、61 summary keys | 完整 |
| 新组件进入严格静态门 | ruff strict + mypy scope | 19 targets、0 diagnostics | 完整 |
| 大文件压力下降 | file-size ratchet | warning count 22 -> 21 | 完整 |
| 浏览器证据无控制台噪音 | type-analysis renderer 内联 favicon | Playwright 最终页面无 console error | 完整 |

## 可复现命令

```powershell
python -m pytest tests\test_ci_workflow.py tests\test_static_analysis.py tests\test_type_analysis.py -q -o cache_dir=runs\pytest-cache-v1270-focused
python -B scripts\check_ci_workflow_hygiene.py --out-dir f\1270\解释\ci-workflow-hygiene
python -B scripts\check_type_analysis.py --out-dir f\1270\解释\type-analysis
python -B scripts\check_file_size_ratchet.py --out-dir f\1270\解释\file-size-ratchet
```

结果：`23 passed`；CI hygiene `63/63`；mypy `target_count=19`、`diagnostic_count=0`；
file-size ratchet `over_warning_count=21`、`unwaived_over_limit_count=0`。

## 归档

- parity 摘要：`f/1270/解释/ci-workflow-hygiene-refactor-parity.json`
- CI hygiene 四格式报告：`f/1270/解释/ci-workflow-hygiene/`
- strict mypy 四格式报告：`f/1270/解释/type-analysis/`
- 浏览器截图：`f/1270/图片/ci-hygiene-type-scope-v1270.png`

file-size 的全仓逐文件 JSON 接近四万行，本版只需要其中的 warning count，因此不复制进正式归档；
`21` 已写入 parity JSON，原命令可随时重新生成完整报告。

## 边界

本版只拆工程治理模块，不修改 workflow 策略、check ID、decision、报告字段、writer 输出、训练语义、
缓存实验、`decide()` 或 promotion verdict。拆分后总行数因显式类型和模块边界略有增加；优化目标是降低
单文件认知负担与冲突半径，不用总 LOC 下降包装结果。

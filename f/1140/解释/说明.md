# v1140 运行截图与解释

v1140 对 v1135-v1139 模型能力回归链路里的 report loader 样板做了保守去重。新增共享 helper `locate_upstream_report` 和 `read_json_object`，五个 regression 模块保留原来的公开函数名，只把内部实现改成调用共享 helper。

真实运行命令：

```powershell
python -B scripts\generate_report_loader_dedup_v1140.py --out-dir f\1140\解释\report-loader-dedup-v1140 --require-dedup-ready --force
```

关键结果：

- `status=pass`
- `decision=report_loader_dedup_ready`
- `dedup_ready=True`
- `read_json_report_definition_count=431`
- `private_loader_copy_count=439`
- `migrated_module_count=5`
- `next_step=build_model_capability_regression_trend_report_v1141`

截图文件：

- `f/1140/图片/v1140-report-loader-dedup.png`

边界说明：v1140 是契约保持型重构，只迁移 v1135-v1139 五个目标模块，不批量改动历史 400+ 个 loader 样板，不改变已有 JSON/CSV/Markdown/HTML/text 产物语义。

一句话总结：v1140 把最新模型能力回归链路的 report loader 样板收进共享 helper，为后续 v1141 trend report 降低重复和维护成本。

# v1268 运行证据说明

## 结论

v1268 完成 Stage-1 评审遗留收口与 CI execution economy 优化。当前 CI hygiene 报告为
`status=pass`、`decision=continue_with_node24_native_ci`、`check_count=63`、
`failed_check_count=0`，新增执行经济策略的六个原子检查全部通过。

## 需求-证据矩阵

| 需求 | 实现 | 机械证据 | 状态 |
| --- | --- | --- | --- |
| 同一 commit 不因 main push 与 tag push 重复运行完整 CI | `.github/workflows/ci.yml` 只监听 `main` branch push，未声明 tag trigger | `tag_push_suppressed=True` | 完整 |
| 依赖下载可复用且 requirements 变化会失效缓存 | `actions/setup-python@v6` 配置 pip cache 与 dependency path | `pip_dependency_cache_ready=True` | 完整 |
| 同一 ref 的旧 CI 可被新提交取消 | workflow/ref concurrency group + `cancel-in-progress: true` | `concurrency_cancel_ready=True` | 完整 |
| 上述策略不能只靠文档约定 | `ci_workflow_hygiene_policy.py`、hygiene report 与负向测试 | `execution_policy_violation_count=0` | 完整 |
| Stage-2 简报纳入仓库但保持未激活 | `docs/stage2-aiproj-operational-brief.md` 明示 `STATUS: INACTIVE` | 文档索引与源文件 | 完整 |
| 不削弱既有质量门 | 31 个顺序检查与原命令检查保持通过 | `order_violation_count=0`、`missing_step_count=0` | 完整 |

## 可复现命令

```powershell
python -m pytest tests\test_ci_workflow.py tests\test_project_configuration.py -q -o cache_dir=runs\pytest-cache-v1268-focused
python -B scripts\check_ci_workflow_hygiene.py --out-dir f\1268\解释\ci-execution-economy
python -B scripts\check_type_analysis.py --out-dir runs\type-analysis-v1268
python -B scripts\check_static_analysis.py --out-dir runs\static-analysis-v1268
```

关键结果：聚焦测试 `17 passed`；CI hygiene `63/63`；严格 mypy scope
`target_count=16, diagnostic_count=0`；ruff staged gate `current_issue_count=545,
new_issue_count=0, strict_format_status=pass`。

## 产物

- 结构化报告：`f/1268/解释/ci-execution-economy/ci_workflow_hygiene.json`
- 表格报告：`f/1268/解释/ci-execution-economy/ci_workflow_hygiene.csv`
- 阅读报告：`f/1268/解释/ci-execution-economy/ci_workflow_hygiene.md`
- 浏览器报告：`f/1268/解释/ci-execution-economy/ci_workflow_hygiene.html`
- 浏览器核验：`f/1268/图片/ci-execution-economy-v1268.png`

截图由 Playwright 打开本地 HTTP 只读页面后生成。可访问性快照明确显示
`Execution economy=True`、`Tag push suppressed=True`、`Pip cache=True`、
`Cancel superseded=True`，并显示六条 `execution:*` 检查为 `pass`。

## 边界

本版没有运行训练、修改 checkpoint、改写缓存实验结果、调整 `decide()`、改变模型 verdict
或扩大 promotion claim。tag 不再单独触发 CI，但 tag 指向的 commit 必须先经 `main` push 的同一套
CI 验证；该策略减少的是重复执行，不是验证内容。

# v1318 随机状态预校验

在 checkpoint 加载阶段校验 RNG，在原来的初始化完成边界真正恢复。
CPU 格式由独立 Python、NumPy、Torch 生成器验证，不通过全局 setter 做试探性恢复。
CUDA 只检查容器/张量结构并规范到 CPU，不启动 CUDA，也不声称设备状态已完整验证。

## 本地证据

- 旧代码两项红灯：NumPy 字段无效时 Python 全局已改变；无效 RNG 版本在日志回退之后才报错。
- 原断言不变，新实现通过。新模块 6 个测试方法，包含 21 组损坏输入；完整定向回归 55 项通过。
- 与 v1317 原训练脚本和原 RNG helper 的独立四进程比较通过：完整 checkpoint 数据一致，
  metrics、sample、摘要和 tokenizer 字节一致；损坏快照的 CLI 拒绝且原运行文件不变。
- Ruff、格式和 RNG helper 严格 mypy 通过；快速 CI 门的命令与结果在 [机器摘要](rng-validation.json)。
- 完整测试由同提交 GitHub CI 执行原入口；未降 88.98% 门槛，成功后才创建 v1318.0.0 标签。

```powershell
python -B -m unittest tests.test_rng_validation tests.test_train_preflight tests.test_history_recovery tests.test_checkpoint_io tests.test_tokenizer_binding tests.test_rng_state tests.test_resume_rng tests.test_model_cli_behavior tests.test_architecture_boundaries -v
python -B scripts/run_test_coverage.py --out-dir runs/test-coverage-ci --fail-under 88.98
git show --no-patch v1318.0.0
```

[需求矩阵与边界](../../../docs/v1318-rng-validation.md)

不承诺不可信 pickle 安全、并发全局状态事务或原生设备错误回滚。整个 CLI 仍会先设置种子，
“校验不改变全局状态”不是“失败命令的所有行为都无副作用”。个人面试资料不属于公开交付。

# v1276 superposition 实验预注册与证据

## 1. 目标、来源与边界

本版在自有 toy substrate 上复现 Toy Models of Superposition 的基本现象：20 个相互独立的
非负 feature 被压进 5 个 hidden dimensions，检验 feature 越稀疏时，模型是否会用非正交方向
表示超过 5 个 feature，并且这种 packing 是否优于解析可得的最佳 dedicated 解。

参考定义来自 Elhage et al. 2022 的 ReLU-output toy model：
`x' = ReLU(W^T W x + b)`，feature 以概率 `S` 为零，否则服从 `Uniform[0,1]`，loss 是
importance-weighted squared error。来源：`https://transformer-circuits.pub/2022/toy_model/`。

范围固定为 `toy autoencoder, own substrate`。不声称 MiniGPT 或 LLM 内部已经存在相同结构，
不做完整 phase diagram，也不触碰 v1275 或更早版本的 artifact、cache 与 verdict。本版 CPU-only；
GPU 使用没有正当理由，因此被禁止。

## 2. 需求—证据矩阵（运行前冻结）

| 需求 | 实现位置 | 机械证据 | 运行前状态 |
|---|---|---|---|
| 20→5 tied ReLU autoencoder | `superposition_v1276.py::ToyAE` | shape/forward 测试 | 完整 |
| 两 arm、五 sparsity、五 seed | `run_phase_a` | 50-cell cache completeness | 完整 |
| 解析 dedicated baseline | `dedicated_loss` | tiny brute-force quadrature | 完整；probe 后披露修正 |
| plateau 收敛与排除披露 | `plateau_drift` / G1 | 稳定/未收敛合成测试 | 完整 |
| 三个 τ 的表示数与 interference | `weight_metrics` | 精确构造 W 测试 | 完整 |
| Spearman 与 exact p | `spearman_exact` | 单调/非单调测试 | 完整 |
| G1-G5 与四分支 verdict | `decide` | 每个 gate/branch 测试 | 完整 |
| probe 先于 full grid | run CLI `probe`→`full --resume` | sparse packing stop 测试 | 完整 |
| Phase B cache-only | analyze CLI | actual cache byte-stable 复算 | 完整 |
| 五格式报告与单图 | readability writer / `plot_result` | 文件与浏览器检查 | 完整 |

## 3. 新 family 设计说明（先于实现，≤10 行）

抽象名是 `Superposition experiment`。数据面只有 frozen config、cell key、`W/b`、loss trajectory
和 final evaluation loss；行为面只有 cell training、解析 baseline、cache-only metrics、纯函数
`decide()` 与 renderer。`run_` 脚本只生成/续写 Phase-A cache，`analyze_` 脚本只读 cache，
不允许导入训练入口后隐式补跑。一个核心模块承载 substrate，不新建 package 或通用 harness。

## 4. 对抗性 premortem

### 4.1 “表示数变多”但并非最优

低 norm threshold 会把随机小权重也算作 feature，造成视觉上的 packing。为此 G2 对
`τ={0.3,0.5,0.7}` 全部成立才通过；G4 还要求 sparse 模型以预注册 margin 击败解析 dedicated
optimum。出现 R>5 但 loss 不优时必须进入 `superposition_not_optimal`，不能包装成 finding。

### 4.2 解析 baseline 写错或量纲漂移

本版 loss 定义为 batch 平均、feature 维求和：
`E[sum_i I_i (x_i-x'_i)^2]`。dedicated 解精确重建 importance 最大的 m 个 feature；未表示
feature 使用该分布的最优常数输出。
预注册 commit `2cc520f0` 最初把“drop”解释成输出 0，写成 `E[x²]`。真实 probe 证明该式
不是 **best** dedicated baseline：模型有 per-feature bias，未表示 feature 可以输出最优常数
`E[x]=(1-S)/2`。因此按失败条件披露并从未改动的 probe cache 重算，修正式使用方差：

```text
q = 1-S
L_dedicated(S) = (q/3 - q^2/4) * sum(importance of dropped n-m features)
```

importance arm 在 S=0/0.99 的 baseline 分别为 `0.0464644659/0.0018446393`；uniform arm
分别为 `1.25/0.049625`。公式用 tiny Cartesian quadrature（包含 dropped-feature 最优 bias）
独立复算；测试缺失即 G4 无效。2% epsilon、G1-G5 与 verdict 顺序均未改变。

### 4.3 优化失败被误报为科学阴性

每 cell 固定训练 6000 steps，Adam `lr=0.003`，large batch=1024；每 100 steps 在固定 8192
样本 trajectory set 上记录 loss，最后在独立固定 262144 样本上估计 final loss。最后 10% 的
trajectory loss range 除以解析 baseline 必须 `<=0.02`。每 cell 至少 4/5 seeds 收敛；不收敛
seed 会保留在 cache 并标为 excluded，绝不删除原始记录。少于 4 个即 G1 失败并 route review。

### 4.4 finite-sample 与事后 margin

G4 使用解析 expectation，不把同一批次的经验 dedicated loss 当理论。final eval 取 262144 样本
以压低 S=0.99 稀有激活的 Monte Carlo 误差。`optimality_epsilon=0.02` 预注册为相对 baseline
margin：sparse 必须 `median_loss <= baseline*(1-0.02)`；dense 必须不满足这一不等式。运行后
不得改成绝对 epsilon、换 eval seed 或增加只对有利 arm 生效的容差。

### 4.5 importance control 与初始化混淆

同一 `(S,seed)` 的 IMPORTANCE/UNIFORM arm 使用同一输入 RNG 和同一模型初始化，唯一差异是
loss importance。IMPORTANCE 为 `0.7^i`，UNIFORM 全 1。G5 只在 IMPORTANCE dense、主阈值
`τ=0.5` 检查 represented set 是否恰为 `{0,1,2,3,4}`，至少 4/5 seeds；uniform 没有可恢复的
顺序，因此明确豁免。G5 单独报告，不反向决定 G1-G4 的 superposition headline。

## 5. 冻结配置和判据

### 5.1 Phase A 配置

- `n=20`, `m=5`, sparsity=`[0.0,0.7,0.9,0.97,0.99]`。
- arms=`importance,uniform`；seeds=`[0,1,2,3,4]`。
- `steps=6000`, `batch=1024`, `lr=0.003`, `record_every=100`。
- trajectory eval=`8192`，final eval=`262144`；所有 tensor 和训练均在 CPU。
- 模型初始化按 seed 配对；data generator 按 `(S,seed)` 配对，arm 不进入 RNG seed。

### 5.2 CPU probe 停止门

先运行四格：两 arm × `S={0.0,0.99}` × seed 0。每个 sparse probe 在最宽松的 `τ=0.3`
都必须 `R>m` 才允许 full grid；任一 arm `R(0.3)<=5` 都定义为“没有 packing at all”，立即停止并
re-panel。dense probe 不作为停止门，但其 R、loss、plateau 必须完整记录。full 模式必须显式
`--resume` probe cache，并复用四格，不能重训或绕过停止判定。

### 5.3 G1-G5 的机械定义

- **G1**：cache 50 格齐全；每格原始 seed 数=5；每格 converged 数≥4；只有 converged seed
  进入中位数与 hypothesis gate。
- **G2**：对两 arm 和每个 τ，dense `R<=5` 与 sparse `R>5` 都分别在至少 4/5 kept seeds 成立。
  所有 τ/arm 都成立才 pass；所有 sparse τ/arm 都在至少 4/5 seeds 保持 `R<=5` 才是 dedicated null；
  其余是 mixed-τ，route review。
- **G3**：每 arm 先按 cell 汇聚 kept seeds 的 `R(τ=0.5)` median，再对五个 S 的 median 做
  Spearman。要求序列 non-decreasing、`rho>=0.9`，并对五个 median 的 120 个排列计算 one-sided
  exact p，要求 `p<=0.05`。这就是 brief 中 pooled seeds 的固定解释，运行后不得换成挑 seed。
- **G4**：两 arm 都满足 sparse 相对 baseline 至少改善 2%，且 dense 不改善 2%。
- **G5**：importance dense 在 `τ=0.5` 的 represented set 等于 top-m，至少 4/5 kept seeds。

### 5.4 verdict 顺序

1. G1 失败、G3 失败或 mixed-τ → `review`；
2. dedicated null → `no_superposition_dedicated_only`；
3. G2 pass 且 G4 fail → `superposition_not_optimal`；
4. G1-G4 pass → `superposition_emerges_with_sparsity`。

`status=pass` 只表示有效测量或诚实 null；`review` 表示网格、收敛、单调性或 τ 一致性不支持
headline。G5 作为 `importance_ordered` 独立字段，不改写上述四分支。

## 6. 运行记录

预注册 commit 为 `2cc520f0`。随后运行四个 CPU probe，未使用 GPU：

```text
importance S=0.00: converged=True loss=0.04651546 R={0.3:5, 0.5:5, 0.7:5}
importance S=0.99: converged=True loss=0.00038292 R={0.3:13,0.5:11,0.7:11}
uniform    S=0.00: converged=True loss=1.25098765 R={0.3:17,0.5:6,0.7:4}
uniform    S=0.99: converged=True loss=0.00801130 R={0.3:20,0.5:20,0.7:20}
probe_ready=True
```

两个 sparse probe 在最宽松 τ=0.3 都有 R>5，packing 停止门通过。与此同时 dense loss 几乎
精确等于 `Var(Uniform[0,1]) × dropped importance`（uniform 为 `15/12=1.25`），暴露了上述
baseline 公式错误。完整网格在修复前没有启动；probe cache 的 W、b、trajectory、final loss
保持不变，修复只影响 Phase-B 解析 baseline。修复 commit 为 `076c61e3`，之后才 resume 同一
probe cache。修复没有改变 2% epsilon、plateau bound、τ grid、seed、训练配置或 verdict 顺序。
由同一 cache 重算出的 dense loss ratio 为 importance `1.001098`、uniform `1.000790`；sparse
ratio 为 `0.207583/0.161437`，因此 dense 假阳性被消除且不依赖重新训练制造 sparse finding。

### 6.2 完整 Phase A 网格

full 命令显式使用 `--resume probe_cache.pt`。四个 probe cell 直接复用，只训练剩余 46 格。
最终 cache 正好包含 `2 arms × 5 sparsities × 5 seeds = 50` cells；所有计算均为 CPU 单线程，
GPU 使用次数为 0。每格原始 W、b、61-point trajectory 和独立 final loss 都保留。

所有 50 格通过 plateau gate，最大相对 drift 远低于 0.02：importance 最大 `0.003905`，uniform
最大 `0.002766`。没有 seed 被排除，故每格 kept count 都是 5。

按五 seed 中位数汇总：

| arm | S | R(0.3) | R(0.5) | R(0.7) | loss / dedicated | median interference |
|---|---:|---:|---:|---:|---:|---:|
| importance | 0.00 | 5 | 5 | 5 | 0.999922 | 4.496098 |
| importance | 0.70 | 8 | 8 | 8 | 0.888654 | 4.666823 |
| importance | 0.90 | 9 | 9 | 9 | 0.470247 | 3.761261 |
| importance | 0.97 | 10 | 10 | 10 | 0.251235 | 3.711782 |
| importance | 0.99 | 13 | 11 | 11 | 0.192564 | 4.097567 |
| uniform | 0.00 | 15 | 6 | 4 | 1.000200 | 3.591874 |
| uniform | 0.70 | 10 | 10 | 10 | 0.793616 | 3.426114 |
| uniform | 0.90 | 18 | 18 | 18 | 0.569636 | 3.178149 |
| uniform | 0.97 | 20 | 20 | 20 | 0.290378 | 3.006457 |
| uniform | 0.99 | 20 | 20 | 20 | 0.161437 | 3.008364 |

### 6.3 G1-G5 复算

- **G1 convergence：pass。** 50 格齐全，每格 kept=5，无 silent exclusion。
- **G2 emergence：fail / mixed-τ。** importance 的三个 τ 全部通过；uniform 仅 τ=0.7 通过，
  τ=0.3/0.5 因 dense seed 中低 norm columns 被计为 represented 而失败。它也不是 dedicated null，
  因为 sparse uniform 在三个 τ 都稳定达到 R=20。
- **G3 monotone：pass。** importance 的 τ=0.5 medians 为 `[5,8,9,10,11]`，
  `rho=1.0, exact p=0.008333`；uniform 为 `[6,10,18,20,20]`，
  `rho=0.974679, exact p=0.016667`。
- **G4 optimality：pass。** importance dense `0.04646084` 对 baseline `0.04646447`，没有达到
  2% 改善；sparse `0.00035521` 对 `0.00184464`，ratio `0.192564`。uniform dense
  `1.25024962` 对 `1.25`；sparse `0.00801130` 对 `0.049625`，ratio `0.161437`。
- **G5 importance order：pass。** dense τ=0.5 的 represented set 在 5/5 seeds 都恰为
  `{0,1,2,3,4}`。

根据预注册顺序，mixed-τ 优先 route `review`。因此正式结果是
`status=review / decision=review`。这不是训练失败，也不是 `no_superposition_dedicated_only`：
数据清楚显示 sparse packing 且显著优于 dedicated baseline；但 uniform dense 对 norm threshold
不稳，使“对所有 τ、两 arm 同时满足 dense dedicated / sparse packing”的 headline 没有获得许可。
阈值不能在看到结果后删掉 0.3/0.5 或把 uniform control 排除。

### 6.4 Phase B 与产物

analyze CLI 只读 `phase_a_cache.pt`，未调用训练函数。它生成 JSON、CSV、text、Markdown、HTML
和一张双 arm R(τ)-vs-S 图。因为 formal status 是 review，CLI 预期返回 1；产物仍完整写出，
这个退出码是 gate 语义而不是运行崩溃。

- cache：117344 bytes，SHA-256
  `56280A73F70EB1D8F07DA1B978A70BB77DD8E97D2D24C048D35FCDB358CE6688`。
- JSON：SHA-256
  `46CA9442EC4C09CE973A40A5C3159FFF7524FBD01CCA418FD8ACF3D30DD5E0AF`。
- figure：SHA-256
  `F0D11474060E805B983C6CFE55674A0CC550FC288FF1C8AE87160791A4C68DBB`。

### 6.5 运行后阈值自审

相对修复 commit `076c61e3`，运行后源码只增加报告可见字段：失败的 G2 check 名称、最小 kept
count、两个 Spearman exact p。`SuperConfig`、训练、解析公式、probe gate、G1-G5 与 `decide()`
均未变化。actual cache test 会从 committed cache 两次重算并要求结果 byte-stable。

### 6.6 浏览器可读性复核

在仓库根目录临时启动只绑定 `127.0.0.1:8766` 的静态 HTTP 服务，随后按顺序使用 Playwright
MCP 打开 `superposition_v1276.html`、调整到 `1440×1000`、读取 accessibility snapshot、检查
console 并截取 full-page 图片。snapshot 直接读到 `verdict=review`、`g2_emergence=False`、
`g2_failed_checks=uniform|0.3, uniform|0.5`、`min_kept_count=5`，以及 G1/G3/G4/G5 为 True；
这证明 HTML 没有把正式 mixed-τ 结论渲染成通过。浏览器控制台统计为
`0 errors / 0 warnings`，也没有 favicon 404。最终截图是
`f/1276/图片/superposition-report-v1276.png`。截图完成后已关闭 Playwright 页面并停止临时
HTTP 服务 PID 27852，没有留下监听端口。

### 6.7 本地机械验证与 CI import 修复

定向命令 `python -m pytest tests/test_superposition_v1276.py -q` 在正式 cache 存在后得到
`16 passed`，因此 actual-cache 双重复算不再是 skip。`py_compile` 和 Ruff 均通过。
随后逐项执行当前 `.github/workflows/ci.yml` 中 coverage 之前的 20 组门禁：2793 个 Python
源文件编码与语法全部干净，文档可读性 15/15，CI hygiene 67/67，静态分析没有新增 issue，
命名预算没有新增 violation，类型诊断为 0，honest-measurement、artifact schema、file-size
ratchet、track closeout、path portability、历史 smoke 与 normalization guard 均通过；其中
normalization guard 包含 136 项测试。

首次推送前运行 `python -B scripts/run_test_coverage.py --out-dir runs/test-coverage-v1276
--fail-under 88.98`，结果为 `3547 tests in 1141.123s, OK`；line coverage 为 `90.74%`
（91133/100430），高于冻结 floor `88.98%`。浏览器截图 SHA-256 为
`C660B8B078D44F32391CD00C07B9D0B319EDE8656FF7C73E97297227DD45A609`。这些数字是最终
本地 verify 的输出，不是手工估算；本地 `runs/*v1276*` 只用于验证，提交前按 cleanup gate 删除。

首次远端 run `29190062065` 的 24 个前置 step 全绿，但正式入口使用 `unittest discovery`，CI
环境不安装 pytest；新测试模块在 `import pytest` 处失败，而且模块级测试函数也不会被 unittest
自动收集。这是测试入口适配缺陷，不是科学失败。修复没有把 pytest 加进生产依赖，而是移除该
依赖：浮点比较改用 `math.isclose`，skip 改用 `unittest.SkipTest`，并通过标准 `load_tests`
协议把原有 16 个模块级契约逐一包装成 `FunctionTestCase`。修复后
`python -B -m unittest tests.test_superposition_v1276 -v` 与 pytest 入口都得到 `16/16`；测试
数量和断言语义没有缩减。已发布的 `v1276.0.0` 不强制改写，CI 修复以 patch tag
`v1276.0.1` 收口。

## 7. 结论边界

v1276 给出强但受限的证据：两 arm 在高 sparsity 都能把超过 m=5 的 features 压进表示，并在
loss 上大幅击败最佳 dedicated 解；importance arm 还稳定恢复 top-5 顺序。正式 verdict 仍是
review，因为 uniform dense 的“是否 represented”在 τ=0.3/0.5/0.7 间不一致。下一步应由外部
评审判断这是 norm-based G2 定义需要未来单独研究，还是 dense control 的真实残余结构；本版
不做事后修门，也不据此声称 MiniGPT/LLM 内存在 superposition。

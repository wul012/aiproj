# v1110 运行截图与解释

## 本版目标

v1110 对 v1109 lookup-only receipt 做重建式 contract check：读取 `f/1109/解释/receipt-v1109`，再根据 receipt 内保存的 `receipt_index_review_path` 回到 `f/1108/解释/receipt-index-review-v1108`，重新构建 v1109 receipt 并比较关键字段。

本版不训练模型，不修改 v1109 receipt，不授权生产使用。它只证明 v1109 receipt 的用途、路径、lookup key、source evidence 和 no-promotion 边界可以从上游 review 重新推导出来。

## 真实运行命令

```powershell
python -B scripts\check_randomized_holdout_publication_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_v1110.py f\1109\解释\receipt-v1109 --out-dir f\1110\解释\receipt-check-v1110 --require-pass --force
```

## 关键输出

```text
status=pass
contract_check_ready=True
original_granted_use=downstream_governance_lookup_only
rebuilt_granted_use=downstream_governance_lookup_only
original_lookup_key_count=1
rebuilt_lookup_key_count=1
original_promotion_ready=False
rebuilt_promotion_ready=False
passed_check_count=46
failed_check_count=0
```

输出 sidecar 位于：

```text
f/1110/解释/receipt-check-v1110
```

JSON 是后续 v1111 index 的主要机器可读输入，HTML 和截图用于人工复核。

## 截图说明

截图文件：

```text
f/1110/图片/v1110-receipt-check.png
```

Playwright MCP 页面快照确认 HTML 中存在 `Contract Summary` 和 `Checks` 区块。

## 验证

- `python -m py_compile` 覆盖新增模块、artifact writer、CLI、测试和常量文件。
- focused v1110 tests：`6 passed in 0.33s`。
- 真实 CLI 使用 v1109 receipt 生成 v1110 check evidence。
- source hygiene：`2295/2295 clean`。
- `git diff --check` 通过。

## 一句话总结

v1110 把 v1109 receipt 推进为可从 v1108 review 重建并逐字段复核的 contract-checked governance artifact。

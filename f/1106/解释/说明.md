# v1106 运行截图与解释

## 本版目标

v1106 对 v1105 lookup-only receipt 做重建式 contract check：读取 `f/1105/解释/receipt-v1105`，根据 receipt 里保存的 `receipt_index_review_path` 回到 `f/1104/解释/receipt-index-review-v1104`，重新构建一份 v1105 receipt，并比较关键字段是否一致。

本版不训练模型，不改变 v1105 receipt，也不把 receipt check 解释成模型能力提升。它只证明：v1105 receipt 的路径、用途、digest、lookup-only 范围和 no-promotion 边界可以从上游 review 再推导出来。

## 真实运行命令

```powershell
python -B scripts\check_randomized_holdout_publication_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_v1106.py f\1105\解释\receipt-v1105 --out-dir f\1106\解释\receipt-check-v1106 --require-pass --force
```

## 关键输出

```text
status=pass
contract_check_ready=True
original_granted_use=downstream_governance_lookup_only
rebuilt_granted_use=downstream_governance_lookup_only
original_lookup_key_count=1
rebuilt_lookup_key_count=1
original_promotion_ready=False
rebuilt_promotion_ready=False
passed_check_count=46
failed_check_count=0
```

输出 sidecar 位于：

```text
f/1106/解释/receipt-check-v1106
```

包含 JSON、CSV、TXT、Markdown 和 HTML。JSON 是后续 v1107 index 的主要机器可读输入，HTML 和截图用于人工复核。

## 截图说明

截图文件：

```text
f/1106/图片/v1106-receipt-check.png
```

截图来自 Playwright MCP 打开的 HTML sidecar。页面快照确认标题、`Contract Summary` 和 `Checks` 区块可见，说明 contract check 的汇总和逐项检查能被人工读取。

## 验证

- `python -m py_compile` 覆盖新增模块、artifact writer、CLI、测试和常量文件。
- focused v1106 tests：`6 passed in 0.32s`。
- 真实 CLI 使用 v1105 receipt 生成 v1106 check evidence。
- source hygiene：`2279/2279 clean`，无 BOM、无语法错误、无兼容性错误。
- `git diff --check` 通过。

## 一句话总结

v1106 把 v1105 receipt 从“已记录”推进为“可从 v1104 review 重建并逐字段复核”的 contract-check 证据。

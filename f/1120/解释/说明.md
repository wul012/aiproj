# v1120 运行截图和解释

## 本版目标

v1120 审阅 v1119 receipt index，确认它仍然只用于 downstream governance lookup，并且可以进入下一步 lookup-only receipt recording。

本版不训练模型，不修改 v1119 index，也不把任何 candidate 标记为 production promotion ready。

## 输入与输出

输入目录：

```text
f/1119/解释/receipt-index-v1119
```

输出目录：

```text
f/1120/解释/receipt-index-review-v1120
```

核心输出包括 JSON、CSV、TXT、Markdown 和 HTML。JSON 是 v1121 receipt recording 的机器可读输入，HTML 和截图用于人工复核。

## 运行结果

真实 CLI 输出确认：

```text
status=pass
review_ready=True
receipt_index_row_count=1
lookup_key_count=1
source_evidence_count=2
lookup_ready=True
contract_check_ready=True
promotion_ready=False
passed_check_count=22
failed_check_count=0
```

`promotion_ready=False` 是本版关键边界，表示该审阅只批准 lookup-only receipt recording，不批准模型上线。

## 截图

截图文件：

```text
f/1120/图片/v1120-receipt-index-review.png
```

Playwright MCP 打开 HTML 后确认页面包含：

- `Review Summary`
- `Receipt Index Rows`
- `Source Evidence`
- `Checks`

## 验证

本版已执行：

```text
python -m py_compile ...v1120...
python -m pytest tests/test_...review_v1120.py -q -o cache_dir=runs/pytest-cache-v1120-focused
python -B scripts/check_source_encoding.py --out-dir runs/source-encoding-hygiene-v1120
git diff --check
```

结果：

```text
focused tests: 5 passed in 0.63s
source hygiene: 2335/2335 clean
failed checks: 0
```

## 一句话总结

v1120 把 v1119 receipt index 从“可查找”推进到“已审阅、可进入下一份 lookup-only receipt recording”。

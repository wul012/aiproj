# v1145 运行截图与解释

## 本版目标

v1145 承接 v1144 的 real holdout scorecard smoke，不再继续增加只读治理层，而是补一条真实训练证据：把 `loss_signal_bridge` 小语料 materialize 出来，跑一次 bounded CPU training，然后同时检查训练 loss 是否下降、decoder anchor 样本分布是否均衡。

本版不做模型 promotion，不宣称 MiniGPT 已具备稳定泛化能力，也不把一次小训练包装成生产模型质量结论。它只证明两件事：

- v1144 之后可以真实跑训练，而不是只复用历史 artifact。
- 当前训练语料里的 carry-forward、direct answer、decoder bridge 三类样本分布均衡，后续可以拿这个 checkpoint 做 decoder-anchor probe。

## 关键输出

- 主报告：
  - `f/1145/解释/model-capability-loss-signal-bridge-decoder-anchor-distribution-v1145/model_capability_loss_signal_bridge_decoder_anchor_distribution_v1145.json`
  - 同目录下还有 CSV、TXT、Markdown、HTML。
- 真实训练目录：
  - `f/1145/解释/model-capability-loss-signal-bridge-decoder-anchor-distribution-v1145/real-loss-signal-training-run/`
  - 包含 `metrics.jsonl`、`checkpoint.pt`、`tokenizer.json`、`run_manifest.json`、`train_config.json`、`loss_curve.svg`。
- 输入语料：
  - `loss-signal-bridge-inputs/loss_signal_bridge_decoder_anchor_corpus.txt`
  - `loss-signal-bridge-inputs/loss_signal_bridge_decoder_anchor_examples.jsonl`
  - `loss-signal-bridge-inputs/decoder_anchor_seed_revision.json`
  - `loss-signal-bridge-inputs/decoder_anchor_failure_diagnostic.json`
- 嵌套分布审计：
  - `decoder-anchor-distribution-audit/model_capability_route_promotion_bounded_real_replay_decoder_anchor_distribution_audit.json`
  - 同目录下还有 CSV、TXT、Markdown、HTML。
- 运行截图：
  - `f/1145/图片/model-capability-loss-signal-bridge-decoder-anchor-distribution-v1145.png`

## 运行命令

```powershell
python scripts/run_model_capability_loss_signal_bridge_decoder_anchor_distribution_v1145.py --out-dir output/model-capability-loss-signal-bridge-decoder-anchor-distribution-v1145 --require-pass --force
```

## 运行结果

```text
status=pass
decision=model_capability_loss_signal_bridge_decoder_anchor_distribution_ready
loss_signal_bridge_decoder_anchor_distribution_ready=True
loss_signal_ready=True
training_record_count=5
first_train_loss=3.307331
last_train_loss=2.307431
train_loss_delta=-0.999899
first_val_loss=3.320089
last_val_loss=2.825226
val_loss_delta=-0.494864
decoder_anchor_distribution_ready=True
decoder_anchor_example_count=9
carry_forward_share=0.3333
direct_answer_share=0.3333
decoder_bridge_share=0.3333
rebalanced_seed_needed=False
model_quality_claim=loss_signal_bridge_and_decoder_anchor_distribution_real_execution
promotion_ready=False
next_step=run_decoder_anchor_probe_against_v1145_checkpoint
failed_check_count=0
```

## 截图说明

Playwright MCP 通过临时本地 HTTP server 打开 HTML 报告，截图保存到 `f/1145/图片`。快照中确认了以下关键字段：

- `loss_signal_bridge_decoder_anchor_distribution_ready=True`
- `loss_signal_ready=True`
- `training_record_count=5`
- `train_loss_delta=-0.999899`
- `val_loss_delta=-0.494864`
- `decoder_anchor_distribution_ready=True`
- `decoder_anchor_example_count=9`
- `carry_forward_share=0.3333`
- `direct_answer_share=0.3333`
- `decoder_bridge_share=0.3333`
- `rebalanced_seed_needed=False`
- `promotion_ready=False`

## 清理记录

- 已停止临时 HTTP server：`PID=35660`。
- 已关闭本轮 Playwright 打开的 v1145 页面，并清理上轮遗留的 v1144 页面。
- `output/model-capability-loss-signal-bridge-decoder-anchor-distribution-v1145` 只是中间输出，后续完成提交前会删除，最终证据保留在 `f/1145`。

## 一句话总结

v1145 把 v1144 的 holdout scorecard smoke 往前推进成了一次真实小训练：loss 确实下降，decoder anchor 语料分布也通过审计，但仍保持 `promotion_ready=False`，下一步应拿本版 checkpoint 做 decoder-anchor probe。

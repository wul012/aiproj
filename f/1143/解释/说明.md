# v1143 required term coverage real execution 运行证据

v1143 将 v1137 `capability-regression-01` 从 `reuse_existing_evidence_paths` 的 manifest 行推进为一次真实、受限、可复核的 MiniGPT 生成执行。它只覆盖 `required_term_coverage` 这一个检查，不扩展到 holdout scorecard，不声明模型晋升，也不把单次命中解释成整体模型质量提升。

## 运行入口

```powershell
python scripts/run_model_capability_required_term_real_execution.py --out-dir output/model-capability-required-term-real-execution-v1143 --require-pass --force
python scripts/run_model_capability_required_term_real_execution.py --out-dir "f/1143/解释/model-capability-required-term-real-execution-v1143" --require-pass --force
```

脚本默认读取：

```text
f/1137/解释/model-capability-regression-suite-manifest-v1137/model_capability_regression_suite_manifest_v1137.json
```

并选择其中的：

```text
suite_id=capability-regression-01
check_id=required_term_coverage
primary_source=bounded_objective_loss_signal_bridge_target_only_memory_tokenizer_coverage_aware_holdout_dry_run.py
```

## 关键输出

报告目录：

```text
f/1143/解释/model-capability-required-term-real-execution-v1143
```

主要文件：

```text
model_capability_required_term_real_execution_v1143.json
model_capability_required_term_real_execution_v1143.csv
model_capability_required_term_real_execution_v1143.txt
model_capability_required_term_real_execution_v1143.md
model_capability_required_term_real_execution_v1143.html
tiny-required-term-checkpoint/checkpoint.pt
tiny-required-term-checkpoint/tokenizer.json
```

本版为证据生成了一个极小、确定性的 MiniGPT checkpoint 和 tokenizer。它不是训练成果，也不是 promoted checkpoint；它只用于证明正式 `MiniGPTGenerator` 能在 CPU 上完成一次真实 generation pass，并产生可评分 continuation。

## 结果摘要

```text
status=pass
decision=model_capability_required_term_real_execution_ready
required_term_real_execution_ready=True
suite_id=capability-regression-01
check_id=required_term_coverage
source_execution_mode=reuse_existing_evidence_paths
case_count=1
executed_case_count=1
passed_case_count=1
failed_case_count=0
required_terms=fixed, loss
hit_terms=fixed, loss
model_quality_claim=single_check_real_execution
promotion_ready=False
next_step=run_real_holdout_scorecard_smoke_v1144
failed_check_count=0
```

真实生成行：

```text
prompt=answer with exactly two words: fixed loss\nanswer:
continuation= fixed loss
required_terms=fixed, loss
hit_terms=fixed, loss
missed_terms=
case_pass=True
```

## 边界说明

- 本版从 v1137 manifest 中读取 lookup-only suite row，但输出的是一次真实 `MiniGPTGenerator` 执行。
- 本版只验证 `capability-regression-01`，不把 v1135-v1139 的其他计划项一起算作已执行。
- 本版保持 `promotion_ready=False`。
- 本版的模型质量声明限定为 `single_check_real_execution`。
- v1144 的合理下一步是 real `holdout_scorecard_smoke`，v1145 再接 `loss_signal_bridge` 和 `decoder_anchor_distribution`。

## 截图证据

Playwright MCP 打开 HTML 报告后确认页面显示：

- `required_term_real_execution_ready=True`
- `suite_id=capability-regression-01`
- `check_id=required_term_coverage`
- `passed_case_count=1`
- `failed_case_count=0`
- `model_quality_claim=single_check_real_execution`
- `promotion_ready=False`

截图保存为：

```text
f/1143/图片/model-capability-required-term-real-execution-v1143.png
```

## 清理

截图时临时启动了本地 HTTP server：

```text
python -m http.server 8765 --bind 127.0.0.1
```

截图完成后已停止该进程。

# v1317 训练前置校验

参数错误在读入语料、设定随机种子之前拒绝；实际分片大小在模型创建和日志恢复/清空之前检查。
校验只检查生效的设置，不修改值、默认参数、模型算法或 get_batch 的现有最小长度条件。

## 本地证据

- 旧代码两项失败：评估次数为零未拒绝；验证分片不足时报错前删除原日志。
- 同样的断言在新代码通过。新测试 9 项，包含 33 组错误选项子场景；完整定向回归 49 项通过。
- 独立三进程对比：与 v1316 完整 checkpoint 数据一致，指标、摘要、sample 和 tokenizer 字节一致；
  错误 CLI 请求返回非零且已有输出字节不变。
- Ruff 与新模块严格 mypy 通过。全部快速 CI 门结果记录在 [机器摘要](train-preflight.json)。
- 完整测试使用同提交 GitHub CI 的原入口和 88.98% 门槛；标签仅在成功后创建，不预填远端结果。

```powershell
python -B -m unittest tests.test_train_preflight tests.test_history_recovery tests.test_checkpoint_io tests.test_tokenizer_binding tests.test_rng_state tests.test_resume_rng tests.test_model_cli_behavior tests.test_architecture_boundaries -v
python -B scripts/run_test_coverage.py --out-dir runs/test-coverage-ci --fail-under 88.98
git show --no-patch v1317.0.0
```

[范围与需求矩阵](../../../docs/v1317-train-preflight.md)

边界：这是确定性输入预检，不保证后续不会 OOM、磁盘写入失败或训练发散；不验证源数据身份。
旧 checkpoint 的模型参数来源、no-sample 分支和合法零值保持原样。个人面试讲解不在交付范围内。

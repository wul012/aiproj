# v1269 运行证据说明

## 结论

v1269 完成静态分析历史债务的第一次深度压缩，并给 baseline 更新入口增加 shrink-only 保护。
ruff baseline 从 `545` 收紧到 `271`，净减少 `274` 条，下降约 `50.3%`。当前正式报告为
`status=pass`、`new_issue_count=0`、`strict_lint_issue_count=0`、
`strict_format_status=pass`。

## 需求-证据矩阵

| 需求 | 实现位置 | 证据 | 状态 |
| --- | --- | --- | --- |
| 有意脚本 bootstrap 不再污染历史缺陷 | 62 个精确行级 `# noqa: E402` | baseline 中 `E402=0` | 完整 |
| 兼容 facade 再导出显式化 | 27 个治理/报告 facade 与 tiny scorecard script 的精确 `F401` 标注 | 294 个 facade 回归测试通过 | 完整 |
| 删除真实无用导入与重复绑定 | `model_card.py`、`training_portfolio_batch_artifacts.py` | ruff 中 `F811=0` | 完整 |
| baseline 不能通过 update 吞入新问题 | `check_static_analysis.py` shrink-only 更新门 | 增长拒绝与子集允许测试 | 完整 |
| 不使用全局 ignore | 没有修改 `pyproject.toml` ruff ignore 范围 | diff 与配置检查 | 完整 |
| 不改变科学线语义 | 只调整 lint 元数据、静态门和治理 facade import | 无训练/fixture/verdict 修改 | 完整 |

## 可复现命令与结果

```powershell
python -m pytest tests\test_static_analysis.py tests\test_training_portfolio_batch.py tests\test_model_card.py tests\test_tiny_scorecard_comparison_smoke.py -q -o cache_dir=runs\pytest-cache-v1269-focused
python -m pytest <27 facade test files> -q -o cache_dir=runs\pytest-cache-v1269-facades
python -B scripts\check_static_analysis.py --out-dir f\1269\解释\static-analysis-ratchet
```

结果：聚焦 `31 passed`；facade 契约 `294 passed`；正式静态门
`current_issue_count=271`、`baseline_issue_count=271`、`new_issue_count=0`、
`baseline_update_allowed=True`、`baseline_update_blocker_count=0`。

## 归档

- `f/1269/解释/static-analysis-ratchet/static_analysis.json`
- `f/1269/解释/static-analysis-ratchet/static_analysis_issues.csv`
- `f/1269/解释/static-analysis-ratchet/static_analysis.md`
- `f/1269/解释/static-analysis-ratchet/static_analysis.html`
- `f/1269/图片/static-analysis-ratchet-v1269.png`

Playwright 先读取可访问性快照，再生成完整页面截图。第一次打开时发现 favicon 404，本版在 HTML
模板加入内联空 favicon 后重新生成；最终页面无控制台错误，并显示 `status=pass`、271/271、
零新增问题与 strict format 通过。

## 边界

本版没有把剩余 271 条历史问题隐藏掉，也没有声明全仓 ruff clean。它把已证明是兼容再导出或直接脚本
bootstrap 的项目从 baseline 转成精确本地说明，并修复少量真实重复；剩余 `F401/E702/E741/F841/F541/F821`
继续留在 baseline 中等待后续有边界的真实修复。

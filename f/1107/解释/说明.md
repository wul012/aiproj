# v1107 运行截图与解释

## 本版目标

v1107 把 v1105 lookup-only receipt 和 v1106 contract check 合并成 receipt index。这个 index 是给后续 review 和下游治理查找使用的稳定入口。

本版不训练模型，不新增 promotion 资格，不把 index_ready 解释成模型可上线。它只确认 receipt 与 check 都能被读取、互相匹配，并且仍然限定为 `downstream_governance_lookup_only`。

## 真实运行命令

```powershell
python -B scripts\build_randomized_holdout_publication_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_v1107.py --receipt f\1105\解释\receipt-v1105 --receipt-check f\1106\解释\receipt-check-v1106 --out-dir f\1107\解释\receipt-index-v1107 --require-index-ready --require-lookup-ready --force
```

## 关键输出

```text
status=pass
index_ready=True
lookup_scope=downstream_governance_lookup_only
lookup_key_count=1
source_evidence_count=2
lookup_ready=True
contract_check_ready=True
promotion_ready=False
passed_check_count=25
failed_check_count=0
```

输出 sidecar 位于：

```text
f/1107/解释/receipt-index-v1107
```

JSON 是后续 v1108 review 的机器可读输入；HTML、Markdown 和截图用于人工审阅。

## 截图说明

截图文件：

```text
f/1107/图片/v1107-receipt-index.png
```

Playwright MCP 页面快照确认 HTML 中存在 `Receipt Index Rows`、`Source Evidence` 和 `Checks` 区块。这说明 receipt 行、来源证据和检查结果都能被人工复核。

## 验证

- `python -m py_compile` 覆盖新增模块、artifact writer、CLI、测试和常量文件。
- focused v1107 tests：`4 passed in 0.56s`。
- 真实 CLI 使用 v1105 receipt 和 v1106 check 生成 v1107 index evidence。
- source hygiene：`2283/2283 clean`。
- `git diff --check` 通过。

## 一句话总结

v1107 把 v1105 receipt 与 v1106 contract check 汇总为可查找、可审阅、但仍不允许 promotion 的 lookup-only index。

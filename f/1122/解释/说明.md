# v1122 运行截图和解释

## 本版目标

v1122 对 v1121 lookup-only receipt 做重建式 contract check。它从 v1121 receipt 中读取 source review，再重新构建一份 receipt，并与原始 receipt 对比。

本版不训练模型，不修改 v1121 receipt，也不批准 production promotion。

## 输入与输出

输入目录：

```text
f/1121/解释/receipt-v1121
```

输出目录：

```text
f/1122/解释/receipt-check-v1122
```

## 运行结果

```text
status=pass
contract_check_ready=True
original_granted_use=downstream_governance_lookup_only
rebuilt_granted_use=downstream_governance_lookup_only
original_lookup_key_count=1
rebuilt_lookup_key_count=1
original_promotion_ready=False
rebuilt_promotion_ready=False
passed_check_count=46
failed_check_count=0
```

## 截图与验证

截图文件：

```text
f/1122/图片/v1122-receipt-check.png
```

Playwright MCP 确认 HTML 包含 `Contract Summary` 和 `Checks`。本版还通过 py_compile、focused tests（`6 passed in 0.33s`）、source hygiene（`2343/2343 clean`）和 `git diff --check`。

## 一句话总结

v1122 证明 v1121 receipt 可以从 v1120 review 重新推导出来，为 v1123 downstream index 提供 contract-check 证据。

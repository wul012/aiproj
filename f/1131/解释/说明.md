# v1131 运行截图与解释

v1131 是工程后期保养五版中的第二版，目标是把稳定读者文档从根 README 中拆出来。它没有删除 README 的历史版本记录，而是在 README 顶部增加 `Documentation Map`，并新增 `docs/overview.md`、`docs/model-training.md`、`docs/publication-receipts.md`、`docs/no-promotion-boundary.md`、`docs/versioned-artifacts.md` 五个稳定入口。

真实运行命令：

```powershell
python -B scripts\devtools\check_project_docs_readability_v1131.py --out-dir f\1131\解释\project-docs-readability-v1131 --require-pass --force
```

关键结果：

- `status=pass`
- `decision=project_docs_readability_split_ready`
- `doc_target_count=5`
- `ready_doc_count=5`
- `missing_readme_link_count=0`
- `failed_count=0`

截图文件：

- `f/1131/图片/v1131-project-docs-readability.png`

这张截图证明 HTML 报告可以打开，并且页面中能看到 `Documentation Targets` 表格和 `Recommendations` 区域。报告检查的不只是文件存在，还包括 README 链接、目标文档标题和必要术语，避免 docs 建好之后变成孤岛。

一句话总结：v1131 把 README 从唯一入口改成导航入口，并用可运行检查保护 docs 拆分后的可读性。

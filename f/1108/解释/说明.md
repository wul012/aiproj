# v1108 运行截图与解释

## 本版目标

v1108 审阅 v1107 receipt index，确认它可以进入下一次 lookup-only receipt recording。审阅重点是 index 是否仍然只有一条 lookup row、是否保留两条 source evidence、contract check 是否 ready，以及 promotion 是否继续关闭。

本版不训练模型，不改变 v1107 index，不授权生产使用。它只把 v1107 index 从“已生成”推进为“已审阅，允许作为下一次 receipt recording 的输入”。

## 真实运行命令

```powershell
python -B scripts\review_randomized_holdout_publication_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_v1108.py f\1107\解释\receipt-index-v1107 --out-dir f\1108\解释\receipt-index-review-v1108 --require-review-ready --require-lookup-ready --force
```

## 关键输出

```text
status=pass
review_ready=True
receipt_index_row_count=1
lookup_key_count=1
source_evidence_count=2
lookup_ready=True
contract_check_ready=True
promotion_ready=False
passed_check_count=22
failed_check_count=0
```

输出 sidecar 位于：

```text
f/1108/解释/receipt-index-review-v1108
```

JSON 是 v1109 receipt recording 的主要输入；HTML 和截图用于人工复核。

## 截图说明

截图文件：

```text
f/1108/图片/v1108-receipt-index-review.png
```

Playwright MCP 快照确认 HTML 中存在 `Review Summary`、`Receipt Index Rows`、`Source Evidence` 和 `Checks` 区块。

## 验证

- `python -m py_compile` 覆盖新增模块、artifact writer、CLI、测试和常量文件。
- focused v1108 tests：`5 passed in 0.35s`。
- 真实 CLI 使用 v1107 index 生成 v1108 review evidence。
- source hygiene：`2287/2287 clean`。
- `git diff --check` 通过。

## 一句话总结

v1108 把 v1107 lookup-only index 推进为可进入下一次 receipt recording 的 reviewed governance artifact。

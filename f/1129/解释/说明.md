# v1129 运行截图与解释

v1129 把 v1128 已经通过的 receipt index review 记录为新的 downstream lookup-only receipt。它的目标不是提升模型质量，也不是放开 production promotion，而是在 v1125-v1128 已经完成 receipt、contract check、index、review 之后，给下一轮只读消费方提供一个新的可查询收据入口。

本版真实运行命令：

```powershell
python -B scripts\record_randomized_holdout_publication_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_v1129.py f\1128\解释\receipt-index-review-v1128 --out-dir f\1129\解释\receipt-v1129 --require-receipt-ready --force
```

关键结果：

- `status=pass`
- `receipt_ready=True`
- `failed_count=0`
- `receipt_index_row_count=1`
- `source_evidence_count=2`
- `lookup_key_count=1`
- `promotion_ready=False`
- `passed_check_count=25`
- `failed_check_count=0`

截图文件：

- `f/1129/图片/v1129-receipt.png`

截图来自 Playwright MCP 打开的 HTML 报告页面。页面中可以看到 `Receipt Boundary`、`Consumer Receipts` 和 `Checks` 三块内容，并且能直接确认 source review、source receipt index、source receipt、source check、source origin review 和 source origin index 等路径仍然可追溯。

一句话总结：v1129 是本轮五版收口里的 receipt 记录版，它把 v1128 review 转为 lookup-only 下游收据，同时继续明确禁止把治理链证据误读成模型可生产晋级证据。

# v1260 A0 production excellence census 说明

本版执行 `docs/production-excellence-aiproj-brief.md` 的 A0：先做事实盘点和低风险入口文档修正，不进入 A1 的 ruff/mypy，也不改模型能力路线。

## 做了什么

- 记录当前 CI 实际步骤，并确认 `main` 最新 CI 为绿色。
- 新增 stdlib-only、warning-only 的 archive/runs inventory 脚本：`scripts/check_archive_runs_inventory.py`。
- 新增 A0 census 文档：`docs/aiproj-track-a0-census.md`。
- 新增稳定规则文档：`docs/archive-runs-inventory.md`。
- 用真实仓库生成 `f/1260/解释/archive-runs-inventory/*` 五格式证据。
- 用 Playwright MCP 打开 HTML 报告并截图到 `f/1260/图片/archive-runs-inventory-v1260.png`。
- 修正 `START_HERE.md` 中停留在 v1099 的陈旧版本描述，并在 README/docs map 中索引 A0 文档。

## 关键结果

```text
status=pass
decision=archive_runs_inventory_recorded
warning_only=True
archive_total_mb=390.0008
warning_count=0
e=224.7105 MB
runs=11.349 MB
```

A0 只建立预算和可见性，不搬迁历史证据，不删除归档，不把 warning 升成 fail。

## 边界

本版没有改变任何模型能力 verdict、训练数据、cached artifact、promotion 语义或 release readiness 结论。`status=pass` 在这里只表示 A0 census 已完成且库存工具可运行，不表示模型质量升级。

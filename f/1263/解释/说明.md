# v1263 运行解释

## 目标

v1263 完成 production-excellence A2 coverage ratchet：把 CI 覆盖率门从旧的
`80` 提升到 `88.98`。该 floor 来自 v1262 完整 unittest coverage 的
`90.98%` 观测基线减 2 个百分点。

## 运行命令

```powershell
python -B scripts\run_test_coverage.py --out-dir runs\test-coverage-v1263 --fail-under 88.98
```

## 关键结果

```text
status=pass
decision=continue_with_coverage_gate
line_coverage_percent=90.98
fail_under=88.98
coverage_gap=0.0
covered_lines=90257
num_statements=99207
missing_lines=8950
file_count=1373
```

## 归档内容

`解释/test-coverage/` 保留长期可读的 coverage report bundle：

- `test_coverage_report.json`
- `test_coverage_report.csv`
- `test_coverage_report.md`
- `test_coverage_report.html`

本次没有把 `runs/test-coverage-v1263/coverage.json` 复制进长期归档，因为该
原始 coverage 文件约 7.6MB，主要是 coverage.py 的中间数据；正式 JSON
报告已经包含 summary、每文件 coverage 行和最低覆盖文件列表，足以支持审计。

## 边界

这是一版工程质量门，不是模型能力版本。它不训练模型、不修改缓存实验产物、
不改变 `decide()` 判定语义、不提升任何 checkpoint，也不把 coverage pass
解释成模型质量提升。

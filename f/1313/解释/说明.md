# v1313 训练断点随机状态

本版新增可选的 checkpoint `rng_state`，在模型与优化器初始化完成后恢复，在真正的训练
续跑边界捕获。末尾采样与不在计划内的最后一次评估不进入续跑随机序列，计划内评估保持原行为。

## 结果

- 旧代码在停止步 1/2/3/5 上均不能复现连续训练；相同断言在新代码上通过。
- focused 23 项通过，独立 CLI 进程的 8 项状态/字节比较全部为 true。
- 完整单元覆盖率入口通过 3,585 项测试；89.26%（89,517 / 100,288）高于原 88.98% 门槛。
- 21 个 fast CI 命令通过；不修改依赖版本、科学实验、既有测试、fixture 或 baseline。

## 复现

在仓库根目录使用安装了 requirements 的环境：

```powershell
python -B -m unittest tests.test_rng_state tests.test_resume_rng tests.test_model_cli_behavior tests.test_architecture_boundaries -v
python -B scripts/run_test_coverage.py --out-dir runs/test-coverage --fail-under 88.98
```

机器摘要为 [checkpoint-rng.json](checkpoint-rng.json)，完整说明为
[工程记录](../../../docs/v1313-checkpoint-rng.md)。精确相等只适用于相同数据、配置、评估安排和
确定性运行环境；CUDA 分支只做了 CPU 主机上的模拟验证，不声称真实 GPU 轨迹通过。
这不是模型效果优化，也不等于偿清 v1312 的维护代码覆盖债。

# v1181 运行说明：grokking trajectory phase report

本版在 v1179 真实 grokking 实验和 v1180 证据复核之后，新增一层“轨迹阶段剖面”。

输入是：

```powershell
f\1179\解释\grok_v1179
```

运行命令：

```powershell
python scripts\analyze_grok_trajectory_v1181.py f\1179\解释\grok_v1179 --out-dir f\1181\解释\grok_trajectory_phases_v1181 --require-pass --force
```

核心结果：

- `status=pass`
- `decision=grokking_phase_profile_consistent`
- `seed_count=5`
- `row_count=10`
- `curve_count=10`
- `wd_on_delayed_grok_count=5`
- `wd_off_memorized_censored_count=5`
- `paired_phase_separation_count=5`
- `wd_on_low_plateau_rate_mean=0.8321336571672611`
- `wd_on_min_gap=10400.0`
- `wd_on_max_gap=25100.0`
- `longest_delay_seed=1341`

解释：

- weight decay arm 的 5 个 seed 都被分类为 `delayed_grok`，即先在训练集达到记忆，再经历长时间验证集低准确率平台，最后才泛化。
- no decay arm 的 5 个 seed 都被分类为 `memorized_only_censored`，即同样能记住训练集，但在 40000 step 预算内没有泛化。
- 这证明 v1179 的结论不是只靠 summary 字段得出，而是能从完整曲线压缩出一致的阶段结构。

边界：

- 本版不重新训练模型。
- 本版不扩大 v1179 的模型质量声明。
- 本版只把已有曲线转换成更易阅读、可检查、可讲解的 phase table。

截图：

- `f/1181/图片/grok-trajectory-phases-v1181.png`

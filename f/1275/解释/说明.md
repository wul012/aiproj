# v1275 Fourier ticket 实验预注册与运行证据

## 1. 目标与边界

本版检验一个具体、可证伪的问题：v1185 已交付的 `a+b (mod 97)` grokking
checkpoint 在稀疏化后是否保留 v1188/v1191 已确认的 Fourier 回路，以及由幅值剪枝找到的
稀疏子网络在 rewind 到初始化后能否重新 grok。结论范围固定为
`toy scale, own substrate`，不外推到 LLM，也不修改任何既有 checkpoint、缓存或 verdict。

冻结输入：

- checkpoint：`f/1185/解释/grok_checkpoint_v1185/grok_checkpoint_v1185.pt`
- SHA-256：`46F2A11A945F0BD140AF09FE298B28DD31062B4E3018EA159876C263F7DCB7DB`
- heldout：沿用 checkpoint 中 `p=97, train_frac=0.2, seed=1337` 复算得到的 7527 对。
- 已知频率：`[43, 3, 48, 26, 44]`，来自 v1191，运行后不得改选。

## 2. 需求—证据矩阵（运行前冻结）

| 需求 | 实现位置 | 机械证据 | 运行前状态 |
|---|---|---|---|
| Arm P 两种幅值剪枝、五档稀疏度 | `fourier_ticket_v1275.py::run_arm_p` | 单测 + Phase-A cache | 完整 |
| 同稀疏度随机掩码对照 | `fourier_ticket_v1275.py::make_mask` | 精确稀疏度与 5 个随机种子 | 完整 |
| 50% 稀疏停止门 | `arm_p_result` | 通过/阻断分支测试 + 实测阻断 | 完整 |
| Arm L 三 seed、ticket/random/reinit | `run_arm_l` | 12 个训练单元的 cache grid | 豁免：预注册停止门触发 |
| 预注册四分支 verdict | `decide` | 四分支合成 cache 测试 | 完整 |
| Phase B 不训练、从 cache 复算 | `analyze_fourier_ticket_v1275.py` | cache 重载 + byte-stable verdict | 完整 |
| 五格式报告和一张综合图 | readability writer + `plot_result` | 文件存在、HTML 浏览器检查 | 完整 |
| 冻结 checkpoint 未变 | 运行前后 SHA-256 | 哈希完全相同 | 完整 |

## 3. 新 family 设计说明（先于实现）

抽象名是 `Fourier ticket experiment`。数据面只有冻结 checkpoint、精确掩码、训练记录和
可序列化 cache；行为面只有 Arm P 测量、Arm L 复训、纯函数 `decide()` 与只读报告渲染。
`run_` 脚本唯一职责是训练并写 cache，`analyze_` 脚本只读 cache，禁止隐式重训。

## 4. 对抗性 design-panel premortem

### 4.1 循环论证风险

已知频率来自同一 v1185 checkpoint，因此 Arm P 不是“重新发现频率”的独立验证，而是检验
剪枝后是否保存已知回路。为避免把自选 top-5 当成命中，主判据使用固定频率集合的 power
share；同时报告剪枝模型自身 top-5 和与固定集合的 overlap。Arm L 才检验 rewind 后重新训练
的模型是否回到同一集合。任何运行后换频率、换 k 或挑最好 seed 的做法都判为失败。

### 4.2 对照充分性风险

幅值掩码可能只是“任意稀疏化都能工作”。Arm P 对每种 mode、每档 sparsity 使用 5 个确定性
随机掩码，保持完全相同的保留元素数。Arm L 对每个 seed 同时保留：同一初始化的幅值 ticket、
同一初始化的随机 mask，以及同一幅值 mask 的 fresh reinit。缺任一对照时，只能报告测量不完整，
不得给 lottery-ticket 结论。

### 4.3 预算诚实性风险

原始三层级完整网格每个 seed 需要 `1 dense + 3 levels × 3 sparse arms = 10` 次训练，与
`≤5 runs/seed` 上限不可同时满足。预注册降级为：先删除 0.875，再删除 0.75，只运行 0.5；
每 seed 共 4 次、三 seed 共 12 次。这里得到的是“首轮 50% IMP ticket”证据，不冒充完整
三轮 IMP 曲线。预算上限优先于层级数量，不能通过漏掉 random/reinit 对照来节省预算。

### 4.4 掩码语义风险

剪枝目标固定为 tied `token_embedding/lm_head` 的唯一参数和两块 MLP weight；attention、
position embedding、LayerNorm、bias 不剪。`per_tensor` 在每个目标张量内部精确保留 top-k，
`global` 在三块唯一参数的拼接幅值上精确保留 top-k。tied weight 只计一次。Arm L 使用 global
mask。所有稀疏度按实际零元素重新计数并写入 cache。

### 4.5 复现与污染风险

Phase A 只训练一次并写 cache；Phase B 禁止调用训练函数。训练前后都复算 checkpoint 哈希。
掩码随机数使用独立 generator，不污染模型初始化；split seed 与 init seed 分开记录。任何旧产物
mtime/hash 改变、cache 缺格或分析时重训都使本版无效。

## 5. 预注册门槛与 verdict（Phase A 前冻结）

Arm P：

- sparsity 固定为 `0.50, 0.70, 0.80, 0.90, 0.95`；mode 固定为 `per_tensor, global`。
- probe 通过当且仅当两种 mode 在 0.50 的 heldout accuracy 都 `>=0.90`；否则不启动 Arm L。
- 每种 mode 选择仍满足 accuracy `>=0.90` 的最高 sparsity；其固定频率 power share 必须比
  5 个匹配随机 mask 的均值高至少 `0.05`。两种 mode 都满足才算 `P passes`。

Arm L：

- seeds 固定为 `1337, 1338, 1339`，只运行 level `0.50`（预算降级已在运行前声明）。
- 每 seed 先按冻结 wd=1.0 配方训练 dense；稀疏 arm 的步数上限是该 seed dense `t_gen` 的
  `1.5×`，向上取整到 100-step evaluation interval。
- “grok”要求 `t_gen` 非空且最终 heldout accuracy `>=0.90`。
- “同一回路”要求 ticket 与对应 dense 的固定频率 share 差不低于 `-0.05`，且 top-5 overlap
  至少 `4/5`；三 seed 全部满足才算 aligned。
- control tie 定义为 random 或 reinit 的 grok seed 数不小于 ticket 的 grok seed 数。

`decide()` 顺序固定：

1. P 未通过 → `pruning_breaks_circuit`；
2. ticket 未在三个 seed 全部 grok，或任一 control tie → `ticket_needs_dense_start`；
3. ticket 全部 grok 且全部同回路 → `ticket_matches_fourier_circuit`；
4. ticket 全部 grok但回路未对齐 → `ticket_trains_but_unaligned`。

阴性分支是结果而非测试失败。`status=pass` 只证明 checkpoint 哈希、P 网格、停止逻辑、预算、
三 seed 与所需对照被完整测量；结构缺失才是 `status=review`。

## 6. 运行记录

预注册内容先进入 commit `f12f8793dc0bfcc9acaf1f05494f9d8502ffa373`，提交时间
`2026-07-12T12:07:01+08:00`。随后才运行以下测量。

### 6.1 独立 CPU probe

运行命令等价于：加载 `DEFAULT_CHECKPOINT` 到 CPU，调用 `run_arm_p`，将结果写到
`output/fourier-ticket-v1275/arm-p-probe.json`。关键输出：

```text
checkpoint_sha_before=46F2A11A945F0BD140AF09FE298B28DD31062B4E3018EA159876C263F7DCB7DB
per_tensor 0.50: acc=0.406536 known=0.314068 random=0.205057 margin=0.109011
global     0.50: acc=0.486781 known=0.305180 random=0.204559 margin=0.100621
probe_ok=False
p_pass=False
checkpoint_sha_after=46F2A11A945F0BD140AF09FE298B28DD31062B4E3018EA159876C263F7DCB7DB
```

两种幅值策略都在第一档就远低于 `0.90`。因此按照预注册门，不启动 Arm L。这里的
`actual_training_runs=0` 是正确停止的证据，不是遗漏；CUDA 可用，但本版没有创建任何 GPU
训练进程。

### 6.2 Arm P 全表

冻结 checkpoint 的未剪枝 heldout accuracy 是 `0.965989`，固定五频 power share 是
`0.305171`，top-5 正好为 `[43, 3, 48, 26, 44]`。

| mode | sparsity | heldout acc | random-mask acc mean | fixed share | random share mean | share margin |
|---|---:|---:|---:|---:|---:|---:|
| per_tensor | 0.50 | 0.406536 | 0.014694 | 0.314068 | 0.205057 | 0.109011 |
| per_tensor | 0.70 | 0.074266 | 0.009938 | 0.298791 | 0.166861 | 0.131930 |
| per_tensor | 0.80 | 0.028830 | 0.010602 | 0.273920 | 0.146738 | 0.127182 |
| per_tensor | 0.90 | 0.015677 | 0.010602 | 0.244907 | 0.123376 | 0.121531 |
| per_tensor | 0.95 | 0.011558 | 0.010097 | 0.211020 | 0.114988 | 0.096032 |
| global | 0.50 | 0.486781 | 0.013764 | 0.305180 | 0.204559 | 0.100621 |
| global | 0.70 | 0.119835 | 0.011346 | 0.305282 | 0.166480 | 0.138802 |
| global | 0.80 | 0.038927 | 0.010283 | 0.305102 | 0.149546 | 0.155556 |
| global | 0.90 | 0.012223 | 0.010814 | 0.305274 | 0.126980 | 0.178294 |
| global | 0.95 | 0.009698 | 0.009991 | 0.314153 | 0.116563 | 0.197590 |

结果有一个重要但不能夸大的细节：global magnitude mask 即使在 95% 稀疏时，频谱的固定
五频 share 仍约为 0.314，且 top-5 名称不变；然而准确率已经落到 chance 附近。这说明
“保留 Fourier 频率标签/总功率”并不等于“保留可执行的 Fourier circuit”。相位、维度间配对、
MLP 组合权重或低幅值协同参数被破坏后，频率统计仍能看起来对齐，但函数已经失效。因此
预注册分支名是 `pruning_breaks_circuit`，更精确的解释是“幅值剪枝破坏了回路功能”，而不是
声称所有 Fourier 结构都从权重中消失。

### 6.3 正式 cache 与 Phase B

```text
python -B scripts/run_fourier_ticket_v1275.py --device cuda \
  --out f/1275/解释/fourier_ticket_v1275/phase_a_cache.pt
status=pass verdict=pruning_breaks_circuit runs=0/15

python -B scripts/analyze_fourier_ticket_v1275.py \
  --cache f/1275/解释/fourier_ticket_v1275/phase_a_cache.pt \
  --out-dir f/1275/解释/fourier_ticket_v1275 \
  --figure f/1275/图片/fourier-ticket-v1275.png
status=pass decision=pruning_breaks_circuit
```

cache 只有 3616 bytes，SHA-256 为
`08F4F0C53B79937C9E212B0ADE84C6B5DF3643CDEC2A16078BDBA99034104405`。它只保存配置、
P 表格、停止原因与哈希，不保存另一个模型副本。JSON 证据 SHA-256 为
`8FAB243BBB62E48ED925B58B6356DBF9828E2B40A306989CC876CE9D65D889B8`；综合图 SHA-256
为 `FB5BD5F2B6AC7E61FD69EAE4FE314DB816D137086548188D437EE8F8D592E868`。

### 6.4 阈值后改自审

Phase A 后相对预注册 commit 的源码差异只包含：增加 `arm_l_skipped` 可见字段、给报告补准确
description、给图补 “Arm L not run” 标注，以及修正 legend 句柄；另有共享 HTML renderer
支持可选 description。`TicketConfig`、固定频率、accuracy floor、alignment margin、四分支顺序、
训练预算和 mask 实现均未修改。由此排除“看到阴性结果后调门槛”的失败类。

### 6.5 浏览器核验

Playwright MCP 通过临时 `127.0.0.1:8765` 静态服务打开最终 HTML。accessibility snapshot
确认页面同时可见 `pruning_breaks_circuit`、`arm_p_probe_ok=False`、
`actual_training_runs=0`、`arm_l_skipped=True`、10 行 Arm P 数据与三条边界建议；console
结果为 `0 errors / 0 warnings`。全页截图保存为
`f/1275/图片/fourier-ticket-report-v1275.png`。核验后浏览器 tab 与 PID 10384 静态服务均已停止。

### 6.6 最终机械验证

```text
focused pytest: 26 passed in 11.01s
full pytest:    3773 passed in 1172.75s
source check:   2789/2789 clean, BOM=0, syntax errors=0
workflow:       67/67 checks passed
ruff baseline:  271 historical, 0 new; strict format=pass
name budget:    7515 historical, 0 new
mypy scope:     22 targets, 0 diagnostics
file size:      21 warnings, 8 waived limits, 0 new/unwaived growth
coverage:       90.90% = 91072/100184 lines; floor=88.98%
browser:        0 console errors, 0 warnings
```

honest-measurement、artifact-schema、A-track closeout、archived-path portability、handoff assurance、
failure-smoke、scorecard plan、threshold-boundary plan、release-readiness drift 与 normalization guard
也全部返回 pass。full pytest 与 coverage runner 分别独立运行，没有把一条结果冒充两条证据。

## 7. 本版结论

在这个 v1185 mod-97 checkpoint 上，针对 tied embedding/unembed 与 MLP 的无结构幅值剪枝并
不是一张可直接保留能力的中奖彩票：第一档 50% 稀疏已经把 heldout accuracy 从 0.966 降至
0.407/0.487。固定 Fourier 五频仍在频谱中占优，却不足以维持计算功能。按预注册停止 Arm L
是本版最重要的科学纪律；本版没有得到“稀疏 ticket 可重训”的证据，也没有反向声称所有
剪枝方法都失败。

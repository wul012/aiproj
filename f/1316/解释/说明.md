# v1316 已提交日志边界与续训恢复

新 checkpoint 中的 `history_state` 记录 metrics.jsonl 的字节长度与 SHA-256。
同目录续训核对已提交前缀，先备份完整失败日志，再恢复活动日志；前缀损坏或备份冲突则拒绝。
备份是保留的运行证据，不是例行删除的缓存。另存目录、旧格式无标记保持原行为。

## 已有证据

- 旧实现故障重试得到 `[2, 3, 3]`；同一断言在新实现得到 `[2, 3]`。
- 最终定向 unittest：40 项通过，13.108 秒，退出码 0。
- 独立四进程实验：9 项检查通过，权重/优化器精确一致，指标与摘要字节一致，备份完整。
- 两个生产 helper 严格 mypy 通过；旧断言、参数向量、fixture 函数体的语法树对比不变。
- 本地完整覆盖率未获得完成结果，不算通过。完整原入口必须由同提交 GitHub CI 通过后
  才创建 v1316.0.0；实际测试数、覆盖率和 CI 链接记录在标签注释中。

```powershell
python -B -m unittest tests.test_history_recovery tests.test_checkpoint_io tests.test_tokenizer_binding tests.test_rng_state tests.test_resume_rng tests.test_model_cli_behavior tests.test_architecture_boundaries -v
python -B scripts/run_test_coverage.py --out-dir runs/test-coverage-ci --fail-under 88.98
git show --no-patch v1316.0.0
```

[机器记录](history-recovery.json) · [范围与需求矩阵](../../../docs/v1316-history-recovery.md)

范围限单写入者和文件级恢复，不是整个目录的事务、鉴权签名或掉电耐久性保证。
既有总体覆盖指标仍包含历史代码，不据此宣称已偿清维护代码覆盖债。

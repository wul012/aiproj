# v1182 运行说明：grokking paired contrast

本版在 v1181 阶段剖面之后，新增“同 seed 成对对照表”。

输入是：

```powershell
f\1181\解释\grok_trajectory_phases_v1181
```

运行命令：

```powershell
python scripts\analyze_grok_paired_contrast_v1182.py f\1181\解释\grok_trajectory_phases_v1181 --out-dir f\1182\解释\grok_paired_contrast_v1182 --require-pass --force
```

核心结果：

- `status=pass`
- `decision=grokking_weight_decay_pair_contrast_consistent`
- `seed_count=5`
- `phase_row_count=10`
- `pair_count=5`
- `matched_memorization_count=5`
- `on_delayed_grok_count=5`
- `off_memorized_censored_count=5`
- `no_decay_censored_count=5`
- `mean_final_val_gain=0.8003984`
- `min_final_val_gain=0.753687`
- `mean_steps_saved_by_grok_stop=24760.0`
- `longest_delay_seed=1341`
- `largest_final_val_gain_seed=1339`

解释：

- 每个 seed 都被合并成一行：左边是 weight decay arm，右边是 no-decay arm。
- 两个 arm 都在同一步完成训练集记忆，所以差异不是“一个学会了训练集、另一个没学会”。
- 只有 weight decay arm 进入 delayed grok；no-decay arm 虽然记住训练集，但在预算内没有泛化。
- final validation gain 的最小值仍为 `0.753687`，说明对照差异不是个别 seed 的偶然现象。

边界：

- 本版不重新训练。
- 本版不改变 v1179 的 toy-scale 边界。
- 本版只把 v1181 phase rows 改写成更适合讲解 weight decay counterfactual 的 paired table。

截图：

- `f/1182/图片/grok-paired-contrast-v1182.png`

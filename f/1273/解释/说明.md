# v1273 名称预算机械门运行证据

## 结论与边界

v1273 完成 elegance hotspot program 的 E-A1：对 `src/` 与 `scripts/` 建立文件名和公开标识符 40 字符预算，历史违规以稳定 digest 进入 shrink-only baseline，CI 对任何新增长名失败。本版不重命名历史 publication/receipt 路径，不触碰 schema registry、缓存运行产物、科学线训练、checkpoint、`decide()` 或 verdict。最终树全量回归为 `3759 passed in 1254.87s`。

首次采用后使用普通 CI 模式复跑，结果为 `status=pass`、`decision=continue_with_name_budget`、`current_violation_count=7515`、`new_violation_count=0`、`scan_error_count=0`。`pass` 表示“当前没有超出已登记历史集合的新债”，不表示 7,515 条存量已经消失。

## 需求—证据矩阵

| 需求 | 实现 | 证据 | 状态 |
|---|---|---|---|
| 名称规则可执行 | AST 扫描 filename 与公开定义 | `name-budget/name_budget.json` | 通过 |
| 历史只减不增 | digest 集合差与 fail-closed update | `tests/test_name_budget.py` 反例 | 通过 |
| 行移动不误报 | digest 不包含 line | `test_line_moves_keep_digest` | 通过 |
| 预算不能偷放宽 | baseline 固定 `max_name_length=40` | `test_invalid_budget_fails` | 通过 |
| CI 不可删除或错序 | workflow hygiene 检查命令与顺序 | focused 37 tests | 通过 |
| 新门自身受严格检查 | ruff strict paths + mypy scope 22 | static/type reports | 通过 |
| 不越过科学线 | 未改训练、缓存、verdict、`decide()` | git diff 边界检查 | 通过 |
| 为 E-A2 提供事实 | top 25 与类别 census | Markdown/HTML/CSV | 通过 |
| 全仓行为无回归 | 全量 pytest | `full-pytest.txt`：3759 passed | 通过 |

## 真实输入与输出

输入是当前仓库 `src/`、`scripts/` 和 `docs/elegance/name-baseline.json`。扫描 2,049 个 Python 文件，历史违规分类为 function 3,201、variable 2,671、filename 1,610、field 33。输出四件套位于本目录的 `name-budget/`；JSON 是自动消费事实，CSV/Markdown/HTML 是不同阅读视图。

执行命令：

```powershell
python -B scripts/check_name_budget.py --update-baseline --out-dir runs/name-budget-v1273-adopt
python -B scripts/check_name_budget.py --out-dir runs/name-budget-v1273
python -m pytest tests/test_name_budget.py tests/test_engineering_health.py tests/test_ci_workflow.py tests/test_static_analysis.py tests/test_type_analysis.py -q
python -m pytest -q -o cache_dir=runs/pytest-cache-v1273-full
```

第一次命令只用于首次采用；第二次才等价于 CI 常态。两次均通过，最终定向回归为 `38 passed`，其中包含 malformed baseline fail-closed 反例；最终树全量回归为 `3759 passed in 1254.87s`。新增源码及接线合计 399 行，低于 400 行版本上限。

## 后续判断

top offenders 集中在冻结 publication receipt/index/review 链，名称最长 210 字符。E-A2 不会直接按长度改名，而要先检查 schema、publication receipt、缓存 artifact 和调用路径 pin；若 top 5 都受保护，将按计划诚实跳过，不制造高风险重命名工作。

## 清理说明

最终仅保留 committed baseline、源代码、测试、文档和本目录精简报告。首次采用的重复 `runs/name-budget-v1273-adopt`、pytest cache、`__pycache__` 与浏览器临时目录在提交前删除；截图服务器在浏览器核验后停止。

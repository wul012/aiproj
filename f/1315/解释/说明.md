# v1315 tokenizer 语义绑定

新 checkpoint 增加 `tokenizer_sha256`，恢复时核对 tokenizer 类型、顺序词表、unknown token 和 BPE merge 顺序。

- 旧实现红灯：同大小 token ID 交换未被拒绝。
- 定向回归：最终 focused 31 项通过；字符/BPE/格式稳定性/拒绝前无输出均覆盖。
- 全量：`Ran 3593 tests in 1022.269s`，`OK`，退出 0。
- 覆盖率 89.26%（89552 / 100323），门槛 88.98% 不变。
- 21 个快速 CI 命令全部退出 0；摘要见 [checkpoint-write.json](checkpoint-write.json)。

边界：旧 checkpoint 无摘要仍可加载；该摘要不是签名，不验证数据和超参数，也不迁移推理入口。
完整范围见 [版本说明](../../../docs/v1315-tokenizer-binding.md)。

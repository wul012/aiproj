# v1261 运行说明：production-excellence A1 staged static analysis

本版继续执行 `docs/production-excellence-aiproj-brief.md`，从 A0 census 进入 A1 static analysis。

## 本版目标

v1261 的目标是把 ruff 接入为可复核、可失败、可持续收敛的静态分析门禁，同时避免一次性对 `src/` 和 `scripts/` 做大规模机械格式化。

## 边界

- 不改变任何模型训练逻辑。
- 不重跑历史能力实验。
- 不调整 cached artifact verdict。
- 不声称历史代码已经全部无 lint 债。
- 不引入 mypy；mypy scope 是 A1 后续版本。

## 核心结果

```text
status=pass
decision=continue_with_static_analysis_gate
current_issue_count=545
baseline_issue_count=545
new_issue_count=0
strict_lint_issue_count=0
strict_format_status=pass
```

这表示当前 `src/` 和 `scripts/` 还有 545 个历史 ruff findings，但它们已经写入 committed baseline。后续新增 finding 会失败；strict path 中的维护入口必须保持 lint-clean 和 format-clean。

## 关键文件

- `scripts/check_static_analysis.py`：ruff 门禁入口，输出 JSON/CSV/Markdown/HTML。
- `docs/static-analysis/ruff-baseline.json`：当前历史 finding baseline。
- `docs/static-analysis.md`：日常维护说明。
- `docs/aiproj-track-a1-static-analysis.md`：A1 执行记录。
- `.github/workflows/ci.yml`：新增 Static analysis gate。
- `src/minigpt/ci_workflow_hygiene_policy.py`：CI hygiene 现在要求 static analysis 存在并位于 coverage 前。
- `tests/test_static_analysis.py`：保护 baseline 比较、strict lint、strict format、CLI 输出和 baseline 更新路径。

## 证据

静态分析报告位于：

```text
f/1261/解释/static-analysis/
```

截图位于：

```text
f/1261/图片/static-analysis-v1261.png
```

## 一句话总结

v1261 把 aiproj 的 Python 静态分析从“没有工具”推进到“CI 中有 ruff、历史债有 baseline、新增问题会失败、维护入口必须干净”的阶段。

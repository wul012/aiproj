# v1278 README 展示版 — 证据与验证记录

## 本版做了什么

在根 README 的 Documentation Map 之前前置插入展品层（徽章行、双语 At a glance、
The science catalog 十三行判决表、How to trust a result here、Boundaries），并补上
v1277 漏掉的「Current version / Latest checkpoint」版本节（含披露）与本版自己的
版本节。既有内容一字未动；无实验、缓存、判决或门槛变化。

## 机读接口侦察（改动前）

- `model_capability_cadence` 以 `^## Latest v(\d+) checkpoint` 逐节机读 README，
  文件顺序即时间序 → 只允许前置插入；新增标题不得匹配该正则（已核对：不匹配）。
- `test_project_configuration` 要求七个 `(docs/xxx.md)` 链接存在（保留原索引 ✓）。
- `check_project_docs_readability` 要求 README 链接目标全部存在。
- `check_model_capability_honest_measurement` 扫描能力表述诚实性。

## 验证结果

- 聚焦门：`test_project_configuration` + `test_project_docs_readability` +
  `test_model_capability_cadence` + `test_maturity` = 32 passed。
- 真实仓库 `check_project_docs_readability`：status=pass，
  missing_readme_link_count=0（含指向 `代码讲解记录_工程保养阶段/README.md` 与
  `f/README.md` 的中文路径相对链接）。
- `check_model_capability_honest_measurement`：status=pass。
- 渲染验证：README 展品段经 GitHub 官方 GFM API（`gh api markdown`）渲染为 HTML，
  headless Edge 1440×2400 截图存于 `f/1278/图片/readme-exhibit-v1278.png`
  （徽章为外链图片、按 GitHub 端加载，本地渲染图中占位不影响版式验证）。
- 全量 pytest 与远端 CI 结论见版本提交与 Actions run。

## 数字来源对照（README 徽章/表格 → 已提交来源）

- tests 3,802 → v1277 收尾全量 pytest（提交 `8c7f8756` 附记）。
- coverage floor ≥88.98 → `.github/workflows/ci.yml` coverage 步骤。
- 新命名违规 0 → `scripts/check_name_budget.py`（CI 步骤）。
- 判决字符串 → 各版本 `f/<version>/解释/*/**.txt` 与 docs briefs（逐字引用）。

# v1287 post-grok purification — 运行证据与解读

Verdict：**`review`（reason=substrate_unsound）**——4e-3/1338（heldout 0.431）与
4e-3/1339（0.373）在延长视界终点低于 0.90，预注册的底座防护（brief 中明文
"也是 post-grok 稳定性防护"）触发。G0 其余全过：前缀门 33/33、11/11
`steps_run == horizon`（早停确实被关闭）、t_mem/t_gen 11/11 逐位复现
v1285/v1286 参照。预算精确命中预注册值：44/48 次、407,800/420,000 步。

## 一句话结论

**grok 后的雕刻是真实的、普遍的，但解在高 lr 下是亚稳的**：11 格里 9 格在
grok 后出现 train+val 同时崩塌的尖峰（连 1e-3 都有），全部在 100–300 步内
自愈——除了两格恰好在视界终点被"截尾在尖峰中间"（即两格 heldout 崩塌的真相：
不是永久死亡，是快照抽在坍塌瞬间）。纯化本身穿过这些尖峰单调推进。

## 逐格结果（anchor = 缓存 grok 时刻纯度；own = 终点自身 top-5 份额）

| lr | seed | anchor → C_ref(3×) | own_final | set_match | heldout | post-grok 尖峰 |
|---|---|---|---|---|---|---|
| 1e-3 | 1337 | 0.305 → 0.137 | **0.629** | ✗ 旋转 | 0.966→**1.0** | 0 |
| 1e-3 | 1338 | 0.312 → 0.312 | 0.504 | ✗ 旋转 | 0.952→**1.0** | 1 |
| 1e-3 | 1339 | 0.305 → 0.324 | 0.557 | ✗ 旋转 | 0.971→**1.0** | 4（最深 0.385） |
| 2e-3 | 1337/38 | 0.586/0.551 → 0.802/0.667 | 0.818/0.717 | ✗ | **1.0** | 2/3 |
| 4e-3 | 1337 | 0.716 → 0.937 | 0.937 | ✓ | **1.0** | 3（一次到 0.716） |
| 4e-3 | 1338 | 0.686 → 0.930 | 0.930 | ✓ | **0.431** | 4，末次被截尾 |
| 4e-3 | 1339 | 0.617 → 0.701 | 0.933 | ✗ | **0.373** | 3，末次被截尾 |
| 8e-3 | 1337–39 | 0.71–0.81 → 0.88–0.91 | 0.88–0.91 | ✓✓✓ | 0.97–1.0 | 0/3/1 |

崩塌的显微镜（全曲线）：4e-3/1338 在 5300 步 val=1.0，5400 步 val=0.431 且
**train 同步掉到 0.457**——这是训练不稳定尖峰（整个解爆掉），不是泛化选择性
失效；4e-3/1339 同型（2900→3000）。两者的"死亡步"都恰好是各自视界的最后
一格；同型尖峰在 4e-3/1337（3400 步 0.823）与 8e-3/1338（2000 步 0.567）
都出现过并完全恢复。

## 预注册判据的走向

- G0：substrate_unsound 短路 → **review**（预注册路由，判据零改动——第六连
  清洁链）；主判决不再展开（组级 climb/purity 中位数按设计留空）
- 下述解读全部标注为**探索性**（review 版本不产正式科学判决）：
  1. **物理纯化普遍存在**：9 个存活格 own 纯度全部上升——canonical
     0.305→0.50–0.63（近翻倍）、2e-3 →0.72–0.82、8e-3 →0.88–0.91
  2. **canonical 的纯化会旋转频率集**（3/3 set_match=✗，1337 的 C_ref 反而
     跌到 0.137 而 own=0.629）：设计的 climb 指标（缓存集份额）在 1e-3 上
     测不到它，旋转诊断按设计捕获——若底座门不先触发，主判决会在"旋转"上
     给出误导性的 lr_gated，双重理由支持 review
  3. **低 lr 的 post-grok 训练改善泛化**：1e-3/2e-3 五格 heldout 全部升到 1.0
  4. **v1286 的"终点纯度随 lr 单调"是截断快照**，不是平衡态——4e-3 延长后
     纯度 0.93 已超过 8e-3 的 0.89

## banked（v1288 候选）

- **尖峰解剖**：post-grok 崩塌-自愈尖峰的机理（范数/曲率在尖峰前后的轨迹；
  wd=0 对照是否消峰；视界外延几百步给两死格"复活验尸"）——同一套截断重跑
  机制即可
- canonical 频率集旋转的动力学（旋转发生在何时、往哪些频率去）

## 复现

```powershell
python scripts/run_grok_purification_v1287.py            # Phase A（GPU，44 runs）
python scripts/analyze_grok_purification_v1287.py        # Phase B（CPU，零重训）
python -m pytest tests/test_grok_purification_v1287.py -q
```

## decide() 自审

预注册 `19a429c8` 后判据与代码**零改动**；P2 止损未触发（8e-3/1337 干净）；
substrate_unsound 是 brief 里明文预告的稳定性防护的正式产出。本版把 v1286
末尾 banked 的问题（"post-grok 纯化那段值得一版"）直接观测了，答案比预期
大一号：纯化是真的，但它骑在一条会周期性塌方的山脊上。

# v1109 运行截图与解释

## 本版目标

v1109 把 v1108 review 记录为 lookup-only downstream receipt。它把“已审阅的 v1107 index”转成一份给后续 contract check 使用的 receipt artifact。

本版不训练模型，不改变 v1108 review，不授权生产使用。它只记录消费方、授权用途、source review 路径、lookup key 和 no-promotion 边界。

## 真实运行命令

```powershell
python -B scripts\record_randomized_holdout_publication_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_v1109.py f\1108\解释\receipt-index-review-v1108 --out-dir f\1109\解释\receipt-v1109 --require-receipt-ready --force
```

## 关键输出

```text
status=pass
receipt_ready=True
granted_use=downstream_governance_lookup_only
receipt_index_row_count=1
source_evidence_count=2
lookup_key_count=1
promotion_ready=False
passed_check_count=25
failed_check_count=0
```

输出 sidecar 位于：

```text
f/1109/解释/receipt-v1109
```

JSON 是 v1110 contract check 的主要输入；HTML 和截图用于人工确认 receipt 边界。

## 截图说明

截图文件：

```text
f/1109/图片/v1109-receipt.png
```

Playwright MCP 快照确认 HTML 中存在 `Receipt Boundary`、`Consumer Receipts` 和 `Checks` 区块。

## 验证

- `python -m py_compile` 覆盖新增模块、artifact writer、CLI、测试和常量文件。
- focused v1109 tests：`6 passed in 0.71s`。
- 真实 CLI 使用 v1108 review 生成 v1109 receipt evidence。
- source hygiene：`2291/2291 clean`。
- `git diff --check` 通过。

## 一句话总结

v1109 把 v1108 review 记录为下一份 lookup-only receipt，并为 v1110 的重建式 contract check 准备输入。

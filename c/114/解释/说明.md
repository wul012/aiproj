# v114 运行截图说明：Benchmark Scorecard Artifact Split

## 本版主题

v114 把 `benchmark_scorecard.py` 中的 JSON/CSV/Markdown/HTML 输出和渲染逻辑抽到 `src/minigpt/benchmark_scorecard_artifacts.py`。

这版不改 benchmark 评分公式、不改 scorecard schema、不改 CLI 输出文件名，只让评分核心和证据输出层分开维护。

## 截图说明

- `01-unit-tests.png`
  - 证明 benchmark scorecard artifact tests、scorecard regression、scorecard comparison、compileall 和全量 unittest 均通过。
- `02-benchmark-artifact-smoke.png`
  - 证明 `benchmark_scorecard.py` 行数下降，并且 scorecard 输出文件和 module pressure smoke 正常。
- `03-benchmark-artifact-structure-check.png`
  - 证明 `benchmark_scorecard_artifacts.py`、`tests/test_benchmark_scorecard_artifacts.py`、README、代码讲解、`c/114` 归档都已对齐。
- `04-benchmark-artifact-output-check.png`
  - 证明 scorecard JSON/CSV/Markdown/HTML 输出存在，CSV header 稳定，HTML escape 和 wrapper parity 正常。
- `05-playwright-benchmark-scorecard-html.png`
  - 证明生成后的 benchmark scorecard HTML 可以通过已安装 Google Chrome 真实渲染。
- `06-docs-check.png`
  - 证明 README 当前版本、tag 列表、截图说明、`c/README` 和项目成熟度讲解索引已经指向 v114。

## 关键代码

- `src/minigpt/benchmark_scorecard_artifacts.py`
  - 新增 benchmark scorecard 输出和渲染模块。
- `src/minigpt/benchmark_scorecard.py`
  - 保留评分核心，并把 public writer/render 函数委托给 artifact 模块。
- `tests/test_benchmark_scorecard_artifacts.py`
  - 新增 artifact 层测试，锁住 HTML/Markdown/CSV/JSON 和 wrapper 委托。

## 标签含义

`v114.0.0` 表示 MiniGPT 完成 benchmark scorecard artifact split。它延续 v110-v113 的厚模块治理路线，把评估证据输出层从评分核心中分离。

# v231 eval readiness chain integration test 运行说明

## 本版目标

v231 补强测试纪律，新增一条跨模块 integration test。

它解决的问题是：eval readiness 已经从 scorecard comparison 推到 decision 和 maturity narrative，但此前更多是分层单测，缺少一条用真实上一层产物喂给下一层的链路测试。

## 边界

本版不改变生产代码行为。

本版不新增报告层，也不扩大模型能力声明。

## 截图说明

- `图片/01-eval-readiness-chain-tests.png`：新增链路测试通过。
- `图片/02-eval-readiness-chain-smoke.png`：smoke 输出显示 comparison、decision、maturity narrative 的 readiness 字段连续传递。
- `图片/03-source-encoding-smoke.png`：source encoding 检查通过。
- `图片/04-full-unittest.png`：全量 unittest discovery 通过。

## 运行产物角色

本版中间输出只用于截图和验证，提交前删除。

`c/231` 只保留最终截图和本说明。

## Tag 含义

`v231.0.0` 表示 eval readiness 链路已有 integration test 保护，防止 scorecard comparison、promotion decision 和 maturity narrative 之间的 review 语义断裂。

## 一句话总结

v231 不扩功能，而是把 v226-v230 的 eval readiness 证据链用测试钉牢，提高 aiproj 的测试成熟度。

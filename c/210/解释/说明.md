# v210 运行截图说明

本版目标是把 v209 promoted baseline decision 已经保留的 handoff suite boundary，继续接到 promoted training-scale seed。这样下一轮 plan seed 不只继承真实 `suite_path`，也保留被选中 baseline 的上游 handoff suite guard。

本版不重新计算 suite consistency，不改变 seed 的 blocker 规则，不改变 `plan_training_scale.py` 命令生成逻辑。`--suite-name standard-zh` 仍然来自 selected scale run 的真实 suite path；handoff guard 只作为审阅证据进入 seed。

关键文件：

- `src/minigpt/promoted_training_scale_seed.py`
- `src/minigpt/promoted_training_scale_seed_artifacts.py`
- `scripts/build_promoted_training_scale_seed.py`
- `tests/test_promoted_training_scale_seed.py`
- `README.md`

这版截图要证明的事情：

1. 聚焦测试能通过，说明 baseline seed、summary、CSV/Markdown/HTML 和 CLI 都接住了 decision handoff suite guard 字段。
2. CLI smoke 能从 promoted decision 构建 ready seed，同时仍然生成下一轮 `plan_training_scale.py` 命令。
3. JSON/CSV/Markdown/HTML 都能看到 `selected_handoff_suite_consistency`、`selected_handoff_suite_mismatch_count` 和 `selected_handoff_selected_suite_path`。
4. README、`c/README.md` 和代码讲解记录已经把 v210 的位置、归档位置和链路角色对齐。

计划使用的截图文件：

- `01-promoted-training-scale-seed-tests.png`
- `02-promoted-training-scale-seed-smoke.png`
- `03-promoted-seed-artifact-suite-guard-smoke.png`
- `04-source-encoding-smoke.png`
- `05-full-unittest.png`
- `06-docs-check.png`

一句话总结：v210 把 promoted decision 的 handoff suite guard 接到 next-cycle seed，让下一轮训练规划入口继续保留 clean-comparison 边界。

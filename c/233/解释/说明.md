# v233 portfolio comparison maturity output 运行说明

## 本版目标

v233 把 training portfolio comparison 的 `best_score_maturity_status` 暴露到 Markdown 和 HTML。

它解决的问题是：v232 已经让 JSON 知道最高分组合是否带 review-level maturity，但人工阅读报告时仍可能看不到这个边界。

## 边界

本版不改变比较计算逻辑。

本版只补人读输出和测试断言。

## 截图说明

- `图片/01-training-portfolio-output-tests.png`：comparison 与 artifact 输出聚焦测试通过。
- `图片/02-portfolio-maturity-output-smoke.png`：smoke 输出显示 Markdown/HTML 都包含 `Best score maturity`。
- `图片/03-source-encoding-smoke.png`：source encoding 检查通过。
- `图片/04-full-unittest.png`：全量 unittest discovery 通过。

## 运行产物角色

本版中间输出只用于截图和验证，提交前删除。

`c/233` 只保留最终截图和本说明。

## Tag 含义

`v233.0.0` 表示 training portfolio comparison 的 best-score maturity guard 已经进入 Markdown/HTML 人读输出。

## 一句话总结

v233 让 v232 的 review 边界不只存在于 JSON，也出现在人工评审最常看的 Markdown 和 HTML 总览中。

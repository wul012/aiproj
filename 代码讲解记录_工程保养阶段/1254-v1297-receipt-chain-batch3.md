# 1254 - v1297 正式批 3:receipt_index 链家族全量清尾,长名存量 246 → 100

## 本版目标与不做什么

批量重命名放量到家族全量:receipt_index 链家族剩余的 **146 个**
>150 字符模块(v1296 只取了最长 30 个)一批清完,同规则短名化
(`receipt_chain_<role>_<version>`);这一层级的消费者脚本/测试文件名
与模块 stem 完全嵌入,机械推导命中——**43 个脚本 + 86 个测试**同批
短名化(脚本留双路径 shim,测试直接改名 + 消费者改写),共 **329 个
文件**的 import 一次成型。不做什么:不碰 packet 家族(43 个 >150,
下一批)与 <150 的中长名尾部;不碰模块内部历史符号名与产物文件名。

## 放量的依据与地图生成的教训内化

v1296 的两遍命名教训直接进了地图生成器:tail 折叠后先剥冗余
`receipt_` 再拼前缀,生成时即断言**零重复、零撞名、零超预算**
(40 字符文件名上限)——146 个短名一次通过,执行零返工。批脚本
仍是先全量断言后统一落盘;git mv 保历史。

## 数字

| 指标 | 批前 | 批后 |
|---|---|---|
| long_name_stock | 246 | **100** |
| max_stem_length | 191 | 191(packet 家族的最长者仍在,下一批) |
| name-budget 存量 | 7,515 | 7,515(换底,零净变) |
| 家族消费测试 | — | **473/473**(90 个文件) |
| 改写文件 | — | 329 |

15 个 CI 闸门本地全扫全绿;换底工具一次通过(本批地图已内化
预算检查,拒绝分支无需开火——工具的存在让地图生成器不敢偷懒)。

## 工程教训

1. **教训要内化到工具链上游**:v1296 在换底处被拦、当场补救;
   本批把同一条规则前移到地图生成断言,146 个名字零返工。
   拦截点越靠上游,返工半径越小。
2. **放量的前提是模式的全部分支都被踩过**:试点(6)踩出五类耦合,
   批 2(30)踩出预算拒绝,批 3(146)才敢全量——三步放大各自
   只引入一个新变量。
3. **同名家族的脚本/测试嵌入性是分层的**:上一批 0 命中、本批
   129 命中,都是机械推导如实报告的结果——按层处理,不跨层硬猜。

产物:146 模块 + 43 脚本 + 86 测试短名化,146+43 个 shim,329 文件
改写;elegance-baseline 拧紧至 long_name_stock=100;name-baseline
零净变换底。剩余:packet 家族 43 个 >150(含 max_stem 191 的持有者)
与 <150 尾部,下一批清 packet 后 max_stem 将大幅回落。

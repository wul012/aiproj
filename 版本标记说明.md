# 版本标记说明

本文件记录已经推送到 GitHub 的版本 tag，以及每个 tag 对应的项目状态。

| Tag | Commit | 简单解释 |
|---|---|---|
| `v1.0.0` | `1d6197c` | MiniGPT 第一版：完成字符级 tokenizer、dataset、Transformer 模型、训练脚本、生成脚本、单元测试、代码讲解记录和 `a/1` 截图归档。 |
| `v2.0.0` | `3308b80` | MiniGPT 第二版：加入训练历史 `metrics.jsonl`、训练摘要、`loss_curve.svg`、训练后样例输出、checkpoint resume、`plot_history.py` 和 `a/2` 截图归档。 |

查看本地 tag 注释：

```powershell
git tag -n99
```

查看某个 tag 对应的提交：

```powershell
git show v1.0.0 --no-patch --format=fuller
git show v2.0.0 --no-patch --format=fuller
```

当前策略：

```text
v1.x 保留 MiniGPT 初始学习闭环
v2.x 保留可复盘训练产物和 resume 能力
v3.x 计划推进 tokenizer 能力，例如可选 BPE tokenizer
```

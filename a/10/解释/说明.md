# 第十版运行调试归档说明

本轮版本目标：

```text
加入 sampling lab，用同一个 checkpoint 和 prompt 对比不同 temperature/top_k/seed 下的生成结果，并导出 JSON/CSV/SVG。
```

本轮不改变模型结构和训练逻辑，重点是观察生成阶段的采样策略。

---

# 1. 01-unit-tests.png

对应命令：

```powershell
python -B -m unittest discover -s tests -v
```

结果：

```text
sampling、comparison、dashboard、model_report、chat、attention、prediction、tokenizer、dataset、history、model 测试全部通过。
```

意义：

```text
证明 v10 新增的采样配置解析、结果统计、CSV/SVG 导出逻辑没有破坏已有能力。
```

---

# 2. 02-sampling-train-smoke.png

对应命令：

```powershell
python -B scripts/train.py --device cpu --out-dir tmp/v10-sampling-smoke --max-iters 2 --eval-interval 1 --eval-iters 1 --batch-size 2 --block-size 32 --n-layer 1 --n-head 1 --n-embd 32 --sample-tokens 12
```

结果：

```text
训练可以正常完成并写出 checkpoint、tokenizer、metrics 和 sample 产物。
```

意义：

```text
提供一个真实 checkpoint 给 sample_lab.py 使用。
```

---

# 3. 03-sample-lab.png

对应命令：

```powershell
python -B scripts/sample_lab.py --device cpu --checkpoint tmp/v10-sampling-smoke/checkpoint.pt --prompt token --max-new-tokens 18 --out-dir tmp/v10-sampling-smoke/sample_lab
```

结果：

```text
脚本输出 conservative、balanced、creative 三组采样结果，并写出 sample_lab.json、sample_lab.csv、sample_lab.svg。
```

意义：

```text
可以直接比较 temperature/top_k/seed 对生成 continuation 的影响。
```

---

# 4. 04-sampling-artifacts-check.png

对应检查：

```powershell
检查 sample_lab.json/csv/svg 是否存在，并查看每个 case 的 temperature、top_k、seed、char_count、unique_char_count。
```

结果：

```text
三类采样产物都存在，JSON 中记录了每组采样配置和 continuation 统计。
```

意义：

```text
采样实验既可以给人看，也可以给程序继续读取。
```

---

# 5. 05-docs-check.png

对应检查：

```powershell
python -B -c path existence check for README/docs/a10
```

结果：

```text
v10 的 README、代码讲解和运行归档文件都存在。
```

意义：

```text
项目继续保持 mini-kv 风格的“代码、测试、截图、讲解记录”闭环。
```

---

# 清理说明

```text
本轮 smoke 训练产生的 tmp/v10-sampling-smoke 会在最终清理阶段删除。
没有启动需要长期保留的后台进程。
```

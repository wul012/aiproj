# v365 运行截图说明

本版验证 `maturity_narrative` 能消费 v364 新增的 benchmark history `readiness_requirement`。

## 截图

- `图片/01-maturity-readiness-requirement-review.png`：使用一份本地样例 maturity narrative HTML 截图。样例里 benchmark scorecard 和 scorecard decision 仍是可推进状态，但 benchmark history readiness requirement 因 `insufficient_ready_entries` 失败，所以 portfolio status 被拉到 `review`。

## 运行证据

- 生成命令：`python -B scripts\build_maturity_narrative.py --project-root runs\__tmp_maturity_v365\project --out-dir runs\v365-maturity-readiness-review`
- 关键 stdout：
  - `portfolio_status=review`
  - `benchmark_history_readiness_requirement_failed_count=1`
  - `benchmark_history_readiness_requirement_exit_code_max=1`
  - `benchmark_history_readiness_requirement_failed_reasons=["insufficient_ready_entries"]`

## 边界

这张截图证明 readiness requirement 已进入成熟度叙事判定，不代表模型能力提升。它仍然需要真实标准 benchmark、多轮 checkpoint 对比和人工检查共同支持。

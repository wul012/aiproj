# v342 运行截图说明

## 本版主题

v342 把 `CI tiny scorecard plan digest gate` 从 CI workflow hygiene 的明细 check 提升为 summary 字段，并把这个 gate ready 状态透传到 project audit context / summary。

## 截图索引

1. `图片/01-targeted-tests.png`
   - 证明 CI workflow hygiene、project audit 相关单测通过。
2. `图片/02-ci-hygiene-command.png`
   - 证明当前 workflow hygiene 命令对 gate summary 仍然通过。
3. `图片/03-ci-workflow-summary.png`
   - 展示 CI workflow hygiene summary 中新增的 gate ready 字段。
4. `图片/04-audit-context-code.png`
   - 展示 project audit context 如何读取并透传 gate ready。
5. `图片/05-audit-summary-fields.png`
   - 展示 project audit summary 中的 gate ready 字段。
6. `图片/06-readme-v342-checkpoint.png`
   - 证明 README 当前版本、v342 checkpoint 和 tag 说明已更新。
7. `图片/07-source-encoding.png`
   - 证明 source encoding hygiene 通过。
8. `图片/08-py-compile-diff-check.png`
   - 证明本版相关 Python 文件通过 `py_compile`，且 `git diff --check` 返回码为 0。

## 关键结论

- v342 不改变 tiny smoke、wrapper 或 checker 的顺序。
- gate ready 状态现在可以被下游摘要直接消费。
- project audit 可以直接看到 CI tiny scorecard plan digest gate readiness。

## Tag 含义

`v342.0.0` 表示 CI tiny scorecard plan digest gate 已被提升为摘要级治理信号。

一句话总结：v342 让 plan digest gate 从“workflow 明细”变成“下游摘要可读字段”。

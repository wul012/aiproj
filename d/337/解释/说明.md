# v337 运行截图说明

## 本版主题

v337 新增 `scripts/run_ci_tiny_scorecard_comparison_smoke.py`，把 GitHub Actions 中的 tiny scorecard inline summary-check smoke 长命令收束成一个稳定 wrapper。

## 截图索引

1. `图片/01-targeted-tests.png`
   - 证明 wrapper 单测和 CI workflow hygiene 单测通过。
2. `图片/02-ci-hygiene-command.png`
   - 证明 workflow hygiene 当前输出 `status=pass`、`check_count=13`、`required_order_count=2`。
3. `图片/03-workflow-wrapper-step.png`
   - 展示 `.github/workflows/ci.yml` 现在调用 wrapper，而不是直接写长 benchmark 命令。
4. `图片/04-wrapper-config.png`
   - 展示 wrapper 固定的 CI tiny scorecard 预算和 summary-check 参数。
5. `图片/05-real-wrapper-smoke.png`
   - 证明 wrapper 能真实驱动 tiny scorecard comparison、decision 和 inline summary-check。
6. `图片/06-readme-v337-checkpoint.png`
   - 证明 README 当前版本、v337 checkpoint 和 tag 说明已更新。
7. `图片/07-source-encoding.png`
   - 证明 source encoding hygiene 通过。
8. `图片/08-py-compile-diff-check.png`
   - 证明本版相关 Python 文件通过 `py_compile`，且 `git diff --check` 返回码为 0。

## 关键结论

- v337 不改变模型训练逻辑、评分逻辑或 decision 逻辑。
- v337 把 CI 的 tiny scorecard 预算从 workflow 长命令移到可测试脚本。
- CI hygiene 仍然守住 wrapper、summary-check sidecar 和 smoke-before-coverage 顺序。
- 这仍然是评估证据链的工程治理，不是模型质量提升声明。

## Tag 含义

`v337.0.0` 表示 CI tiny scorecard inline smoke 已由专用 wrapper 承接，workflow 入口更稳定，固定预算由测试保护。

一句话总结：v337 让 tiny scorecard CI smoke 从“长命令配置”变成“可测试的稳定入口”。

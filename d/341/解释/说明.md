# v341 运行截图说明

## 本版主题

v341 把 `scripts/check_ci_tiny_scorecard_plan.py` 接入 GitHub Actions，并让 CI workflow hygiene 要求它在 tiny scorecard wrapper 之后、coverage 之前运行。这样 v340 的 plan digest checker 不再只是手动验收工具，而是正式 CI 质量门。

## 截图索引

1. `图片/01-targeted-tests.png`
   - 证明 CI workflow hygiene、plan checker、CI wrapper 相关单测通过。
2. `图片/02-ci-hygiene-command.png`
   - 证明当前真实 workflow 满足新增的 plan digest gate 规则。
3. `图片/03-ci-workflow-step.png`
   - 展示 GitHub Actions 中新增的 `CI tiny scorecard plan digest check` 步骤。
4. `图片/04-hygiene-policy-code.png`
   - 展示 CI hygiene 中新增 required command fragment 和 order rules。
5. `图片/05-hygiene-report-fields.png`
   - 展示 hygiene JSON 中的 required step/order 计数和 plan checker checks。
6. `图片/06-readme-v341-checkpoint.png`
   - 证明 README 当前版本、v341 checkpoint 和 tag 说明已更新。
7. `图片/07-source-encoding.png`
   - 证明 source encoding hygiene 通过。
8. `图片/08-py-compile-diff-check.png`
   - 证明本版相关 Python 文件通过 `py_compile`，且 `git diff --check` 返回码为 0。

## 关键结论

- v341 不改变模型训练、生成或评分逻辑。
- plan digest checker 已经进入 GitHub Actions。
- CI hygiene 会把缺失或顺序错误的 plan digest check 视为 workflow hygiene 失败。

## Tag 含义

`v341.0.0` 表示 CI tiny scorecard plan digest checker 已成为覆盖率前的 CI 质量门。

一句话总结：v341 让 tiny benchmark CI 证据链在进入 coverage 前完成独立 digest 验收。

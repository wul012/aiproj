# v392 promoted comparison maturity CI reasons 运行说明

## 本版主题

v392 把 promotion index 中的 selected/global handoff maturity CI regression reason counts，继续带到 promoted training scale comparison。

本版解决的问题是：promoted comparison 会过滤 clean-required CI-regressed promotion，但原报告只能说明 count 和 exclusion reason，不能展示 CI 回归原因分布。v392 让 comparison review 可以直接看到 full 和 comparison-ready 两套 reason counts。

## 关键改动

- `src/minigpt/promoted_training_scale_comparison.py`
  - promotion rows 读取 selected/global reason counts。
  - summary 聚合 full-promotion 和 comparison-ready reason counts。
  - recommendations 在 CI regression 路径下带上 observed reason-count detail。
- `src/minigpt/promoted_training_scale_comparison_artifacts.py`
  - CSV、Markdown、HTML 展示 full 和 comparison-ready reason counts。
- `scripts/compare_promoted_training_scale_runs.py`
  - stdout 打印 full 和 comparison-ready reason-count JSON。
- `tests/test_promoted_training_scale_comparison.py`
  - fixture 和断言覆盖 filtered clean-required inputs、stale compare-ready recheck、rendered outputs、CLI stdout 和 recommendation。

## 截图说明

- `图片/01-promoted-training-scale-comparison-maturity-ci-reasons-evidence.png`
  - 通过 Playwright MCP 打开 `解释/v392-promoted-training-scale-comparison-maturity-ci-reasons-evidence.html` 后截图。
  - 证明 v392 证据页能正常渲染，并展示 promoted comparison 字段链路、测试命令和边界说明。

## 验证命令

本版收口需要运行：

```text
python -m py_compile src/minigpt/promoted_training_scale_comparison.py src/minigpt/promoted_training_scale_comparison_artifacts.py scripts/compare_promoted_training_scale_runs.py tests/test_promoted_training_scale_comparison.py
python -m pytest tests/test_promoted_training_scale_comparison.py -q
python -m pytest -q
python -B scripts/check_source_encoding.py --out-dir runs/source-encoding-hygiene-v392
git diff --check
```

## 版本标记

建议 tag：

```text
v392.0.0 MiniGPT v392 promoted comparison maturity CI reasons
```

这个标记表示 promoted comparison 已经能携带 full 和 comparison-ready handoff maturity CI reason counts，跨 promoted run 比较报告可以直接解释 CI 回归排除原因。

## 一句话总结

v392 让 promoted comparison 从“知道哪些 promoted inputs 被 CI 回归排除”推进到“知道这些 CI 回归为什么发生”。

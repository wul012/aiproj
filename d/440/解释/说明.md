# v440 baseline-candidate threshold boundary diagnosis gate 运行说明

## 本版目标

v440 在 v439 `review_diagnosis` 基础上增加严格 gate：默认探索模式仍然可以输出 `status=pass`，但当调用方传入 `--require-diagnosis-pass` 时，只要诊断不是 `pass` 就会返回非零退出码。

本版不重跑训练、不改变候选结论，也不把 tiny smoke 当作模型质量证明。它的目标是让“候选未接受”从可读诊断进一步变成可选 CI/晋升阻断信号。

## 运行命令

```powershell
python -B scripts\run_baseline_candidate_threshold_boundary_smoke.py --smoke-summary d\438\解释\baseline-candidate-threshold-boundary-smoke\tiny-scorecard-comparison-smoke\tiny_scorecard_comparison_smoke_summary.json --out-dir d\440\解释\baseline-candidate-threshold-boundary-diagnosis-gate --thresholds 0:1:0.5 --require-diagnosis-pass --force
```

本轮命令预期返回 `2`，因此外层脚本捕获退出码并写入：

```text
d/440/解释/baseline_candidate_threshold_boundary_diagnosis_gate_exit.txt
```

## 关键结果

```text
status=pass
decision=live_threshold_boundary_review
source_mode=reuse_summary
execution_gate_mode=diagnosis_strict
execution_require_diagnosis_pass=True
execution_expected_exit_code=2
accept_count=0
reject_count=3
review_diagnosis_status=review
review_diagnosis_decision=candidate_not_accepted
review_issue_count=1
review_action_count=2
captured_exit=2
```

这里的 `status=pass` 说明 smoke/matrix/handoff-check 链路正常；`execution_expected_exit_code=2` 和实际 `captured_exit=2` 说明严格 diagnosis gate 正确阻断了全阈值拒绝的候选。

## 输出证据

```text
d/440/解释/baseline-candidate-threshold-boundary-diagnosis-gate
d/440/解释/baseline_candidate_threshold_boundary_diagnosis_gate_stdout.txt
d/440/解释/baseline_candidate_threshold_boundary_diagnosis_gate_exit.txt
d/440/图片/01-baseline-candidate-threshold-boundary-diagnosis-gate.png
d/440/解释/baseline_candidate_threshold_boundary_diagnosis_gate_snapshot.md
```

HTML 截图展示 `Gate mode=diagnosis_strict`、`Expected exit=2`、`Diagnosis=candidate_not_accepted`、`Accept count=0` 和 `Reject count=3`。

## 截图说明

本轮先尝试 Playwright MCP，但 MCP transport 已关闭；随后按仓库规则回退到 Playwright CLI，使用临时本地 HTTP 服务和本机 Chrome channel 截图。临时服务已在收尾时停止。

## 验证结果

```powershell
python -m py_compile src\minigpt\baseline_candidate_threshold_boundary_smoke.py scripts\run_baseline_candidate_threshold_boundary_smoke.py tests\test_baseline_candidate_threshold_boundary_smoke.py
python -m pytest tests\test_baseline_candidate_threshold_boundary_smoke.py tests\test_baseline_candidate_threshold_matrix.py -q -o cache_dir=runs\pytest-cache-v440-focus
python -m pytest -q -o cache_dir=runs\pytest-cache-v440
python -B scripts\check_source_encoding.py --out-dir runs\source-encoding-hygiene-local
git diff --check
```

结果：

- py_compile：通过。
- 聚焦测试：`14 passed`。
- 全量测试：`765 passed`。
- source encoding：`status=pass`，`source_count=344`，`syntax_error_count=0`。
- `git diff --check`：通过，仅出现 Git 对 Markdown/Python 文件的 CRLF 提示。

## 一句话总结

v440 把 baseline-candidate boundary diagnosis 从“可解释”推进到“可选阻断”：同一份候选未接受证据在探索模式可归档，在严格 gate 下会稳定返回退出码 `2`。

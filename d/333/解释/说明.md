# v333 运行截图说明

## 本版主题

v333 把 `remediation_gate.issues[]` 的首条 issue metadata 镜像到 tiny scorecard comparison smoke 的文本 summary。

新增文本字段：

```text
remediation_gate_first_issue_code
remediation_gate_first_issue_severity
remediation_gate_first_issue_category
remediation_gate_first_issue_action_code
remediation_gate_first_issue_owner_scope
```

## 截图索引

1. `图片/01-focused-tests.png`
   - 证明聚焦测试通过，默认和严格路径都覆盖了文本 issue 字段。
2. `图片/02-strict-smoke-text-issue-fields.png`
   - 证明 strict smoke stdout 中出现 first issue code/severity/category/action/owner。
3. `图片/03-summary-text-fields.png`
   - 展示 `.txt` summary 中新增的 line-oriented 字段。
4. `图片/04-readme-v333-checkpoint.png`
   - 证明 README 当前版本、v333 checkpoint 和 tag 说明已更新。
5. `图片/05-full-unittest.png`
   - 证明全量 `unittest discover` 通过。
6. `图片/06-source-encoding.png`
   - 证明 source encoding hygiene 通过。
7. `图片/07-py-compile.png`
   - 证明本版相关文件通过 `py_compile`。
8. `图片/08-diff-check.png`
   - 证明 `git diff --check` 返回码为 0。

## 关键结论

- v333 不改变 JSON `remediation_gate.issues[]` 结构。
- v333 不改变 strict gate 的阻断条件。
- 文本 summary 现在能直接表达首条 issue 的稳定分类字段。
- 这让 shell-only CI 不必解析 JSON，也能知道为什么 strict gate stop。

## Tag 含义

`v333.0.0` 表示 clean remediation gate 的首条 issue metadata 已进入 text summary。

一句话总结：v333 让 remediation gate 的结构化失败原因同时服务 JSON 消费者和 line-oriented CI 消费者。

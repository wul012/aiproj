# v338 运行截图说明

## 本版主题

v338 让 `scripts/run_ci_tiny_scorecard_comparison_smoke.py` 在每次运行后写出 `ci_tiny_scorecard_smoke_plan.json` 和 `.txt`，保存固定 CI tiny benchmark 配置、summary-check 路径、最终命令和 return code。

## 截图索引

1. `图片/01-targeted-tests.png`
   - 证明 wrapper plan 单测和 CI workflow 单测通过。
2. `图片/02-ci-hygiene-command.png`
   - 证明 workflow hygiene 仍然通过。
3. `图片/03-wrapper-plan-code.png`
   - 展示 wrapper 中新增的 plan 文件名、plan 构造、文本渲染和写出逻辑。
4. `图片/04-real-wrapper-smoke.png`
   - 证明真实 wrapper smoke 跑通，并输出 `ci_plan_json`、`ci_plan_text`。
5. `图片/05-plan-json-fields.png`
   - 展示 plan JSON 中的固定配置、sidecar 路径、command text 和 return code。
6. `图片/06-readme-v338-checkpoint.png`
   - 证明 README 当前版本、v338 checkpoint 和 tag 说明已更新。
7. `图片/07-source-encoding.png`
   - 证明 source encoding hygiene 通过。
8. `图片/08-py-compile-diff-check.png`
   - 证明本版相关 Python 文件通过 `py_compile`，且 `git diff --check` 返回码为 0。

## 关键结论

- v338 不改变底层 tiny scorecard 训练、比较或 decision 逻辑。
- wrapper plan 是 invocation 审计证据，不是新的模型分数。
- plan 在底层 smoke 运行后写出，避免被 `--force` 清空输出目录。
- plan 和 smoke summary、summary-check sidecar 一起构成更完整的 CI 证据链。

## Tag 含义

`v338.0.0` 表示 CI tiny scorecard wrapper 已能持久化 invocation plan artifacts。

一句话总结：v338 让 CI tiny scorecard wrapper 的固定预算和实际命令可被机器复查。

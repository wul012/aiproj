# v714 运行截图和解释：pair-readiness structured-template contract

## 本版目标

v714 读取 v713 的 repair comparison，把已经回归的 loss-retention prefix repair 路线转成新的结构化模板 contract。

本版不训练模型，不声称模型能力提升；它只确认下一步训练输入可以从失败路线中安全转向 `prompt -> answer` 结构化目标。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_structured_template_contract.py e\713\解释\model-capability-required-term-pair-readiness-repair-comparison --out-dir e\714\解释\model-capability-required-term-pair-readiness-structured-template-contract --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_structured_template_contract_ready
failed_count=0
contract_ready=True
structured_training_row_count=14
source_default_hit_delta=-1
model_quality_claim=contract_only
```

## 截图说明

- `e/714/图片/v714-pair-readiness-structured-template-contract.png`

截图展示了 structured rows、source delta、contract checks 和 next action。

## 关键结论

- v713 已证明 loss-retention patch 回归，`source_default_hit_delta=-1`。
- v714 不继续堆 `loss=lo/los` 这类单边 prefix weighting。
- 新 contract 使用 14 条结构化模板行，保留 `fixed=`、`loss=` 与 `fixed=|loss=` 三个 heldout probes。
- `fixed=|loss=` 没有作为 training row 出现，避免 pair probe 泄漏。

一句话总结：v714 把 v713 的失败路线收束成可 materialize 的结构化训练 contract，为下一版真实训练输入准备好边界。

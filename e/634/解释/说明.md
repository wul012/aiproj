# v634 required-term pair loss-internal balanced-anchor seed 3535 运行说明

## 本版目标

v634 运行 v629 的第二个 joint variant：`balanced_anchor`。它用于验证均衡 anchor 是否比 v630 joint-cycle 更能同时保住 generation 和 internal。

本版是真实 tiny 训练。

## 训练配置

```text
seed=3535
corpus_mode=equals_surface_no_pair_id_loss_internal_balanced_anchor_repair
repeat=320
bridge_repeat=24
max_iters=1800
block_size=16
n_embd=64
learning_rate=0.02
device=cpu
```

## 生成结果

```text
status=pass
decision=required_term_pair_coexistence_refresh_no_pair_full
training_status=pass
checkpoint_exists=True
pair_full_observed=False
```

replay 观察：

```text
fixed= -> fixed fixed
loss=  -> fixed fixed
```

解释：balanced-anchor 退回 fixed-only，不能替代 v630 joint-cycle。

## 截图说明

- `图片/v634-loss-internal-balanced-anchor-seed-3535.png`
  - 训练/replay HTML 截图，证明 checkpoint 存在，但 `Pair full observed=False`。

## 结论

v634 是有价值的负结果：均衡 anchor 并没有解决 loss-side generation，后续应继续以 v630 joint-cycle 为主线。

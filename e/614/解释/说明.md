# v614 required-term pair contrast-free objective comparison 运行说明

## 本版目标

v614 比较 v611、v612、v613 三条 contrast-free objective route。目标是用同一个比较器确认是否出现 pair-full，或者只是新的 fixed-only/loss-only tradeoff。

本版不训练新 checkpoint，只做比较归档。

## 输入证据

```text
v611 contrast-free
v612 delimiter-span
v613 context-switch
```

## 结果

```text
status=pass
decision=select_fixed_retention_route_for_loss_rebalance
compared_report_count=3
pair_full_report_count=0
fixed_only_tradeoff_report_count=2
loss_only_tradeoff_report_count=0
union_hit_terms=fixed
fixed_recovery_route=v612-delimiter-span
```

解释：

- 三条 route 都没有 pair-full。
- v612 与 v613 都是 fixed-only。
- union hit terms 只有 `fixed`，说明新的 objective 没有恢复 loss。

## 截图说明

- `图片/v614-contrast-free-objective-comparison.png`
  - Playwright MCP 截取的 v614 HTML 报告。
  - 证明 `pair_full_report_count=0`、`fixed_only_tradeoff_report_count=2`、fixed route 为 `v612-delimiter-span`。

## 结论

v614 证明 contrast-free batch 没有产生 pair-full；下一步需要路线决策，避免再次进入已经失败的 loss-rebalance 循环。

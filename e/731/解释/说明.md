# v731 运行截图和解释：direct-prompt bridge contract patch

## 本版目标

v731 根据 v730 surface mismatch diagnostic，对 v727 objective-structure contract 增加 direct prompt bridge rows。

目标是让训练语料出现 raw `fixed=` 和 `loss=` surface，但仍不加入 heldout pair probe `fixed=|loss=`。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_direct_prompt_bridge_contract_patch.py --diagnostic e\730\解释\model-capability-required-term-pair-readiness-surface-mismatch-diagnostic --base-contract e\727\解释\model-capability-required-term-pair-readiness-objective-structure-contract --out-dir e\731\解释\model-capability-required-term-pair-readiness-direct-prompt-bridge-contract-patch --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_direct_prompt_bridge_contract_patch_ready
patch_ready=True
base_training_row_count=18
patched_training_row_count=26
bridge_row_count=8
fixed_bridge_row_count=4
loss_bridge_row_count=4
model_quality_claim=contract_patch_only
```

## 截图说明

- `e/731/图片/v731-direct-prompt-bridge-contract-patch.png`

截图显示 patch ready、8 条 bridge rows、fixed/loss 各 4 条，并且 heldout pair probe 没有进入训练行。

## 关键观察

- v731 不是重新训练，而是 contract patch。
- bridge rows 包含 raw `fixed=` / `loss=` surface。
- patch 保留 v727 的 objective-structure rows。
- `fixed=|loss=` 仍然只保留在 eval probes 中。

一句话总结：v731 为 v730 发现的 direct surface mismatch 加入受检查的 bridge rows。

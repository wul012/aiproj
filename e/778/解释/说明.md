# v778 release readiness comparison regression split

## 目标

v778 继续维护拆分，不新增功能版本。v777 已经拆出 narrative 文本层，本版进一步把 CI workflow、coverage、benchmark history 的回退判定和 status score 表拆出主 comparison 文件。

## 关键变化

- 新增 `src/minigpt/release_readiness_comparison_regressions.py`。
- `src/minigpt/release_readiness_comparison.py` 从 605 行降到 457 行。
- regression 模块承接 `ci_workflow_regression_reasons`、`is_test_coverage_regression`、`is_benchmark_history_regression`、`benchmark_history_status_score`、`max_abs_delta` 等 helper。
- 主 comparison 文件继续负责 row/delta/summary 的编排，不再内嵌 regression 语义细节。

## 验证

- `python -m py_compile src\minigpt\release_readiness_comparison.py src\minigpt\release_readiness_comparison_regressions.py`
- `python -m pytest tests\test_release_readiness_comparison.py -q -o cache_dir=runs\pytest-cache-v778-focused`
- 结果：`20 passed`

## 截图

- `e/778/图片/v778-release-readiness-comparison-regression-split.png`

## 一句话总结

v778 把 release readiness comparison 的回退判定集中到 regression 模块，让主 comparison 文件从 767 行连续降到 457 行。

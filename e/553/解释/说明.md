# v553 required-term pair coexistence corpus split 运行说明

## 本版目标

v552 后 `model_capability_required_term_pair_coexistence_refresh.py` 已接近 500 行。v553 做一次 contract-preserving refactor：把 corpus mode、corpus builder 和 source prompt 选择从 refresh 执行文件中拆出，避免后续继续把训练样本设计塞进同一个大文件。

本版不训练新 checkpoint，不改变 v552 的负结果，也不新增治理链。它只改善维护边界。

## 关键改动

- 新增 `src/minigpt/model_capability_required_term_pair_coexistence_corpus.py`
  - 承接 `PAIR_COEXISTENCE_CORPUS_MODES`。
  - 承接 `build_pair_coexistence_refresh_corpus()`。
  - 新增 `source_prompts()`，集中管理 `fixed:` / `loss:` 与 `fixed=` / `loss=` 的 prompt 选择。
- 精简 `src/minigpt/model_capability_required_term_pair_coexistence_refresh.py`
  - 从 488 行降到 306 行。
  - 继续 re-export corpus builder 和 corpus modes，旧脚本和测试不需要改调用方式。

## 验证命令

```powershell
python -m py_compile `
  src\minigpt\model_capability_required_term_pair_coexistence_corpus.py `
  src\minigpt\model_capability_required_term_pair_coexistence_refresh.py `
  tests\test_model_capability_required_term_pair_coexistence_refresh.py

python -m pytest `
  tests\test_model_capability_required_term_pair_coexistence_refresh.py `
  tests\test_model_capability_required_term_pair_colon_immediate_stability.py `
  -q `
  -o cache_dir=runs\pytest-cache-v553-target
```

## 核心结果

```text
13 passed
model_capability_required_term_pair_coexistence_refresh.py = 306 lines
model_capability_required_term_pair_coexistence_corpus.py = 219 lines
```

这说明 v553 是行为保持的拆分，不是功能重写。

## 截图说明

- `e/553/图片/01-model-capability-required-term-pair-coexistence-corpus-split.png`
  - 展示本说明和拆分结果。
  - 证明本版目标是维护性收束，不是模型能力结论。

## 后续影响

后续如果继续尝试 balanced equals repair、loss-preserving repair 或 prompt-surface sweep，可以只修改 corpus 模块，不再让训练执行、replay、summary 和样本设计挤在一个文件里。

一句话总结：v553 把 v552 后接近阈值的训练刷新文件拆开，让后续模型能力实验有更稳的维护边界。

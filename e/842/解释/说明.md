# v842 运行截图和解释

## 本版目标

v842 承接 v841 的 decoder anchor probe，把真实 anchor completion signal 收敛成一个受控 policy。

这版不做 policy replay，不宣称模型能力提升，只生成下一步可以执行和审计的 policy 产物。

## 输入

```text
e/841/解释/model-capability-route-promotion-bounded-objective-decoder-anchor-probe/
```

v841 已经证明：

- `completion_pass_count=9`
- `new_text_pass_count=0`
- `anchor_completion_success=True`
- `promotion_ready=False`

v842 只消费这些 probe rows。

## 输出

产物目录：

```text
e/842/解释/model-capability-route-promotion-bounded-objective-decoder-anchor-policy/
```

关键文件：

```text
model_capability_route_promotion_bounded_objective_decoder_anchor_policy.json
model_capability_route_promotion_bounded_objective_decoder_anchor_policy.csv
model_capability_route_promotion_bounded_objective_decoder_anchor_policy.txt
model_capability_route_promotion_bounded_objective_decoder_anchor_policy.md
model_capability_route_promotion_bounded_objective_decoder_anchor_policy.html
```

截图：

```text
e/842/图片/v842-bounded-objective-decoder-anchor-policy-html.png
```

## 真实结果

```text
status=pass
decision=model_capability_route_promotion_bounded_objective_decoder_anchor_policy_ready
bounded_objective_decoder_anchor_policy_ready=True
policy_case_count=3
uncovered_case_count=0
coverage_is_partial=False
promotion_ready=False
model_quality_claim=decoder_anchor_policy_only
next_action=run_bounded_objective_decoder_anchor_policy_replay
```

结果说明：

- 三个 objective cases 都有可用 policy anchor。
- 当前只是 replay-only policy，不是普通 replay 成功。
- guardrails 明确禁止把 injected anchors 计为 unassisted capability。

## 截图说明

HTML 截图展示了：

- policy summary cards。
- 三条 policy rows。
- guardrails 表。

关键可见字段包括：

- `Policy cases=3`
- `Uncovered=0`
- `Promotion=False`
- `Claim=decoder_anchor_policy_only`

## 一句话总结

v842 把 v841 的锚点补齐信号变成了可复核的 replay-only policy，为下一版 policy replay 做准备，同时继续阻断模型能力晋级结论。

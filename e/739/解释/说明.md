# v739 运行截图和解释：direct-completion route comparison

## 本版目标

v739 将 v729 objective-structure、v733 direct-prompt bridge、v738 direct-completion surface 三条训练路线放入同一比较报告。

目标是判断 v738 的 pair-full 是否真的优于前两条失败路线，而不是单独看一个正向训练报告。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_direct_completion_route_comparison.py --objective e\729\解释\model-capability-required-term-pair-readiness-objective-structure-training-run --bridge e\733\解释\model-capability-required-term-pair-readiness-direct-prompt-bridge-training-run --direct-completion e\738\解释\model-capability-required-term-pair-readiness-direct-completion-surface-training-run --out-dir e\739\解释\model-capability-required-term-pair-readiness-direct-completion-route-comparison --require-pass --force
```

输出摘要：

```text
status=pass
decision=pair_readiness_direct_completion_route_candidate_found
previous_best_hit_count=0
direct_completion_default_hit_count=2
direct_completion_hit_delta_from_previous_best=2
direct_completion_pair_full_observed=True
direct_completion_pollution_free=True
selected_route=direct-completion-surface
model_quality_claim=comparison_candidate
```

## 截图说明

- `e/739/图片/v739-direct-completion-route-comparison.png`

截图显示 direct-completion route 相对 previous best 有 `hit_delta=2`，pair-full 为 True，并且污染检查为 True。

## 关键观察

- v729 objective-structure：default direct hit count 为 0。
- v733 direct-prompt bridge：default direct hit count 为 0，且出现过 fixed pollution。
- v738 direct-completion surface：default direct hit count 为 2，pair-full observed 为 True。
- v739 因此选中 `direct-completion-surface`，但下一步仍要求 pair-probe replay 和更严格 promotion checks。

一句话总结：v739 将 v738 从孤立正向训练结果提升为跨路线 candidate 证据，但仍未进入最终 promotion。

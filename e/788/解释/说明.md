# v788 seed handoff review summary split

## 目标

v788 是维护拆分版本，不新增 seed handoff review 功能。目标是把 `promoted_training_scale_seed_handoff_review.py` 中的 batch review summary 和 clean batch review summary 归一化逻辑拆到独立模块。

## 关键变化

- 新增 `src/minigpt/promoted_training_scale_seed_handoff_review_summary.py`。
- `src/minigpt/promoted_training_scale_seed_handoff_review.py` 从 613 行降到 410 行。
- 原模块继续保留 TypedDict、requirement、automation gate、automation summary、clean evidence readiness 和 suite alignment。
- summary 模块承接两个长字段归一化函数，并由原模块 re-export，兼容旧导入路径。

## 验证

- `python -m py_compile src\minigpt\promoted_training_scale_seed_handoff_review.py src\minigpt\promoted_training_scale_seed_handoff_review_summary.py`
- `python -m pytest tests\test_promoted_training_scale_seed_handoff_review.py tests\test_promoted_training_scale_seed_review.py tests\test_promoted_training_scale_decision_review.py -q -o cache_dir=runs\pytest-cache-v788-focused`
- `python -B scripts\check_source_encoding.py --out-dir runs\source-encoding-hygiene-v788`
- `git diff --check`

结果：focused 测试 `11 passed`，source encoding `status=pass`，diff check 无错误。

## 截图

- `e/788/图片/v788-seed-handoff-review-summary-split.png`

## 一句话总结

v788 把 seed handoff review 的长 summary 归一化层拆出，让 review 主模块更集中在 requirement 和 automation gate。

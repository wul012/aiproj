# v694 运行截图和解释：minimal prompt objective readiness

## 本版目标

v694 是 v692/v693 之后的新路线入口。它不训练 checkpoint，不新增 corpus mode，也不声称模型能力提升；它只验证上一条 contextual surface-policy 分支是否已经满足开启 `minimal_prompt_surface_objective` 的前置条件。

## 验证命令和结果

生成 readiness 证据：

```powershell
python -B scripts\run_model_capability_required_term_pair_minimal_prompt_objective_readiness.py e\692\解释\model-capability-required-term-pair-surface-branch-closeout --out-dir e\694\解释\model-capability-required-term-pair-minimal-prompt-objective-readiness --require-pass --force
```

结果：

```text
status=pass
decision=minimal_prompt_surface_objective_ready_for_corpus_contract
failed_count=0
objective_ready=True
recommended_corpus_mode=minimal_prompt_equals_surface_objective
model_quality_claim=objective_readiness_only
```

focused tests：

```powershell
python -m pytest tests\test_model_capability_required_term_pair_minimal_prompt_objective_readiness.py -q -o cache_dir=runs\pytest-cache-v694-focused
```

结果：

```text
5 passed
```

## 截图说明

- `e/694/图片/v694-minimal-prompt-objective-readiness.png`

截图展示 readiness HTML 页面，能看到 `Objective Boundary`、`Readiness Checks` 和 `Next Action`。其中边界明确写着 minimal prompt only，不能使用包含另一个答案的 contextual anchor。

## 产物说明

正式产物目录：

```text
e/694/解释/model-capability-required-term-pair-minimal-prompt-objective-readiness/
```

主要文件：

- `model_capability_required_term_pair_minimal_prompt_objective_readiness.json`
- `model_capability_required_term_pair_minimal_prompt_objective_readiness.csv`
- `model_capability_required_term_pair_minimal_prompt_objective_readiness.txt`
- `model_capability_required_term_pair_minimal_prompt_objective_readiness.md`
- `model_capability_required_term_pair_minimal_prompt_objective_readiness.html`

这些产物是 v695 corpus contract 的输入依据，不是模型训练结果。

## 链路结论

v694 确认 v692 closeout 具备三个关键条件：

- contextual decode aid 已关闭。
- promotion 仍然禁止。
- 下一路线明确是 `minimal_prompt_surface_objective`。

一句话总结：v694 把“下一步做 minimal prompt”从 README 里的建议变成了可复核的 objective readiness contract。

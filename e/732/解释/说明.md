# v732 运行截图和解释：direct-prompt bridge corpus materialization

## 本版目标

v732 使用现有 materializer 消费 v731 direct-prompt bridge contract patch。

目标是生成下一轮训练所需 corpus，并确认 heldout pair probe 仍未泄漏。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_corpus_materialization.py e\731\解释\model-capability-required-term-pair-readiness-direct-prompt-bridge-contract-patch --out-dir e\732\解释\model-capability-required-term-pair-readiness-direct-prompt-bridge-corpus-materialization --repeat 320 --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_corpus_materialized
materialization_ready=True
training_line_count=8320
evaluation_probe_count=3
model_quality_claim=data_artifact_only
```

## 截图说明

- `e/732/图片/v732-direct-prompt-bridge-corpus-materialization.png`

截图显示 8320 条训练行、3 个 eval probes，heldout pair probe 未进入 training rows 或 corpus lines。

## 关键观察

- v731 的 26 条 patched training rows 按 repeat=320 物化为 8320 行。
- bridge rows 已进入 corpus，但 `fixed=|loss=` 仍未进入 corpus。
- 本版是数据产物，不声明模型能力。

一句话总结：v732 将 direct-prompt bridge patch 落成可训练语料，为下一版真实训练做准备。

# v613 required-term pair context-switch seed 3535 运行说明

## 本版目标

v613 用 v610 的 `equals_surface_no_pair_id_fixed_retention_context_switch_repair` 跑真实 seed `3535`。目标是验证 `[fixed-context]` / `[loss-context]` 的局部上下文隔离是否能让 `fixed=` 与 `loss=` 稳定选中各自 term。

本版只记录真实 tiny 训练结果，不宣称模型能力提升。

## 训练配置

```text
seed=3535
corpus_mode=equals_surface_no_pair_id_fixed_retention_context_switch_repair
repeat=260
bridge_repeat=20
max_iters=1800
batch_size=16
block_size=16
n_layer=1
n_head=1
n_embd=64
learning_rate=0.02
temperature=0.2
top_k=1
device=cpu
```

## 结果

```text
status=pass
decision=required_term_pair_coexistence_refresh_no_pair_full
training_status=pass
checkpoint_exists=True
pair_full_observed=False
```

Replay 结果：

```text
fixed= -> fixed=fixed= hit fixed
loss=  -> fixeded=fixe miss loss
```

context-switch route 恢复了 fixed branch，但 loss branch 仍然被 fixed 起始覆盖。

## 截图说明

- `图片/v613-context-switch-seed-3535.png`
  - Playwright MCP 截取的 v613 HTML 报告。
  - 证明训练流程通过、checkpoint 存在、pair-full 仍为 `False`。

## 结论

v613 是 fixed-only 负结果：上下文隔离没有让 `loss=` 稳定恢复 loss。

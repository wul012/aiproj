# v733 运行截图和解释：direct-prompt bridge training run

## 本版目标

v733 使用 v732 direct-prompt bridge corpus 做真实 tiny GPT 训练。

目标是验证 raw `fixed=` / `loss=` bridge rows 是否能恢复 heldout direct hits。结果是：训练成功，但 default hit count 仍为 0。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_training_run.py e\732\解释\model-capability-required-term-pair-readiness-direct-prompt-bridge-corpus-materialization --out-dir e\733\解释\model-capability-required-term-pair-readiness-direct-prompt-bridge-training-run --seed 3535 --max-iters 1800 --eval-iters 2 --batch-size 16 --block-size 16 --n-layer 2 --n-head 2 --n-embd 96 --learning-rate 0.01 --max-new-tokens 12 --temperature 0.2 --top-k 1 --device cpu --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_training_no_pair_full
training_status=pass
checkpoint_exists=True
pair_full_observed=False
default_continuation_hit_count=0
model_quality_claim=not_claimed
```

## 截图说明

- `e/733/图片/v733-direct-prompt-bridge-training-run.png`

截图显示 default 和 suppress-newline profiles 仍然都 miss `fixed` 和 `loss`。

## 关键观察

- v733 没有 pair-full。
- v733 也没有 direct hit。
- `loss=` continuation 出现 `fixed`，说明 bridge patch 引入 raw surface 后又出现 loss prompt fixed pollution。
- 下一步应将 v729 和 v733 放进同一比较诊断，而不是继续直接加 rows。

一句话总结：v733 证明 direct-prompt bridge patch 没有恢复 direct hits，且引入了新的 fixed pollution 风险。

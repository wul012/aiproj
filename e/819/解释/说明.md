# v819 运行证据说明

## 本版目标

v819 把 v818 训练出的 prompt-aligned checkpoint 放回 v803 bounded benchmark suite 做真实 replay，并和 v806 baseline replay 做对比。目标不是再证明训练成功，而是回答一个更直接的问题：v818 checkpoint 是否真的恢复了 bounded replay 能力。

## 关键产物

- `model-capability-route-promotion-bounded-real-replay-prompt-aligned-checkpoint-replay/prompt-aligned-replay/`：v818 checkpoint 的真实 replay 输出。
- `model-capability-route-promotion-bounded-real-replay-prompt-aligned-checkpoint-replay/comparison/`：v806 baseline replay 与 v819 prompt-aligned replay 的对比报告。
- `../图片/v819-bounded-real-replay-prompt-aligned-checkpoint-comparison-html.png`：Playwright MCP 截取的 comparison HTML。

## 运行结果

replay 结果：

- `status=pass`
- `decision=model_capability_route_promotion_bounded_real_replay_completed_with_model_gaps`
- `passed_case_count=0`
- `failed_case_count=5`
- `pass_rate=0.0`
- `model_route_quality_ready=False`

comparison 结果：

- `status=pass`
- `decision=model_capability_route_promotion_bounded_real_replay_prompt_aligned_checkpoint_regressed`
- `baseline_passed_case_count=2`
- `prompt_aligned_passed_case_count=0`
- `passed_case_delta=-2`
- `pass_rate_delta=-0.4`
- `promotion_ready=False`

## 判断

v819 证明 prompt-aligned corpus 虽然覆盖了 exact benchmark prompts，但当前 tiny checkpoint 仍不能稳定生成 `fixed` 与 `loss`。下一步不适合继续盲目增加训练轮次，应先诊断生成输出：它是在首 token、字符级 tokenizer、采样策略、训练提示格式还是模型容量上卡住。

# v610 required-term pair contrast-free objective corpus 运行说明

## 本版目标

v610 根据 v609 诊断新增三条 contrast-free objective 语料路线。目标不是继续给 fixed 或 loss 单边加权，而是把 first-token choice 和 repeated term loop 分离。

本版只新增语料模式与测试，不宣称模型能力提升；真实效果由 v611-v613 训练验证。

## 新增模式

```text
equals_surface_no_pair_id_fixed_retention_contrast_free_repair
equals_surface_no_pair_id_fixed_retention_delimiter_span_repair
equals_surface_no_pair_id_fixed_retention_context_switch_repair
```

## 验证

```text
python -m pytest tests\test_model_capability_required_term_pair_contrast_free_objective_corpus.py tests\test_model_capability_required_term_pair_coexistence_refresh.py -q -o cache_dir=runs\pytest-cache-v610-targeted
27 passed
```

## 截图说明

- `图片/v610-contrast-free-objective-corpus-contract.png`
  - Playwright MCP 截取的 v610 corpus contract HTML。
  - 证明三条新模式已进入归档证据，且仍使用 `fixed=` / `loss=` prompt surface。

## 结论

v610 把 v609 的 first-token 诊断转成三条可训练 objective route，下一步跑真实 seed `3535`。

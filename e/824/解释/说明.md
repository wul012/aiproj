# v824 运行截图和解释

## 本版目标

v824 把 v823 已复现的 decoder-anchor 局部信号转成下一轮可训练语料。它不声称模型能力已经提升，只负责把“forced prefix 能补出 fixed/loss”的观察落回 seed revision。

## 输入证据

- v817 prompt-aligned seed revision：提供 28 条已对齐 benchmark prompt 的训练样本。
- v819 prompt-aligned bounded replay：提供 5 个真实 replay case 的 prompt 和 expected terms。
- v823 decoder anchor policy replay：证明 `objective-answer-check` 在受控 anchor 下存在可复现局部信号。

## 输出结果

- 状态：`pass`
- 决策：`model_capability_route_promotion_bounded_real_replay_decoder_anchor_seed_revision_ready`
- 总样本数：`48`
- 新增样本数：`20`
- bridge 样本数：`15`
- unanchored direct 样本数：`5`
- 下一步：`train_decoder_anchor_seed_revision`

## 关键产物

- `e/824/解释/model-capability-route-promotion-bounded-real-replay-decoder-anchor-seed-revision/model_capability_route_promotion_bounded_real_replay_decoder_anchor_seed_revision.json`
- `e/824/解释/model-capability-route-promotion-bounded-real-replay-decoder-anchor-seed-revision/model_capability_route_promotion_bounded_real_replay_decoder_anchor_seed_revision.jsonl`
- `e/824/解释/model-capability-route-promotion-bounded-real-replay-decoder-anchor-seed-revision/model_capability_route_promotion_bounded_real_replay_decoder_anchor_seed_revision_corpus.txt`
- `e/824/图片/v824-bounded-real-replay-decoder-anchor-seed-revision-html.png`

## 截图说明

截图展示 HTML 报告首页，能看到 `Status=pass`、`Examples=48`、`Added=20`、`Bridge=15`、`Direct=5` 和下一步训练动作。它证明本版报告可浏览、核心计数可见，后续 v825 可以直接消费 corpus。

## 边界

这版只是训练数据修订，不做真实训练，也不做 replay 质量比较。模型是否提升，需要 v825 训练 checkpoint 并用同一套 bounded replay 复核。

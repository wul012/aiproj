# v859 运行截图和解释

## 本版目标

v859 根据 v858 的 next action，对 v856 curriculum patch checkpoint 运行 5 组解码 profile sweep，确认 `loss` 是否只是被固定的 v857 解码参数压住。

本版不训练，不改 corpus，不改 contract，也不把某个 profile 的单词命中当作 promotion。

## 输入

- `e/836/解释/model-capability-route-promotion-bounded-objective-contract/`
- `e/856/解释/model-capability-route-promotion-bounded-objective-unassisted-repair-seed-revision-curriculum-patch-training-run/`
- `e/858/解释/bounded-objective-curriculum-patch-shape-migration-diagnostic/`

## 输出

产物目录：

```text
e/859/解释/bounded-objective-curriculum-patch-profile-sweep/
```

截图：

```text
e/859/图片/v859-bounded-objective-curriculum-patch-profile-sweep-html.png
```

## 真实结果

```text
status=pass
decision=bounded_objective_curriculum_patch_profile_sweep_found_loss_signal_bridge_required
bounded_objective_curriculum_patch_profile_sweep_ready=True
profile_count=5
sweep_row_count=15
any_profile_recovered=False
profile_with_loss_hit_count=2
max_loss_hit_case_count=2
best_profile_id=longer-open
next_action=build_loss_signal_bridge_without_promotion
```

## 关键观察

- `v857-baseline` 复现 v857 边界：两个 `fixed` partial，一个 zero-hit。
- `longer-top20` 在 `minimal_direct_completion` 打到 `loss`，但 missed `fixed`。
- `longer-open` 在 `canonical_direct_completion` 和 `completion_label_surface` 打到 `loss`，但 missed `fixed`。
- 没有任何 profile 达到 `any_profile_recovered=True`。

所以本版结论是：`loss` 并非完全无法生成，但它和 `fixed` 没有在同一 contract surface 上稳定共现。

## 验证

```text
py_compile: pass
focused pytest: 3 passed
Playwright MCP: snapshot and full-page screenshot saved
```

后续还会跑全量 pytest、source encoding 和 `git diff --check` 后再提交。

## 一句话总结

v859 把问题从“模型完全不会 loss”推进到“有 loss signal，但需要桥接 fixed/loss 共现”。

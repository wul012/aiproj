# v773 运行截图和解释：receipt validation split

## 本版目标

v773 开始执行维护性拆分路线，不新增模型能力功能。第一刀选择当前最大文件 `promoted_training_scale_seed_handoff_receipt.py`，把 receipt schema 常量、schema v2-v5 字段校验、compare-key normalization 和 `_int` 归一化抽到独立 validation 模块。

## 改动结果

```text
src/minigpt/promoted_training_scale_seed_handoff_receipt.py: 941 -> 774 行
src/minigpt/promoted_training_scale_seed_handoff_receipt_validation.py: 新增 208 行
```

## 边界说明

- 主文件继续保留公共 API、loader、checker、renderer、sidecar 检查和 `__all__`。
- 新 validation 模块只负责 schema/normalization/版本字段校验。
- `RECEIPT_SCHEMA_V5_REQUIRED_FIELDS` 等常量仍可从原模块导入，保证现有 assurance 模块契约不变。

## 验证命令

```powershell
python -m py_compile src\minigpt\promoted_training_scale_seed_handoff_receipt.py src\minigpt\promoted_training_scale_seed_handoff_receipt_validation.py
python -m pytest tests\test_promoted_training_scale_seed_handoff_receipt.py tests\test_promoted_training_scale_seed_handoff_receipt_contract.py tests\test_promoted_training_scale_seed_handoff_receipt_contract_check.py tests\test_promoted_training_scale_seed_handoff_receipt_contract_check_failure_smoke.py tests\test_promoted_training_scale_seed_handoff_receipt_suite_design.py -q -o cache_dir=runs\pytest-cache-v773-focused
```

结果：

```text
50 passed
```

## 截图说明

- `e/773/图片/v773-receipt-validation-split.png`

截图展示本版拆分边界、行数变化和 focused tests 结果。

一句话总结：v773 把最大 receipt 文件的 schema validation 责任切出去，为后续继续拆 render/sidecar 留出清晰边界。

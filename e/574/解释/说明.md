# v574 required-term pair route fresh seed 3535 repair comparison 运行说明

## 本版目标

v571 与 v573 都是 seed `3535` 的 fresh-seed 训练：一个用 loss-balanced route，一个用已有 full first-token repair。v574 把两份报告放到同一张 comparison 里，避免只靠版本记忆判断哪条路线更好。

本版不重新训练、不新增代码路径。它是 v571/v573 的只读对比证据。

## 运行命令

```powershell
python scripts\run_model_capability_required_term_pair_equals_surface_repair_comparison.py `
  e\571\解释\model-capability-required-term-pair-route-fresh-seed-3535 `
  e\573\解释\model-capability-required-term-pair-route-fresh-seed-3535-first-token-repair `
  --labels v571-loss-balanced-fresh3535 v573-first-token-fresh3535 `
  --out-dir e\574\解释\model-capability-required-term-pair-route-fresh-seed-3535-repair-comparison `
  --force `
  --require-pass
```

## 关键结果

```text
status=pass
decision=required_term_pair_equals_surface_repair_comparison_recorded
compared_report_count=2
term_case_row_count=8
branch_competition_seed_count=0
pair_full_profile_seed_count=0
union_hit_terms=fixed
model_quality_claim=comparison_only
```

对比结论：

```text
v571-loss-balanced-fresh3535 -> fixed-only partial signal
v573-first-token-fresh3535  -> no visible fixed/loss hit
```

这说明 first-token repair mode 没有把 `loss` 拉回来，反而丢掉了 v571 里仅存的 fixed partial signal。

## 截图说明

- `图片/v574-route-fresh-seed-3535-repair-comparison.png`
  - Playwright MCP 截取的 HTML 报告。
  - 证明 source reports、branch rows、term evidence 可见。

## 归档文件

- `解释/model-capability-required-term-pair-route-fresh-seed-3535-repair-comparison/model_capability_required_term_pair_equals_surface_repair_comparison.json`
  - 主 comparison 报告。
- `解释/model-capability-required-term-pair-route-fresh-seed-3535-repair-comparison/model_capability_required_term_pair_equals_surface_repair_comparison.csv`
  - seed-level branch rows。
- `解释/model-capability-required-term-pair-route-fresh-seed-3535-repair-comparison/playwright-snapshot.md`
  - 页面快照旁证。

## 结论

v574 收口了 v571-v573：对 seed `3535` 来说，已有 first-token repair 不是修复，而是退化。下一步需要换 repair 方向，或做更窄的 loss-branch forced-choice/diagnostic，再决定是否值得新增 corpus mode。

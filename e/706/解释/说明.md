# v706 运行截图和解释：pair-readiness corpus materialization

## 本版目标

v706 把 v705 的 split contract 真正落成两个可运行数据文件：

- `pair_readiness_training_corpus.txt`
- `pair_readiness_heldout_eval_fixture.json`

本版仍不训练模型，它只生成训练输入和 heldout 评估夹具。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_corpus_materialization.py e\705\解释\model-capability-required-term-pair-readiness-split-contract --out-dir e\706\解释\model-capability-required-term-pair-readiness-corpus-materialization --repeat 320 --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_corpus_materialized
materialization_ready=True
training_line_count=3840
evaluation_probe_count=3
model_quality_claim=data_artifact_only
```

## 生成文件

训练语料：

```text
e/706/解释/model-capability-required-term-pair-readiness-corpus-materialization/pair_readiness_training_corpus.txt
```

heldout eval fixture：

```text
e/706/解释/model-capability-required-term-pair-readiness-corpus-materialization/pair_readiness_heldout_eval_fixture.json
```

## 截图说明

- `e/706/图片/v706-pair-readiness-corpus-materialization.png`

截图展示 materialized paths、checks、corpus preview 和 next action。

## 结论

v706 证明训练前输入已经可落地：

- corpus 是从 v705 contract 生成。
- heldout pair probe 保持在 eval fixture 中。
- materialization 检查确认 heldout pair probe 没有作为训练行泄漏。

一句话总结：v706 把 pair-readiness contract 落成可训练 corpus 和 heldout eval fixture，准备进入真实训练。

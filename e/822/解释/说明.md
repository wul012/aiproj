# v822 运行证据说明

## 本版目标

v822 将 v821 decoder anchor probe 的成功行收束为一个受控 decoder anchor policy。它的目标是定义下一步可 replay 的策略，同时明确该策略只属于 anchor-assisted 诊断，不是模型无锚能力证明。

## 关键产物

- `model-capability-route-promotion-bounded-real-replay-decoder-anchor-policy/model_capability_route_promotion_bounded_real_replay_decoder_anchor_policy.json`：policy 主报告。
- `model_capability_route_promotion_bounded_real_replay_decoder_anchor_policy.csv`：policy rows。
- `model_capability_route_promotion_bounded_real_replay_decoder_anchor_policy.md` / `.html` / `.txt`：人工阅读报告。
- `../图片/v822-bounded-real-replay-decoder-anchor-policy-html.png`：Playwright MCP 截图。

## 运行结果

- `status=pass`
- `decision=model_capability_route_promotion_bounded_real_replay_decoder_anchor_policy_ready_with_partial_coverage`
- `policy_case_count=1`
- `uncovered_case_count=4`
- `coverage_is_partial=True`
- `promotion_ready=False`
- `next_step=run_decoder_anchor_policy_replay`

## 判断

v822 只为 `objective-answer-check` 选出了 `prefix_fixed_space` 策略，其他 4 个 case 仍没有可用 completion-pass anchor。因此下一步可以做 policy replay，但不能把 policy 当作能力提升证据。它更像一个受控诊断工具，用于确认 anchor 规则是否能稳定复现 v821 的局部信号。

# v692 运行截图和解释：required-term pair surface branch closeout

## 本版目标

v692 对 v679-v691 的 required-term pair surface policy 分支做批次收口。它把七个关键里程碑串成一份 closeout 报告，说明当前分支已经形成可用 contextual decode aid，但没有形成可 promotion 的 minimal-prompt baseline。

本版不训练、不生成、不再新增 surface 变体。它负责总结和收口。

## 输入证据

本版显式读取七个报告：

- v679 `surface_failure`：定位 seed 2535 的 `loss` free-generation surface failure。
- v681 `policy_replay`：证明 contextual policies 可以恢复 pair-full。
- v684 `leakage_risk`：记录 contextual-anchor 风险和 no-promotion 边界。
- v685 `budget_sweep`：找到最小稳定 budget 8。
- v688 `variant_replay`：证明五个 contextual surface 变体全部稳定。
- v690 `baseline_contrast`：证明 non-leaking baselines 仍不稳定。
- v691 `route_decision`：决定关闭 contextual decode aid 分支，下一条能力路线是 minimal prompt objective。

## 实际运行

执行命令：

```powershell
python -B scripts\run_model_capability_required_term_pair_surface_branch_closeout.py --surface-failure e\679\解释\model-capability-required-term-pair-surface-failure-diagnostic --policy-replay e\681\解释\model-capability-required-term-pair-surface-policy-replay --leakage-risk e\684\解释\model-capability-required-term-pair-surface-policy-leakage-risk --budget-sweep e\685\解释\model-capability-required-term-pair-surface-policy-budget-sweep --variant-replay e\688\解释\model-capability-required-term-pair-surface-variant-replay --baseline-contrast e\690\解释\model-capability-required-term-pair-surface-baseline-contrast --route-decision e\691\解释\model-capability-required-term-pair-surface-route-decision --out-dir e\692\解释\model-capability-required-term-pair-surface-branch-closeout --require-pass --force
```

关键输出：

- `status=pass`
- `decision=required_term_pair_surface_branch_closed_as_contextual_decode_aid`
- `milestone_count=7`
- `contextual_decode_aid_ready=True`
- `promotion_allowed=False`
- `recommended_next_route=minimal_prompt_surface_objective`
- `model_quality_claim=contextual_decode_aid_closed_branch`

## 截图说明

- `e/692/图片/v692-surface-branch-closeout.png`

截图展示了 milestone 数量、contextual aid 状态、next route，以及七个里程碑表格。

## 产物说明

正式产物目录：

```text
e/692/解释/model-capability-required-term-pair-surface-branch-closeout/
```

主要文件：

- `model_capability_required_term_pair_surface_branch_closeout.json`
- `model_capability_required_term_pair_surface_branch_closeout.csv`
- `model_capability_required_term_pair_surface_branch_closeout.txt`
- `model_capability_required_term_pair_surface_branch_closeout.md`
- `model_capability_required_term_pair_surface_branch_closeout.html`

## 结论

v692 收口后的结论是：

- 本分支成功形成 contextual decode aid。
- `space_context_control` 可以作为稳定、可读的默认 contextual variant。
- 最小稳定 budget 是 8。
- 五个 contextual surface 变体全部稳定。
- non-leaking baseline 仍不稳定。
- 不允许 promotion。
- 如果继续追模型能力，应另起 minimal-prompt objective。

一句话总结：v692 把 v679-v691 收口为“contextual decode aid 已闭环、minimal prompt 能力未解决”的批次结论。

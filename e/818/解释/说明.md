# v818 运行证据说明

## 本版目标

v818 承接 v817 的 prompt-aligned seed revision，把 28 条提示对齐训练样本真正送进 MiniGPT 训练脚本，产出新的本地 checkpoint，并用独立检查器确认训练目录里的关键产物齐全。

本版不声明模型能力已经提升。能力是否恢复，必须在下一版把该 checkpoint 放回 v803 bounded benchmark suite 做真实 replay 后再判断。

## 关键产物

- `model-capability-route-promotion-bounded-real-replay-prompt-aligned-training-run/run/`：真实训练目录，包含 `checkpoint.pt`、`tokenizer.json`、`metrics.jsonl`、`train_config.json`、`run_manifest.json`、`sample.txt`、`prepared_corpus.txt`。
- `model_capability_route_promotion_bounded_real_replay_prompt_aligned_training_run.json`：训练证据主报告。
- `model_capability_route_promotion_bounded_real_replay_prompt_aligned_training_run.csv`：训练产物清单。
- `model_capability_route_promotion_bounded_real_replay_prompt_aligned_training_run.md` / `.html` / `.txt`：人工阅读版本。
- `../图片/v818-bounded-real-replay-prompt-aligned-training-run-html.png`：Playwright MCP 截取的 HTML 证据页面。

## 运行结果

- `status=pass`
- `decision=model_capability_route_promotion_bounded_real_replay_prompt_aligned_training_run_ready`
- `failed_count=0`
- `prompt_aligned_training_ready=True`
- `final_step=30`
- `final_train_loss=4.1787`
- `final_val_loss=4.2154`
- `prompt_aligned_example_count=28`
- `exact_prompt_answer_count=5`

## 边界说明

v818 证明的是“prompt-aligned corpus 可以完成一次真实训练，并且训练产物可复核”。它不证明该 checkpoint 比 v806 baseline 更好，也不证明它能恢复 fixed/loss 输出。下一步应运行 prompt-aligned checkpoint bounded replay，并和 v806 baseline/v815 repair replay 做对比。

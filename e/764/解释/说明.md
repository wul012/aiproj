# v764 运行截图和解释：objective-level contrast pair-probe replay

## 本版目标

v764 对 v763 objective-level contrast checkpoint 做独立 pair-probe replay，确认 direct pair hit 是否能迁移到 heldout pair prompt surfaces。

本版不重新训练。它是 v763 之后必须补的独立 replay 层，用来避免把训练脚本内 direct hit 误当成可推广结论。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_fixed_preserving_transfer_pair_probe_replay.py e\763\解释\model-capability-required-term-pair-readiness-objective-level-contrast-training-run --out-dir e\764\解释\model-capability-required-term-pair-readiness-objective-level-contrast-pair-probe-replay --require-pass --force
```

输出摘要：

```text
status=pass
decision=pair_readiness_fixed_preserving_transfer_pair_probe_replay_ready
exact_heldout_pair_full=True
required_all_pair_full=True
pair_full_count=3
model_quality_claim=pair_probe_replay_ready
```

## 截图说明

- `e/764/图片/v764-objective-level-contrast-pair-probe-replay.png`

截图确认 HTML 页面可见：

- `Decision pair_readiness_fixed_preserving_transfer_pair_probe_replay_ready`
- `Exact pair True`
- `Required all True`
- `Pair-full count 3`
- `Claim pair_probe_replay_ready`

## 关键观察

- exact-heldout-pair `fixed=|loss=` replay full。
- spaced-heldout-pair `fixed= | loss=` replay full。
- arrow-heldout-pair `fixed -> | loss ->` replay full。
- v764 是目前 objective-level contrast 路线最强的独立 replay 信号。
- 下一步仍要和 prior routes 做比较，并跑更严格 promotion guard，不能直接跳到最终接受。

一句话总结：v764 证明 objective-level contrast checkpoint 在三种 pair prompt surface 上都能独立 replay pair-full。

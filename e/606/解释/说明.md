# v606 required-term pair fixed-retention loss-rebalance seed 3535 运行说明

## 本版目标

v606 用 v605 新增的 `equals_surface_no_pair_id_fixed_retention_loss_rebalance_repair` 语料模式跑真实 seed `3535`。目标是验证 loss-rebalance rows 能否在保留 v601 fixed first-token 恢复信号的同时，把 `loss=` 分支也拉回来。

本版不新增治理链，也不宣称模型能力提升；它只记录一次真实 tiny 训练结果。

## 训练配置

```text
seed=3535
corpus_mode=equals_surface_no_pair_id_fixed_retention_loss_rebalance_repair
repeat=260
bridge_repeat=20
max_iters=1800
batch_size=16
block_size=16
n_layer=1
n_head=1
n_embd=64
learning_rate=0.02
temperature=0.2
top_k=1
device=cpu
```

## 结果

```text
status=pass
decision=required_term_pair_coexistence_refresh_no_pair_full
training_status=pass
checkpoint_exists=True
pair_full_observed=False
```

Replay 结果：

```text
fixed= -> lossssssssss  miss fixed
loss=  -> lossssssssss  hit loss
```

这说明 v606 的 loss-rebalance 语料恢复了 loss branch，但没有保住 v601 的 fixed-first-token 信号。

## 截图说明

- `图片/v606-fixed-retention-loss-rebalance-seed-3535.png`
  - Playwright MCP 截取的 v606 HTML 报告。
  - 证明 `status=pass`、`pair_full_observed=False`、default/suppression profile 都没有 pair-full。

## 结论

v606 是一次有效的负结果：loss-rebalance 没有把 fixed-only 与 loss-only 合并成 pair-full，下一步需要测试 v605 的 dual-cycle route。

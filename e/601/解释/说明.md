# v601 required-term pair fixed-retention first-token seed 3535 运行说明

## 本版目标

v601 使用 `equals_surface_no_pair_id_fixed_retention_first_token_repair` 做真实 tiny training。目标是直接验证 v596/v600 暴露的 `fixed=` first-token gap 是否能被修复。

## 关键结果

```text
status=pass
decision=required_term_pair_coexistence_refresh_no_pair_full
training_status=pass
checkpoint_exists=True
pair_full_observed=False
```

## 观察

first-token mode 明确把 `fixed=` 拉回来了：

```text
fixed= -> fixed=fixed=
```

但它同时覆盖了 `loss=`：

```text
loss= -> fixed=fixed=
```

因此 v601 是 fixed-only tradeoff，不是 pair-full。

## 截图说明

- `图片/v601-fixed-retention-first-token-seed-3535.png`
  - Playwright MCP 截取的 v601 coexistence refresh HTML。
  - 证明 first-token route 训练完成，但仍未达到 pair-full。

## 结论

v601 证明 fixed first-token repair 有效，但会牺牲 loss branch。下一步需要测试 prompt-guard 或做 route comparison。

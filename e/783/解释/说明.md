# v783 seed handoff markdown split

## 目标

v783 是维护拆分版本，不新增 seed handoff 功能。目标是把 `promoted_training_scale_seed_handoff_sections.py` 中超长 Markdown renderer 拆到独立模块，同时保持原导入路径兼容。

## 关键变化

- 新增 `src/minigpt/promoted_training_scale_seed_handoff_markdown.py`。
- `src/minigpt/promoted_training_scale_seed_handoff_sections.py` 从 653 行降到 408 行。
- 新模块承接 `render_promoted_training_scale_seed_handoff_markdown` 和 `write_promoted_training_scale_seed_handoff_markdown`。
- sections 模块继续 re-export 同名函数，现有调用方无需改 import。

## 验证

- `python -m py_compile src\minigpt\promoted_training_scale_seed_handoff_sections.py src\minigpt\promoted_training_scale_seed_handoff_markdown.py`
- `python -m pytest tests\test_promoted_training_scale_seed_handoff.py tests\test_promoted_training_scale_seed_handoff_suite_design.py -q -o cache_dir=runs\pytest-cache-v783-focused`
- 结果：`29 passed`

## 截图

- `e/783/图片/v783-seed-handoff-markdown-split.png`

## 一句话总结

v783 把 promoted seed handoff 的 Markdown 输出从 HTML sections 文件中拆出，让两种 renderer 可以分别维护。

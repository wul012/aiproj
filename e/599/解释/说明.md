# v599 required-term pair fixed-retention objective corpus 运行说明

## 本版目标

v599 根据 v598 的 closeout 结论，新增 fixed-retention objective corpus。目标不是继续强化 `loss`，而是在保留 `loss=loss` 可见性的同时，把 `fixed=` 后的首 token 和完整 `fixed` 续写重新推到训练目标里。

## 新增模式

```text
equals_surface_no_pair_id_fixed_retention_balanced_repair
equals_surface_no_pair_id_fixed_retention_first_token_repair
equals_surface_no_pair_id_fixed_retention_prompt_guard_repair
```

## 关键结果

```text
status=pass
decision=fixed_retention_objective_corpus_modes_ready
new_mode_count=3
pair_id_removed=True
source_prompts=fixed= / loss=
```

## 验证

```text
python -m pytest tests\test_model_capability_required_term_pair_fixed_retention_objective_corpus.py tests\test_model_capability_required_term_pair_coexistence_refresh.py -q -o cache_dir=runs\pytest-cache-v599
27 passed
```

## 截图说明

- `图片/v599-fixed-retention-objective-corpus-contract.png`
  - Playwright MCP 截取的 v599 corpus contract HTML。
  - 证明三个新模式已经进入固定证据归档。

## 结论

v599 把 v598 的“下一步应该做 fixed-retention objective”落成可训练的语料模式，下一步可以用真实 seed 检查它是否改善 `fixed` retention。

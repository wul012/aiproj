# v676 运行截图与解释

## 本版目标

v676 根据 v675 的路线决策，把 `dual-boundary` aligned candidate 扩展到 3 个 seed，判断 pair-full 是否能跨 seed 稳定复现。

本版新增了通用 aligned-candidate seed stability runner，避免继续复用名称误导的 colon-immediate 稳定性报告。

## 关键结果

- `status=pass`
- `decision=required_term_pair_aligned_candidate_partial_stability`
- `seed_count=3`
- `pair_full_seed_count=2`
- `pair_full_seed_rate=0.6667`
- `stable_pair_full=False`
- `model_quality_claim=targeted_pair_refresh_partial_signal`
- `next_action=score pair-full seeds internally and inspect missed seeds before promotion`

## seed 结果

- seed `1535`: pair-full observed
- seed `2535`: no pair-full
- seed `3535`: pair-full observed

## 解释

v676 是必要的降温证据。v672/v673/v674/v675 说明 dual-boundary 在 seed 3535 上表现最好，但 v676 证明它还不是稳定策略：三 seed 中只有两个复现 pair-full。

因此下一步不能直接推广，而应该：

- 对 pair-full seeds 做 forced-choice 内部评分。
- 对 missed seed 2535 做失败分析。
- 保留 `partial stability` 结论，不扩张成泛化能力声明。

## 截图说明

- `e/676/图片/v676-dual-boundary-seed-stability.png`: Playwright MCP 截取的 aligned-candidate seed stability HTML 报告，显示 2/3 pair-full 和下一步建议。

一句话总结：v676 把 dual-boundary 从单 seed 正信号降格为“部分稳定候选”，为后续内部评分和 miss seed 诊断提供真实边界。

# v861 运行截图和解释

## 本版目标

v861 使用 v860 loss-signal bridge corpus 训练一个真实 tiny checkpoint，并记录训练证据。

本版不 replay、不 promotion，也不把 sample 中的近似输出当作能力恢复。

## 输入

- `e/860/解释/bounded-objective-loss-signal-bridge/bounded_objective_loss_signal_bridge.json`
- `e/860/解释/bounded-objective-loss-signal-bridge/bounded_objective_loss_signal_bridge_corpus.txt`

## 输出

产物目录：

```text
e/861/解释/bounded-objective-loss-signal-bridge-training-run/
```

训练 run：

```text
e/861/解释/bounded-objective-loss-signal-bridge-training-run/run/
```

截图：

```text
e/861/图片/v861-bounded-objective-loss-signal-bridge-training-run-html.png
```

## 真实结果

```text
status=pass
decision=bounded_objective_loss_signal_bridge_training_run_ready
bounded_objective_loss_signal_bridge_training_ready=True
final_step=100
final_train_loss=1.4715137481689453
final_val_loss=1.118106722831726
train_loss_delta=-2.121356
repair_example_count=16
neutral_prompt_example_count=6
decoder_anchor_example_count=0
model_quality_claim=training_artifact_only
next_action=run_bounded_objective_loss_signal_bridge_replay_comparison
```

## 训练配置

```text
device=cpu
tokenizer=char
batch_size=8
block_size=64
max_iters=100
eval_interval=10
eval_iters=5
learning_rate=0.001
n_layer=2
n_head=2
n_embd=64
dropout=0.0
seed=1861
```

## sample 边界

sample 输出接近但未达标：

```text
completion: fixed los
c
```

所以 v861 只说明训练产物成立，能力必须等 v862 replay。

## 验证

```text
py_compile: pass
focused pytest: 3 passed
Playwright MCP: snapshot and full-page screenshot saved
```

后续还会跑全量 pytest、source encoding 和 `git diff --check` 后再提交。

## 一句话总结

v861 把 v860 bridge corpus 训练成真实 checkpoint，但模型能力仍由下一版 replay 决定。

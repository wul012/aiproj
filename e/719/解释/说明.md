# v719 运行截图和解释：fixed-recovery contract patch

## 本版目标

v719 把 v718 fixed-recovery repair plan 应用到 v714 structured-template contract 上。

本版不训练模型；它只生成新的 contract patch，为下一版 materialization 做准备。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_fixed_recovery_contract_patch.py e\718\解释\model-capability-required-term-pair-readiness-fixed-recovery-repair-plan --base-contract e\714\解释\model-capability-required-term-pair-readiness-structured-template-contract --out-dir e\719\解释\model-capability-required-term-pair-readiness-fixed-recovery-contract-patch --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_fixed_recovery_contract_patch_ready
failed_count=0
patch_ready=True
base_training_row_count=14
patched_training_row_count=22
added_training_row_count=8
loss_row_count=11
model_quality_claim=contract_patch_only
```

## 截图说明

- `e/719/图片/v719-fixed-recovery-contract-patch.png`

截图展示新增 fixed-recovery rows、checks、patched row count 和 next action。

## 关键结论

- v719 在 v714 的 14 条 structured rows 后追加 8 条 fixed-recovery rows。
- loss rows 仍保留，`loss_row_count=11`。
- `fixed=|loss=` 没有进入 training rows。
- 下一版可以 materialize fixed-recovery patched contract。

一句话总结：v719 把 fixed-recovery 计划落成 contract patch，同时保留 loss 侧信号和 heldout 隔离。

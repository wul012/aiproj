# v697 运行截图和解释：minimal prompt branch-bias diagnostic

## 本版目标

v697 读取 v696 的真实训练报告，不重新训练，专门诊断 `loss=` 为什么没有命中。目标是区分“训练执行失败”和“分支偏置失败”。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_minimal_prompt_branch_bias_diagnostic.py e\696\解释\model-capability-required-term-pair-minimal-prompt-training --out-dir e\697\解释\model-capability-required-term-pair-minimal-prompt-branch-bias-diagnostic --require-pass --force
```

结果：

```text
status=pass
decision=minimal_prompt_branch_bias_fixed_absorbs_loss
fixed_hit_count=2
loss_hit_count=0
loss_prompt_fixed_start_count=2
dominant_bias=fixed
model_quality_claim=diagnostic_only
```

focused tests：

```powershell
python -m pytest tests\test_model_capability_required_term_pair_minimal_prompt_branch_bias_diagnostic.py -q -o cache_dir=runs\pytest-cache-v697-focused
```

结果：

```text
5 passed
```

## 截图说明

- `e/697/图片/v697-minimal-prompt-branch-bias-diagnostic.png`

截图展示 branch-bias diagnostic HTML 页面，能看到 fixed/loss hit count、loss-to-fixed start count、dominant bias 和 next action。

## 结论

v697 确认 v696 的失败不是 checkpoint 缺失或 replay 中断，而是：

- `fixed=` 在 default 和 suppression profile 都命中。
- `loss=` 在两个 profile 都以 `fixed` 开头。
- 分支主导偏置为 `fixed`。

一句话总结：v697 把 v696 的负结果定位为 `fixed` 吸收 `loss` 的 branch-bias failure，为 v698 的 loss-first-token/branch-separation 修复提供依据。

# v583 required-term pair branch-binding route decision 运行说明

## 本版目标

v582 已经证明 v579/v581 两条 branch-binding 变体都不如 v571。v583 把这个判断固化成机器可读 route decision，避免后续继续在 branch-binding v1/v2 上重复训练。

本版不训练、不改语料。它把 comparison 结论转成可被后续 closeout 读取的停止条件。

## 运行命令

```powershell
python scripts\run_model_capability_required_term_pair_branch_binding_route_decision.py `
  e\582\解释\model-capability-required-term-pair-branch-binding-comparison `
  --out-dir e\583\解释\model-capability-required-term-pair-branch-binding-route-decision `
  --force `
  --require-pass
```

## 关键结果

```text
status=pass
decision=stop_branch_binding_v1_and_keep_residual_baseline
branch_binding_route_count=2
branch_binding_visible_hit_route_count=0
best_residual_signal=v571-loss-balanced
model_quality_claim=route_decision_only
```

`v571-loss-balanced` 不是成功路线，只是相对 v579/v581 仍保留 partial fixed hit 的 residual baseline。

## 截图说明

- `图片/v583-branch-binding-route-decision.png`
  - Playwright MCP 截取的 HTML decision 页面。
  - 证明 route rows、decision 和 next action 可见。

## 结论

v583 正式停止 branch-binding v1/v2。后续如果继续 required-term pair route，必须提出强于 v579/v581 的新 objective，而不是继续改同类 binding 句。

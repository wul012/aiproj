# v786 promoted decision HTML split

## 目标

v786 是维护拆分版本，不新增 promoted training scale decision 功能。目标是把 `promoted_training_scale_decision_artifacts.py` 中的 HTML renderer、HTML writer、CSS 和 HTML-only helper 拆到独立模块。

## 关键变化

- 新增 `src/minigpt/promoted_training_scale_decision_html.py`。
- `src/minigpt/promoted_training_scale_decision_artifacts.py` 从 628 行降到 415 行。
- artifact 主模块继续 re-export `render_promoted_training_scale_decision_html` 和 `write_promoted_training_scale_decision_html`。
- JSON、CSV、Markdown、outputs 接线保持不变。

## 验证

- `python -m py_compile src\minigpt\promoted_training_scale_decision_artifacts.py src\minigpt\promoted_training_scale_decision_html.py`
- `python -m pytest tests\test_promoted_training_scale_decision.py tests\test_promoted_training_scale_decision_review.py -q -o cache_dir=runs\pytest-cache-v786-focused`
- `python -B scripts\check_source_encoding.py --out-dir runs\source-encoding-hygiene-v786`
- `git diff --check`

结果：focused 测试 `13 passed`，source encoding `status=pass`，diff check 无错误。

## 截图

- `e/786/图片/v786-promoted-decision-html-split.png`

## 一句话总结

v786 把 promoted decision 的 HTML 输出面从 artifact 主模块拆出，让 artifact 主模块回到格式协调层。

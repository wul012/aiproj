# v701 运行截图和解释：minimal prompt balanced repair plan

## 本版目标

v701 根据 v700 的 first-token tradeoff，对下一轮训练提出 balanced repair plan。它新增并验证 `minimal_prompt_balanced_first_token_repair_objective`，目标是在不使用 contextual anchor 的前提下同时平衡 fixed/loss 首 token 和 direct target。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_minimal_prompt_balanced_repair_plan.py e\700\解释\model-capability-required-term-pair-minimal-prompt-tradeoff-comparison --out-dir e\701\解释\model-capability-required-term-pair-minimal-prompt-balanced-repair-plan --require-pass --force
```

结果：

```text
status=pass
decision=minimal_prompt_balanced_repair_plan_ready
plan_ready=True
proposed_corpus_mode=minimal_prompt_balanced_first_token_repair_objective
seed_to_rerun=3535
repair_focus=balanced_first_token_and_direct_target_retention
model_quality_claim=repair_plan_only
```

focused tests：

```powershell
python -m pytest tests\test_model_capability_required_term_pair_minimal_prompt_balanced_repair_plan.py tests\test_model_capability_required_term_pair_minimal_prompt_loss_first_token_repair_plan.py -q -o cache_dir=runs\pytest-cache-v701-focused
```

结果：

```text
8 passed
```

## 截图说明

- `e/701/图片/v701-minimal-prompt-balanced-repair-plan.png`

截图展示 balanced repair plan HTML 页面，包含 proposed corpus mode、seed、focus 和检查项。

## 结论

v701 确认下一步不是继续单边加权，而是：

- 使用 `minimal_prompt_balanced_first_token_repair_objective`。
- 同时增强 fixed/loss first-token rows。
- 保留 minimal prompt 边界。
- 同 seed `3535` 重新训练。

一句话总结：v701 把 v700 的 tradeoff 证据转成 balanced first-token 修复计划，为 v702 真实训练做准备。

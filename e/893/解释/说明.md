# v893 运行说明

## 本版目标

v893 在 v892 的 delta 结论之后，直接检查 v890 checkpoint 在 `prompt + "\nfixed l"` 后是否愿意继续生成 `oss`。

本版只做 teacher-forced next-token probability probe，不重新训练，不宣称 replay pass。

## 关键输入

- objective contract：`e/836/解释/model-capability-route-promotion-bounded-objective-contract/model_capability_route_promotion_bounded_objective_contract.json`
- replay delta diagnostic：`e/892/解释/bounded-objective-loss-signal-bridge-target-only-memory-stagnation-aware-suffix-replay-delta-diagnostic/bounded_objective_loss_signal_bridge_target_only_memory_stagnation_aware_suffix_replay_delta_diagnostic.json`
- checkpoint：`e/890/解释/bounded-objective-loss-signal-bridge-target-only-memory-stagnation-aware-suffix-training-run/run/checkpoint.pt`
- tokenizer：`e/890/解释/bounded-objective-loss-signal-bridge-target-only-memory-stagnation-aware-suffix-training-run/run/tokenizer.json`

## 关键输出

- v893 probe JSON：`e/893/解释/bounded-objective-loss-signal-bridge-target-only-memory-loss-token-probability-probe/bounded_objective_loss_signal_bridge_target_only_memory_loss_token_probability_probe.json`
- v893 probe HTML：`e/893/解释/bounded-objective-loss-signal-bridge-target-only-memory-loss-token-probability-probe/bounded_objective_loss_signal_bridge_target_only_memory_loss_token_probability_probe.html`
- 运行截图：`e/893/图片/v893-bounded-objective-loss-signal-bridge-target-only-memory-loss-token-probability-probe-html.png`

## 运行结果

- `status=pass`
- `decision=bounded_objective_loss_signal_bridge_target_only_memory_loss_token_probability_probe_loss_suffix_top1_but_decode_blocked`
- `target_top1_rate=1.0`
- `target_topk_rate=1.0`
- `max_target_rank=1`
- `low_probability_step_count=0`
- `min_target_probability=0.9280321598052979`
- `mean_target_probability=0.952898469236162`

三条 contract case 的 `oss` 三步都是 top-1：

```text
canonical_direct_completion: o=0.9545 rank=1, s=0.9481 rank=1, s=0.9655 rank=1
minimal_direct_completion:   o=0.9513 rank=1, s=0.9548 rank=1, s=0.9617 rank=1
completion_label_surface:    o=0.9280 rank=1, s=0.9445 rank=1, s=0.9676 rank=1
```

## 结论

v893 说明模型在 `fixed l` 后已经强烈偏向继续生成 `oss`。v891 仍然只输出 `\nfixed l`，更可能是 replay 生成预算、截断长度或停止条件造成的，而不是模型不知道 `loss` 后缀。

下一步不应该继续盲目扩数据，应检查 decoder budget / max-new-tokens / replay continuation 截断。

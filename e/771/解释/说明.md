# v771 运行截图和解释：objective-level contrast seed 3838 pair-probe replay

## 本版目标

v771 消费 v770 seed `3838` training checkpoint，运行 heldout pair-probe replay。

本版不重新训练，不调整解码参数，也不接受 checkpoint。它补齐 v767 seed stability plan 中第二个补充 seed 的 replay 证据。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_fixed_preserving_transfer_pair_probe_replay.py e\770\解释\model-capability-required-term-pair-readiness-objective-level-contrast-seed-3838-training-run --out-dir e\771\解释\model-capability-required-term-pair-readiness-objective-level-contrast-seed-3838-pair-probe-replay --require-pass --force
```

输出摘要：

```text
status=pass
decision=pair_readiness_fixed_preserving_transfer_pair_probe_replay_ready
failed_count=0
exact_heldout_pair_full=True
required_all_pair_full=True
pair_full_count=2
model_quality_claim=pair_probe_replay_ready
next_action=compare against prior routes and run stricter promotion guards before accepting the checkpoint
```

## 截图说明

- `e/771/图片/v771-objective-level-contrast-seed-3838-pair-probe-replay.png`

截图确认 HTML 页面可见：

- `Status pass`
- `Decision pair_readiness_fixed_preserving_transfer_pair_probe_replay_ready`
- `Exact pair True`
- `Required all True`
- `Pair-full count 2`

## 关键观察

- seed `3838` replay-ready。
- seed `3737` 和 seed `3838` 都是 `pair_full_count=2`。
- 加上 v764 seed `3636` 的 replay-ready，seed stability rollup 的输入已经齐备。
- 下一版应做三 seed rollup，统一记录 ready count、pair-full count 分布和 acceptance-review 边界。

一句话总结：v771 补齐 objective-level contrast 的第三个 seed replay 证据，使 seed stability rollup 可以正式收束。

# v933 randomized holdout publication registry manifest review 运行说明

## 本版目标

v933 消费 v932 manifest，做只读 review：

```text
v932 registry manifest
  -> v933 registry manifest review
```

它确认 manifest 可以被后续治理查询入口消费，但明确拒绝把该 manifest 当作 production promotion。

明确不做：

- 不重新训练。
- 不重新 replay。
- 不改写 v932 manifest。
- 不批准 production promotion。

## 输入和输出

输入：

```text
e/932/解释/randomized-holdout-publication-registry-manifest
```

输出：

```text
e/933/解释/randomized-holdout-publication-registry-manifest-review
```

截图：

```text
e/933/图片/v933-randomized-holdout-publication-registry-manifest-review.png
```

## 实际结果

真实 v932 输入运行结果：

```text
status=pass
decision=randomized_holdout_publication_registry_manifest_review_ready
failed_count=0
randomized_holdout_publication_registry_manifest_review_ready=True
review_id=randomized-holdout-publication-registry-manifest-review-v933
review_status=approved_for_governance_lookup_only
entry_count=1
lookup_ready=True
bounded_publication_accepted=True
promotion_ready=False
consumer_boundary=governance_lookup_only
allowed_use=governance_lookup_only
rejected_use=production_promotion
next_step=build_randomized_holdout_publication_registry_lookup_packet
passed_check_count=17
failed_check_count=0
```

## 截图验证

Playwright MCP 已保存 HTML 快照和截图。页面可见：

- `Status = pass`
- `Review ready = True`
- `Lookup = True`
- `Promotion = False`
- `Rejected use = production_promotion`
- `Failed = 0`

## 一句话总结

v933 把 v932 manifest 复核为 governance lookup only 的可消费证据，同时继续阻断 production promotion。

# v595 required-term pair loss-branch targeted seed stability 运行说明

## 本版目标

v595 按 v594 的 route decision，对 `equals_surface_no_pair_id_loss_branch_targeted_repair` 做 3 seed 稳定性 replay。目标不是推广它，而是确认 loss-only tradeoff 是否跨 seed 复现。

## 运行命令

```text
python -B scripts\run_model_capability_required_term_pair_colon_immediate_stability.py ^
  --out-dir e\595\解释\model-capability-required-term-pair-loss-branch-targeted-seed-stability ^
  --seeds 3535 4535 5535 ^
  --corpus-mode equals_surface_no_pair_id_loss_branch_targeted_repair ^
  --max-iters 1800 ^
  --device cpu ^
  --force
```

## 关键结果

```text
status=pass
seed_count=3
pair_full_seed_count=0
pair_full_seed_rate=0.0
stable_pair_full=False
continuation_hit_count_per_seed=1
```

## 截图说明

- `图片/v595-loss-branch-targeted-seed-stability.png`
  - Playwright MCP 截取的 v595 stability HTML。
  - 证明 3 个 seed 都训练通过，但都没有 pair-full。

## 结论

v595 证明 targeted loss branch 的 tradeoff 可复现：它能保持一个分支命中，但不能形成 fixed/loss 共存。因此下一步应做 missed-seed/first-token 诊断，然后进入 fixed-retention objective。

# v718 运行截图和解释：fixed-recovery repair plan

## 本版目标

v718 读取 v717 route comparison，把 structured-template 的失败形态转成 fixed-recovery repair plan。

本版不改 contract、不训练模型；它只确认下一步是否应该做 fixed recovery patch。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_fixed_recovery_repair_plan.py e\717\解释\model-capability-required-term-pair-readiness-route-comparison --out-dir e\718\解释\model-capability-required-term-pair-readiness-fixed-recovery-repair-plan --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_fixed_recovery_repair_plan_ready
failed_count=0
plan_ready=True
proposed_next_artifact=pair_readiness_fixed_recovery_contract_patch
source_structured_hit_terms=['loss']
source_structured_missed_terms=['fixed']
model_quality_claim=plan_only
```

## 截图说明

- `e/718/图片/v718-fixed-recovery-repair-plan.png`

截图展示 plan checks、next artifact、structured hit/missed terms 和 contract patch 建议。

## 关键结论

- v716 已恢复 `loss`。
- v716 仍 miss `fixed`。
- v718 计划只针对 fixed recovery，不丢弃 structured route 的 loss 信号。
- 下一版应构建 `pair_readiness_fixed_recovery_contract_patch`。

一句话总结：v718 把 v717 的三路比较结论落成 fixed recovery 计划，避免盲目继续扩语料。

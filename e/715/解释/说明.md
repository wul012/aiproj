# v715 运行截图和解释：structured-template corpus materialization

## 本版目标

v715 把 v714 structured-template contract materialize 成真实训练 corpus 和 heldout eval fixture。

本版不训练模型；它是训练前的数据落地版本，确认 v714 的 contract 可以进入既有 materialization 链路。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_corpus_materialization.py e\714\解释\model-capability-required-term-pair-readiness-structured-template-contract --out-dir e\715\解释\model-capability-required-term-pair-readiness-structured-template-corpus-materialization --repeat 320 --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_corpus_materialized
failed_count=0
materialization_ready=True
training_line_count=4480
evaluation_probe_count=3
model_quality_claim=data_artifact_only
```

## 截图说明

- `e/715/图片/v715-structured-template-corpus-materialization.png`

截图展示 materialized paths、checks、corpus preview 和 next action。

## 关键结论

- v715 使用目录输入读取 v714 contract，说明 locator 已支持 structured-template 输出目录。
- 14 条 structured template rows 重复 320 次后形成 4480 行训练 corpus。
- heldout eval fixture 仍保留 3 个 probes。
- `fixed=|loss=` 没有进入 corpus line，pair probe 仍隔离。

一句话总结：v715 把 v714 的结构化 contract 落成可训练数据，为 v716 真实 tiny training 做好输入准备。

# v738 运行截图和解释：direct-completion surface training run

## 本版目标

v738 使用 v737 的 5120 行 direct-completion surface corpus 进行真实 tiny GPT 训练。

目标是验证更紧凑的 `fixed=fixed` / `loss=loss` surface 是否比 bridge rows 更适合 heldout direct probes。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_training_run.py e\737\解释\model-capability-required-term-pair-readiness-direct-completion-surface-corpus-materialization --out-dir e\738\解释\model-capability-required-term-pair-readiness-direct-completion-surface-training-run --seed 3535 --max-iters 1800 --eval-iters 2 --batch-size 16 --block-size 16 --n-layer 2 --n-head 2 --n-embd 96 --learning-rate 0.01 --max-new-tokens 12 --temperature 0.2 --top-k 1 --device cpu --require-pass --force
```

输出摘要：

```text
status=pass
decision=pair_readiness_training_pair_full_observed
training_status=pass
checkpoint_exists=True
pair_full_observed=True
default_continuation_hit_count=2
model_quality_claim=direct_pair_probe_hit
```

## 截图说明

- `e/738/图片/v738-direct-completion-surface-training-run.png`

截图显示 `Pair-full=True`，claim 为 `direct_pair_probe_hit`，下一步要求先做 heldout pair-probe replay，不直接 promotion。

## 关键观察

- v738 是本轮 direct-completion route 的第一条正向训练证据。
- default profile 命中 `fixed` 和 `loss` 两个 heldout direct probes。
- suppress-newline profile 也保持 pair-full，说明结果不只是单个 replay profile 的偶然输出。
- 这仍然不是最终 promotion，因为还需要和 v729/v733 等历史路线比较，并做 heldout pair-probe 复核。

一句话总结：v738 证明 direct-completion surface route 能在同配置 tiny 训练下恢复 heldout direct pair-full，但仍需要下一版比较和复核。

# v653 运行截图与解释

## 本版目标

v653 把 surface-first schedule 结果放回 generation/internal alignment 全矩阵，和 v621、v628、v630、v634、v640、v645 的路线一起比较。

## 关键结果

- `status=pass`
- `decision=keep_generation_pair_full_and_repair_internal_preference`
- `generation_pair_full_count=1`
- `internal_pair_full_count=2`
- `aligned_pair_full_count=0`
- `best_source_labels=loss-internal-first-token,loss-internal-joint-cycle`

## 新路线定位

`surface-first-schedule` 的对比行是：

- `generation_hit_terms=["fixed"]`
- `internal_expected_best_terms=["fixed"]`
- `alignment_class=fixed_only_aligned_partial`

它既不是 generation pair-full，也不是 internal pair-full，更不是 aligned pair-full。

## 截图说明

- `e/653/图片/v653-alignment-comparison-with-surface-first-schedule.png`：alignment comparison HTML 截图，显示 Generation full=1、Internal full=2、Aligned full=0。

## 证据角色

本版让 v651/v652 的负证据进入统一矩阵。结论不是“继续 surface-first”，而是确认当前最强生成候选仍是 v630 `loss-internal-joint-cycle`，当前最强内部锚点仍包括 v621/v640 这类 internal-full 路线。

一句话总结：v653 证明 surface-first schedule 没有改变总体路线选择，项目仍需要保住 v630 生成 pair-full 后再修内部偏好。

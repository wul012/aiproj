# v579 required-term pair branch-binding seed 3535 运行说明

## 本版目标

v578 已经关闭 first-token rows 和 width scaling 两条路线。v579 开始新的 branch-binding objective：让训练语料直接表达 `fixed=` 绑定 `fixed`、`loss=` 绑定 `loss`，并避免继续使用 numeric pair id。

本版不宣称能力提升。它先建立 branch-binding v1 的真实训练基线。

## 运行命令

```powershell
python scripts\run_model_capability_required_term_pair_colon_immediate_stability.py `
  --out-dir e\579\解释\model-capability-required-term-pair-branch-binding-seed-3535 `
  --seeds 3535 `
  --corpus-mode equals_surface_no_pair_id_branch_binding_repair `
  --repeat 260 `
  --bridge-repeat 20 `
  --max-iters 1400 `
  --eval-iters 2 `
  --batch-size 16 `
  --block-size 16 `
  --n-layer 1 `
  --n-head 1 `
  --n-embd 64 `
  --learning-rate 0.02 `
  --max-new-tokens 12 `
  --temperature 0.8 `
  --top-k 2 `
  --device cpu `
  --force `
  --require-pass
```

## 关键结果

```text
status=pass
decision=required_term_pair_colon_immediate_not_stable
seed_count=1
pair_full_seed_count=0
stable_pair_full=False
continuation_hit_count=0
```

branch-binding v1 没有恢复 seed `3535` 的 pair-full，也没有保留 v571 那种 partial fixed hit。

## 截图说明

- `图片/v579-branch-binding-seed-3535.png`
  - Playwright MCP 截取的 HTML stability 报告。
  - 证明新 corpus mode 的真实训练报告和负结果可见。

## 代码边界

新增语料逻辑放入：

```text
src/minigpt/model_capability_required_term_pair_branch_binding_corpus.py
```

旧 `model_capability_required_term_pair_coexistence_corpus.py` 只做薄路由，避免继续膨胀。

## 结论

v579 建立了 branch-binding objective 的第一条负基线。下一步不应该直接做 seed sweep，而应该先诊断新语料为什么连 partial hit 都没有保住。

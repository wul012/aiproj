# v864 运行截图和解释

## 本版目标

v864 根据 v863 的 `pair_binding_gap` 诊断构建 targeted pair-binding patch。

本版不训练、不 replay、不 promotion；它只生成下一次训练可消费的 JSONL 和 corpus。

## 输入

```text
partial-hit diagnostic:
e/863/解释/bounded-objective-loss-signal-bridge-partial-hit-diagnostic/

replay comparison:
e/862/解释/bounded-objective-loss-signal-bridge-replay-comparison/

source corpus:
e/861/解释/bounded-objective-loss-signal-bridge-training-run/run/prepared_corpus.txt
```

## 输出

```text
e/864/解释/bounded-objective-loss-signal-bridge-pair-binding-patch/
```

主要文件：

```text
bounded_objective_loss_signal_bridge_pair_binding_patch.json
bounded_objective_loss_signal_bridge_pair_binding_patch.csv
bounded_objective_loss_signal_bridge_pair_binding_patch_examples.jsonl
bounded_objective_loss_signal_bridge_pair_binding_patch_corpus.txt
bounded_objective_loss_signal_bridge_pair_binding_patch.txt
bounded_objective_loss_signal_bridge_pair_binding_patch.md
bounded_objective_loss_signal_bridge_pair_binding_patch.html
```

截图：

```text
e/864/图片/v864-bounded-objective-loss-signal-bridge-pair-binding-patch-html.png
```

## 真实结果

```text
status=pass
decision=bounded_objective_loss_signal_bridge_pair_binding_patch_ready
pair_binding_patch_ready=True
patch_example_count=18
case_pair_repeat_count=6
pair_binding_example_count=6
completion_surface_example_count=2
decoder_anchor_example_count=0
patched_corpus_char_count=4771
model_quality_claim=pair_binding_patch_only
next_step=train_bounded_objective_loss_signal_bridge_pair_binding_patch
```

## 样本类型

```text
case_pair_repeat
fixed_to_loss_binding
fixed_to_pair_binding
loss_to_pair_binding
loss_left_context_binding
completion_surface_pair_repair
global_pair_binding
global_pair_repeat
```

这些样本围绕三个失败 case 的原始 prompt surface，直接强化 `fixed loss` 的有序输出。

## 边界

v864 只说明 patch 数据已生成：

- 还没有新 checkpoint。
- 还没有 replay 改善证据。
- 不能 claim objective contract recovered。

## 验证

```text
py_compile: pass
focused pytest: 3 passed
real patch build: pass, require-patch-ready
Playwright MCP: snapshot and full-page screenshot saved
```

后续还会跑全量 pytest、source encoding 和 `git diff --check` 后再提交。

## 一句话总结

v864 把 v863 的 pair-binding gap 转成可训练 patch corpus，为下一版真实训练准备输入。

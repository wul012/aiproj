# v640 required-term pair joint-cycle internal-repair seed 3535 运行说明

## 本版目标

v640 运行 v639 新增的 `joint-cycle internal-repair` corpus mode，验证它是否能保住 v630 的 generation pair-full，同时修复 v631 的 internal gap。

本版是真实 tiny 训练。

## 训练配置

```text
seed=3535
corpus_mode=equals_surface_no_pair_id_loss_internal_joint_cycle_internal_repair
repeat=320
bridge_repeat=24
max_iters=1800
block_size=16
n_embd=64
learning_rate=0.02
device=cpu
```

## 生成结果

```text
status=pass
decision=required_term_pair_coexistence_refresh_no_pair_full
training_status=pass
checkpoint_exists=True
pair_full_observed=False
```

replay 观察：

```text
fixed= -> l losssssss
loss=  -> l sssssssss
```

解释：internal-repair 语料没有保住 v630 的 generation pair-full，反而把生成推向 `l...` 片段漂移。

## 截图说明

- `图片/v640-joint-cycle-internal-repair-seed-3535.png`
  - 训练/replay HTML 截图，证明 checkpoint 存在，但 `Pair full observed=False`。

## 结论

v640 是关键负结果：直接增强 internal rows 会破坏 generation pair-full。v641 需要用 forced-choice 判断 internal 是否至少被修复。

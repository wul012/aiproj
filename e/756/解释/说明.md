# v756 运行截图和解释：exact-surface repair pair-probe replay

## 本版目标

v756 对 v755 checkpoint 做独立 pair-probe replay，验证 exact-surface repair 训练结果是否能通过 required exact heldout pair prompt。

本版不 promotion；它是 v755 训练结果的复核层。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_fixed_preserving_transfer_pair_probe_replay.py e\755\解释\model-capability-required-term-pair-readiness-exact-surface-repair-training-run --out-dir e\756\解释\model-capability-required-term-pair-readiness-exact-surface-repair-pair-probe-replay --require-pass --force
```

输出摘要：

```text
status=pass
decision=pair_readiness_fixed_preserving_transfer_pair_probe_replay_partial
exact_heldout_pair_full=False
required_all_pair_full=False
pair_full_count=1
model_quality_claim=pair_probe_replay_partial
```

## 截图说明

- `e/756/图片/v756-exact-surface-repair-pair-probe-replay.png`

截图显示 exact-heldout-pair 和 spaced-heldout-pair 均未 pair-full，arrow-heldout-pair 仍 pair-full。

## 关键观察

- v755 的训练内 pair-full 没有转化成 required exact heldout pair replay ready。
- v756 与 v750 的 replay 形态一致：exact/spaced 只命中 1，arrow 命中 2。
- v753 的 near-exact rows 没能把能力迁移到 exact `fixed=|loss=` surface。
- 下一步应做 ineffective comparison/closeout，而不是继续盲目扩大 near-exact rows。

一句话总结：v756 证明 exact-surface repair route 仍停在 partial replay，不能 promotion。

# v865 运行截图和解释

## 本版目标

v865 使用 v864 pair-binding patch corpus 训练一个真实 tiny checkpoint，并记录训练证据。

本版不 replay、不 promotion，也不把 sample 当作能力恢复证明。

## 输入

```text
e/864/解释/bounded-objective-loss-signal-bridge-pair-binding-patch/
```

训练语料：

```text
e/864/解释/bounded-objective-loss-signal-bridge-pair-binding-patch/bounded_objective_loss_signal_bridge_pair_binding_patch_corpus.txt
```

## 输出

```text
e/865/解释/bounded-objective-loss-signal-bridge-pair-binding-training-run/
```

训练 run：

```text
e/865/解释/bounded-objective-loss-signal-bridge-pair-binding-training-run/run/
```

截图：

```text
e/865/图片/v865-bounded-objective-loss-signal-bridge-pair-binding-training-run-html.png
```

## 真实结果

```text
status=pass
decision=bounded_objective_loss_signal_bridge_pair_binding_training_run_ready
bounded_objective_loss_signal_bridge_pair_binding_training_ready=True
final_step=120
final_train_loss=1.2852036952972412
final_val_loss=1.0368845462799072
train_loss_delta=-2.32423
repair_example_count=18
neutral_prompt_example_count=6
decoder_anchor_example_count=0
model_quality_claim=training_artifact_only
next_action=run_bounded_objective_loss_signal_bridge_pair_binding_replay_comparison
```

## 训练配置

```text
device=cpu
tokenizer=char
batch_size=8
block_size=64
max_iters=120
eval_interval=10
eval_iters=5
learning_rate=0.001
n_layer=2
n_head=2
n_embd=64
dropout=0.0
seed=1865
```

## sample 边界

sample 没有直接输出 `fixed loss`：

```text
Complete with exactly two tokens: fixed loss
completion:


ucaler:
p
```

所以 v865 只说明训练产物成立，能力仍必须由 v866 replay comparison 判断。

## 验证

```text
py_compile: pass
focused pytest: 3 passed
real training: pass
training evidence: pass, require-training-ready
Playwright MCP: snapshot and full-page screenshot saved
```

后续还会跑全量 pytest、source encoding 和 `git diff --check` 后再提交。

## 一句话总结

v865 把 v864 pair-binding patch 训练成真实 checkpoint，但模型能力是否改善仍等待 v866 replay。

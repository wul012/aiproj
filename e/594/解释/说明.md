# v594 required-term pair loss-branch route decision 运行说明

## 本版目标

v594 新增并运行 loss-branch route decision。它读取 v593 comparison，明确三件事：当前路线不可推广、targeted route 只作为稳定性基线、后续必须设计 fixed-retention objective。

## 关键结果

```text
status=pass
decision=select_targeted_loss_branch_for_seed_stability_not_promotion
route_count=3
pair_full_route_count=0
loss_only_tradeoff_route_count=3
selected_stability_route=v590-targeted
selected_stability_corpus_mode=equals_surface_no_pair_id_loss_branch_targeted_repair
fixed_retention_objective_required=True
```

## 截图说明

- `图片/v594-loss-branch-route-decision.png`
  - Playwright MCP 截取的 v594 route decision HTML。
  - 证明本版没有推广当前 route，只选择 targeted 做 v595 稳定性基线。

## 结论

v594 把 v593 的比较结论变成下一步执行策略：不要推广 loss-only route，但用最简单的 targeted objective 做稳定性 replay，确认 tradeoff 是否可复现。

# v774 运行截图和解释：handoff review recommendations split

## 本版目标

v774 继续维护性拆分，不新增功能。目标是拆 `promoted_training_scale_seed_handoff_review.py` 中的 recommendation 区块，把“建数据”和“写建议文案”分开。

## 改动结果

```text
src/minigpt/promoted_training_scale_seed_handoff_review.py: 779 -> 613 行
src/minigpt/promoted_training_scale_seed_handoff_review_recommendations.py: 新增 190 行
```

## 边界说明

- review 主文件继续负责 batch summary、clean evidence requirement、clean batch review requirement、automation gate、automation summary、suite alignment。
- recommendations 模块负责 `build_seed_handoff_review_recommendations()` 和 clean-batch detail 文案。
- 原来的 `from minigpt.promoted_training_scale_seed_handoff_review import build_seed_handoff_review_recommendations` 仍然可用。

## 验证命令

```powershell
python -m py_compile src\minigpt\promoted_training_scale_seed_handoff_review.py src\minigpt\promoted_training_scale_seed_handoff_review_recommendations.py
python -m pytest tests\test_promoted_training_scale_seed_handoff.py tests\test_promoted_training_scale_seed_handoff_review.py -q -o cache_dir=runs\pytest-cache-v774-focused
```

结果：

```text
33 passed
```

## 截图说明

- `e/774/图片/v774-handoff-review-recommendations-split.png`

一句话总结：v774 把 handoff review 的建议文案生成从核心 review builder 中切出，降低主文件阅读负担并保留原公共导入路径。

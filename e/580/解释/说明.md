# v580 required-term pair branch-binding whitespace diagnostic 运行说明

## 本版目标

v579 的 branch-binding v1 没有恢复 seed `3535`。v580 不急着做新训练，而是复用 missed-seed diagnostic，定位失败到底是 fixed/loss 互相竞争，还是生成边界先漂移。

本版不改代码、不训练、不扩大 sweep。它只读 v579 证据。

## 运行命令

```powershell
python scripts\run_model_capability_required_term_pair_colon_immediate_missed_seed_diagnostic.py `
  e\579\解释\model-capability-required-term-pair-branch-binding-seed-3535 `
  --out-dir e\580\解释\model-capability-required-term-pair-branch-binding-seed-3535-diagnostic `
  --force `
  --require-pass
```

## 关键结果

```text
status=pass
decision=required_term_pair_colon_immediate_first_token_gap
missed_seed_count=1
missed_expected_top_count=0
missed_first_token_gap_count=1
fixed_expected_rank=3
loss_expected_rank=2
fixed_top_token=<space>
loss_top_token=<space>
```

这说明 v579 的 branch-binding v1 不是简单地把 `loss` 误选成 `fixed`，而是生成第一 token 先落到空格。

## 截图说明

- `图片/v580-branch-binding-seed-3535-diagnostic.png`
  - Playwright MCP 截取的 missed-seed diagnostic HTML。
  - 证明 decision、missed count 和 first-token gap 可见。

## 结论

v580 给 v581 的方向很清楚：需要 whitespace-bound branch-binding 变体，先把 prompt 后的空格边界控制住，再观察 fixed/loss 是否能回到 pair-full。

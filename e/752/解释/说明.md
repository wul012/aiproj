# v752 运行截图和解释：exact-surface repair plan

## 本版目标

v752 基于 v751 prompt-surface sensitivity diagnostic，生成 minimal exact-surface repair plan。

本版不训练模型、不修改 contract、不添加 rows；它只把下一步修复边界定清楚：补 exact surface，但不能把 exact heldout prompt `fixed=|loss=` 直接放进训练数据。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_exact_surface_repair_plan.py e\751\解释\model-capability-required-term-pair-readiness-fixed-preserving-transfer-prompt-surface-sensitivity-diagnostic --out-dir e\752\解释\model-capability-required-term-pair-readiness-exact-surface-repair-plan --require-pass --force
```

输出摘要：

```text
status=pass
decision=pair_readiness_exact_surface_repair_plan_ready
plan_ready=True
proposed_next_artifact=pair_readiness_exact_surface_repair_contract_patch
repair_row_budget=4
missed_required_surfaces=['exact-heldout-pair']
observed_pair_full_surfaces=['arrow-heldout-pair']
model_quality_claim=plan_only
```

## 截图说明

- `e/752/图片/v752-exact-surface-repair-plan.png`

截图显示 plan ready、next artifact、row budget 4，以及 patch strategy：保留 direct rows 和 fixed-preserving transfer rows，新增不超过 4 条 near-exact bridge rows，并避免训练 exact heldout prompt。

## 关键观察

- v752 的输入是 v751 的真实诊断，不是凭空规划。
- 计划要求从 `arrow-heldout-pair` 的 pair-full 信号迁移到 exact surface。
- 计划明确阻止将 `fixed=|loss=` 作为训练 row，避免污染 heldout eval。
- 下一版应实现 checked contract patch，而不是直接训练。

一句话总结：v752 将 prompt-surface sensitivity 诊断转成可执行但仍保守的 exact-surface repair 计划。

# v589 required-term pair loss-branch objective corpus contract 运行说明

## 本版目标

v589 把 v588 指出的 `loss-branch objective` 先落成可复用 corpus contract。它新增 3 个 corpus mode，但不把实现继续塞进已经接近阈值的主 corpus 文件。

## 新增模式

```text
equals_surface_no_pair_id_loss_branch_targeted_repair
equals_surface_no_pair_id_loss_branch_dual_anchor_repair
equals_surface_no_pair_id_loss_branch_micro_span_repair
```

## 验证

```text
python -m py_compile ...
python -m pytest tests\test_model_capability_required_term_pair_coexistence_refresh.py -q -o cache_dir=runs\pytest-cache-v589
23 passed in 0.35s
git diff --check
pass
```

## 截图说明

- `图片/v589-loss-branch-objective-corpus-contract.png`
  - Playwright MCP 截取的 v589 contract HTML。
  - 证明新模式、状态和测试结果可见。

## 结论

v589 是这轮十版的真实起点：先把 loss branch 的训练输入做成独立、可测试、可扩展的 corpus contract，再进入真实 seed 训练。

# v699 运行截图和解释：minimal prompt loss first-token training

## 本版目标

v699 按 v698 repair plan 真跑 seed `3535`，使用 `minimal_prompt_loss_first_token_repair_objective`。目标是验证加强 loss 首 token 后，是否能修复 v696 的 `loss=` 被 fixed 吸走问题。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_coexistence_refresh.py --out-dir e\699\解释\model-capability-required-term-pair-minimal-prompt-loss-first-token-training --seed 3535 --corpus-mode minimal_prompt_loss_first_token_repair_objective --repeat 260 --bridge-repeat 20 --max-iters 1400 --eval-iters 2 --batch-size 16 --block-size 16 --n-layer 1 --n-head 1 --n-embd 64 --learning-rate 0.02 --max-new-tokens 12 --temperature 0.2 --top-k 1 --device cpu --require-pass --force
```

结果：

```text
status=pass
decision=required_term_pair_coexistence_refresh_no_pair_full
training_status=pass
checkpoint_exists=True
pair_full_observed=False
model_quality_claim=not_claimed
```

## 关键观察

v699 修复了 v696 的 loss miss，但造成 fixed 回退：

- `fixed=` 生成 `fixed=losssssss= l`，fixed 未命中。
- `loss=` 生成 `loss= losssssss= `，loss 命中。
- default 和 suppression profile 结果一致。

这说明 loss-first-token 修复方向有效，但权重过强，变成 loss-dominant tradeoff。

## 截图说明

- `e/699/图片/v699-minimal-prompt-loss-first-token-training.png`

截图展示训练报告页面，保留 no pair-full 决策和 replay variants。

## 链路结论

v699 是第二个有价值的真实训练证据：

- v696：fixed 命中，loss 被 fixed 吸走。
- v699：loss 命中，fixed 被 loss 吸走。

下一步最合理的是做 v696/v699 对比，确认这是可控 tradeoff，而不是继续单边加权。

一句话总结：v699 证明 loss-first-token 修复能移动偏置，但当前版本过量，仍未达到 minimal-prompt pair-full。

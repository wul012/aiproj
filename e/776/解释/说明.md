# v776 运行截图和解释：receipt outputs split

## 本版目标

v776 继续拆 `promoted_training_scale_seed_handoff_receipt.py`。v773 已经把 schema validation 抽出，本版把两段长文本 renderer 抽到 outputs 模块。

## 改动结果

```text
src/minigpt/promoted_training_scale_seed_handoff_receipt.py: 774 -> 505 行
src/minigpt/promoted_training_scale_seed_handoff_receipt_outputs.py: 新增 289 行
src/minigpt/promoted_training_scale_seed_handoff_receipt_validation.py: 208 行
```

## 边界说明

- receipt 主文件保留 loader、check builder、sidecar verification 和 write outputs orchestration。
- outputs 模块负责：
  - `render_promoted_training_scale_seed_handoff_automation_receipt_check`
  - `render_promoted_training_scale_seed_handoff_embedded_receipt_check`
- 原公共导入路径仍由主文件 re-export，调用方不需要调整。

## 验证命令

```powershell
python -m py_compile src\minigpt\promoted_training_scale_seed_handoff_receipt.py src\minigpt\promoted_training_scale_seed_handoff_receipt_outputs.py src\minigpt\promoted_training_scale_seed_handoff_receipt_validation.py
python -m pytest tests\test_promoted_training_scale_seed_handoff_receipt.py tests\test_promoted_training_scale_seed_handoff_receipt_contract.py tests\test_promoted_training_scale_seed_handoff_receipt_contract_check.py tests\test_promoted_training_scale_seed_handoff_receipt_contract_check_failure_smoke.py tests\test_promoted_training_scale_seed_handoff_receipt_suite_design.py -q -o cache_dir=runs\pytest-cache-v776-focused
```

结果：

```text
50 passed
```

## 截图说明

- `e/776/图片/v776-receipt-outputs-split.png`

一句话总结：v776 把 receipt 的输出渲染层从主流程中拆出，主文件从 774 行降到 505 行，接近可维护阈值。

# v728 运行截图和解释：objective-structure corpus materialization

## 本版目标

v728 使用现有 corpus materializer 消费 v727 objective-structure contract。

目标是生成真实训练语料和 heldout eval fixture，为下一版训练准备数据产物。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_corpus_materialization.py e\727\解释\model-capability-required-term-pair-readiness-objective-structure-contract --out-dir e\728\解释\model-capability-required-term-pair-readiness-objective-structure-corpus-materialization --repeat 320 --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_corpus_materialized
failed_count=0
materialization_ready=True
training_line_count=5760
evaluation_probe_count=3
model_quality_claim=data_artifact_only
```

## 截图说明

- `e/728/图片/v728-objective-structure-corpus-materialization.png`

截图显示 materialization ready、5760 条训练行、3 个 eval probes，以及 heldout pair probe 未泄漏。

## 关键观察

- v727 的 18 条 objective-structure training rows 按 repeat=320 物化成 5760 行。
- `fixed=|loss=` 不在 training rows，也不在 corpus lines。
- heldout eval fixture 保留 fixed-direct、loss-direct 和 fixed-loss-pair 三个 probes。
- 本版仍是 data artifact，不声明模型质量。

一句话总结：v728 将 objective-structure contract 落成可训练 corpus，并保持 heldout 边界。

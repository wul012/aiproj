# v944 运行截图与解释

## 本版目标

v944 新增 `randomized_holdout_publication_registry_downstream_consumer_index_review`，对 v943 的 downstream consumer index 做只读审核。

这版解决的问题是：v943 已经把 packet 与 contract check 合成索引，但下游真正继续消费前，还需要一层 review 结论确认索引源文件仍存在、lookup 范围仍是 downstream governance lookup、blocked uses 完整、promotion 仍关闭。

## 输入与输出

- 输入：`e/943/解释/randomized-holdout-publication-registry-downstream-consumer-index/randomized_holdout_publication_registry_downstream_consumer_index.json`
- 输出目录：`e/944/解释/randomized-holdout-publication-registry-downstream-consumer-index-review`
- 截图目录：`e/944/图片`

生成命令：

```powershell
python scripts\build_randomized_holdout_publication_registry_downstream_consumer_index_review.py --consumer-index e\943\解释\randomized-holdout-publication-registry-downstream-consumer-index --out-dir e\944\解释\randomized-holdout-publication-registry-downstream-consumer-index-review --require-review-ready --require-downstream-ready --force
```

## 关键结果

- `status=pass`
- `decision=randomized_holdout_publication_registry_downstream_consumer_index_review_ready`
- `failed_count=0`
- `review_status=approved_for_downstream_consumer_lookup_only`
- `downstream_ready=True`
- `lookup_ready=True`
- `contract_check_ready=True`
- `promotion_ready=False`
- `allowed_use=downstream_governance_lookup_only`
- `next_step=record_randomized_holdout_publication_registry_downstream_consumer_ack`
- `passed_check_count=20`

## 截图说明

`e/944/图片/v944-randomized-holdout-publication-registry-downstream-consumer-index-review.png` 是 Playwright MCP 截图。页面顶部显示 `status=pass`、`review_ready=True`、`downstream=True`、`lookup=True`、`promotion=False`、`failed=0`。

`e/944/解释/v944-playwright-snapshot.md` 是浏览器快照，证明 HTML 报告能被浏览器读取。

## 边界说明

v944 只批准 downstream consumer lookup，不批准生产 promotion。它额外检查 v943 index 指向的 v941 packet 和 v942 packet check 文件仍存在，避免索引通过但源证据路径已经断裂。

一句话总结：v944 给 downstream consumer index 加上了审核口，把它从“可生成索引”推进到“可继续被下游确认接收”的阶段。

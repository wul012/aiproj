# v775 运行截图和解释：promoted comparison recommendations split

## 本版目标

v775 继续维护性拆分，不新增功能。目标是拆 `promoted_training_scale_comparison.py` 的 recommendation 文案层。

## 改动结果

```text
src/minigpt/promoted_training_scale_comparison.py: 777 -> 661 行
src/minigpt/promoted_training_scale_comparison_recommendations.py: 新增 145 行
```

## 边界说明

- comparison 主文件继续负责 promotion index loading、promotion rows、comparison input selection、summary、blockers 和 clean-batch guard。
- recommendations 模块负责 compared/blocked 两类 recommendation 文案。
- 构建报告时仍通过 `_recommendations(summary)` 调用，外部输出不变。

## 验证命令

```powershell
python -m py_compile src\minigpt\promoted_training_scale_comparison.py src\minigpt\promoted_training_scale_comparison_recommendations.py
python -m pytest tests\test_promoted_training_scale_comparison.py -q -o cache_dir=runs\pytest-cache-v775-focused
```

结果：

```text
13 passed
```

## 截图说明

- `e/775/图片/v775-promoted-comparison-recommendations-split.png`

一句话总结：v775 把 promoted comparison 的建议文案从核心 comparison builder 中拆出，让后续继续拆 guard/exclusion 更容易。

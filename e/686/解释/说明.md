# v686 运行截图和解释：required-term pair surface policy execution profile

## 本版目标

v686 把 v684 的风险边界和 v685 的最小稳定 budget 合并成一个可执行 profile。它不是新模型能力结论，而是给后续 surface variant replay 提供一个统一入口。

本版不重新生成模型输出，不训练 checkpoint，不改变 `pair_context_prefix` 的 contextual-anchor 风险判断。

## 输入证据

- `e/684/解释/model-capability-required-term-pair-surface-policy-leakage-risk/`
- `e/685/解释/model-capability-required-term-pair-surface-policy-budget-sweep/`

v684 证明 selected policy 只能作为 contextual decode aid，不能 promotion。v685 证明最小稳定 continuation budget 是 8。

## 实际运行

执行命令：

```powershell
python -B scripts\run_model_capability_required_term_pair_surface_policy_execution_profile.py e\684\解释\model-capability-required-term-pair-surface-policy-leakage-risk e\685\解释\model-capability-required-term-pair-surface-policy-budget-sweep --out-dir e\686\解释\model-capability-required-term-pair-surface-policy-execution-profile --require-pass --force
```

关键输出：

- `status=pass`
- `decision=required_term_pair_surface_policy_execution_profile_selected`
- `profile_id=pair_context_prefix_budget_8`
- `policy_id=pair_context_prefix`
- `max_new_tokens=8`
- `promotion_allowed=False`
- `model_quality_claim=contextual_decode_execution_profile`

## 截图说明

- `e/686/图片/v686-surface-policy-execution-profile.png`

截图展示了 profile id、budget 和 promotion 状态。重点是 `promotion_allowed=False` 和 `max_new_tokens=8` 同时存在：后续可以用这个 profile 跑 variant replay，但不能把它写成 baseline。

## 产物说明

正式产物目录：

```text
e/686/解释/model-capability-required-term-pair-surface-policy-execution-profile/
```

主要文件：

- `model_capability_required_term_pair_surface_policy_execution_profile.json`：机器可读 profile。
- `model_capability_required_term_pair_surface_policy_execution_profile.csv`：profile 单行摘要。
- `model_capability_required_term_pair_surface_policy_execution_profile.txt`：CLI 摘要。
- `model_capability_required_term_pair_surface_policy_execution_profile.md`：人工复核版。
- `model_capability_required_term_pair_surface_policy_execution_profile.html`：浏览器截图入口。

## 结论

v686 固化的执行配置是：

```text
profile_id=pair_context_prefix_budget_8
prompt_template={other_term}={other_term} {term}=
max_new_tokens=8
temperature=0.2
top_k=1
allowed_use=contextual_decode_surface_variant_replay
promotion_allowed=False
```

一句话总结：v686 把 surface policy 的风险和预算约束合成可执行 profile，为后续变体复测提供统一、不过度声明的入口。

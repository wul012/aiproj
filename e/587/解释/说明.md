# v587 required-term pair objective closeout 运行说明

## 本版目标

v587 是 v579-v586 的 objective closeout。它读取 v583 branch-binding route decision 和 v586 target-anchor route decision，把当前结论统一成一个下一步约束：不要继续当前 objective，下一版应设计 loss-branch objective。

本版不训练、不新增 corpus mode。

## 运行命令

```powershell
python scripts\run_model_capability_required_term_pair_objective_closeout.py `
  --branch-binding e\583\解释\model-capability-required-term-pair-branch-binding-route-decision `
  --target-anchor e\586\解释\model-capability-required-term-pair-target-anchor-route-decision `
  --out-dir e\587\解释\model-capability-required-term-pair-objective-closeout `
  --force `
  --require-pass
```

## 关键结果

```text
status=pass
decision=close_current_objectives_and_design_loss_branch_objective
branch_binding_stopped=True
target_anchor_residual_only=True
loss_branch_required=True
model_quality_claim=objective_closeout_only
```

## 截图说明

- `图片/v587-objective-closeout.png`
  - Playwright MCP 截取的 objective closeout HTML。
  - 证明 closeout decision 和 evidence rows 可见。

## 结论

v587 把这轮 objective 实验收口：branch-binding v1/v2 停止，target-anchor 只保留 residual evidence，下一步必须围绕 loss branch 做新 objective。

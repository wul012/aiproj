# v1011 运行截图与解释

## 本版目标

v1011 把 v1010 已审阅通过的 receipt index review 记录成下游 lookup-only receipt。v1010 的 `next_step` 字段仍指向 receipt recording；本版是在新的版本号里完成这个动作，保留原始 handoff 语义，同时把后续检查路由推进到 v1011 contract check。

本版不训练模型，不生成 checkpoint，不扩大模型能力声明，也不允许 production promotion。

## 关键输入

```text
e/1010/解释/review-v1010/randomized_holdout_publication_receipt_packet_index_publication_receipt_index_receipt_index_publication_index_receipt_index_receipt_index_receipt_index_receipt_index_review_v1010.json
```

## 运行命令

```text
python scripts\record_randomized_holdout_publication_receipt_packet_index_publication_receipt_index_receipt_index_publication_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_v1011.py e\1010\解释\review-v1010 --out-dir e\1011\解释\receipt-v1011 --require-receipt-ready --force
```

## 运行结果

```text
status=pass
decision=randomized_holdout_publication_receipt_packet_index_publication_receipt_index_receipt_index_publication_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_v1011_ready
failed_count=0
receipt_ready=True
receipt_status=publication_index_receipt_index_receipt_index_receipt_index_receipt_index_lookup_receipted
consumer_name=publication_index_receipt_index_receipt_index_receipt_index_receipt_index_governance_lookup_reader
granted_use=downstream_governance_lookup_only
receipt_index_row_count=1
source_evidence_count=2
lookup_key_count=1
promotion_ready=False
next_step=check_randomized_holdout_publication_receipt_packet_index_publication_receipt_index_receipt_index_publication_index_receipt_index_receipt_index_receipt_index_receipt_index_receipt_v1011
passed_check_count=21
failed_check_count=0
```

## 产物说明

`e/1011/解释/receipt-v1011/` 下的 JSON、CSV、TXT、Markdown、HTML 都是最终证据。JSON 是后续 v1012 contract check 的源输入；CSV 把 consumer receipt 行摊平；HTML 用于浏览器复核和截图。

## 截图证据

截图文件：

```text
e/1011/图片/v1011-receipt.png
```

## 测试与验证

focused 测试：

```text
5 passed in 9.42s
```

提交前还会执行 source encoding hygiene、全量 pytest、`py_compile` 和 `git diff --check`。

## 一句话总结

v1011 把 v1010 review 转成只读 receipt，给下一步 contract check 提供可复核的 JSON 入口，同时继续阻断 promotion。

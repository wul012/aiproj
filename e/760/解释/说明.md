# v760 运行截图和解释：objective-level contrast plan

## 本版目标

v760 消费 v759 selector，把选中的 `objective_level_contrast` 路线落成下一份 contract 的设计计划。

本版不训练、不物化 corpus、不增加 near-exact bridge rows。它只定义下一份 `pair_readiness_objective_level_contrast_contract` 应该包含哪些 row family、保留哪些 heldout 边界、通过哪些 acceptance checks。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_objective_level_contrast_plan.py e\759\解释\model-capability-required-term-pair-readiness-objective-or-decoding-alternative-selector --out-dir e\760\解释\model-capability-required-term-pair-readiness-objective-level-contrast-plan --require-pass --force
```

输出摘要：

```text
status=pass
decision=pair_readiness_objective_level_contrast_plan_ready
plan_ready=True
selected_route=objective_level_contrast
proposed_next_artifact=pair_readiness_objective_level_contrast_contract
contract_design_row_count=4
planned_training_row_count=26
model_quality_claim=plan_only
```

## 截图说明

- `e/760/图片/v760-objective-level-contrast-plan.png`

截图确认 HTML 页面可见：

- `Decision pair_readiness_objective_level_contrast_plan_ready`
- `Rows 26`
- `Next pair_readiness_objective_level_contrast_contract`
- exact/spaced/arrow 三类 heldout prompt surface 保持 eval-only

## 关键观察

- contract 计划不再做 near-exact surface patch，而是要求 row families 暴露 objective labels。
- 预计 26 行训练设计分成 4 个 row family：objective-header、branch-role-contrast、pair-answer-contrast、separator-neutral-answer。
- acceptance checks 明确要求 row count 平衡、heldout prompt 不进入训练、materializer 可直接消费。
- 下一版应把这个计划转成实际 contract JSON，并接入 corpus materialization 的已知 decision/filename 映射。

一句话总结：v760 把 objective-level contrast 变成可实现的 contract 计划，为下一版真正生成训练 contract 做准备。

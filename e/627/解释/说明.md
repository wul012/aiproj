# v627 required-term pair loss-internal fixed-bridge corpus contract 运行说明

## 本版目标

v627 把 v626 的 bridge gap 结论转成新的训练语料模式：`equals_surface_no_pair_id_loss_internal_fixed_bridge_repair`。

本版不训练 checkpoint；它只确认 fixed-bridge corpus 已注册，v628 再跑真实训练。

## 结果

```text
status=pass
decision=loss_internal_fixed_bridge_corpus_mode_ready
registered_mode_count=4
new_bridge_mode=equals_surface_no_pair_id_loss_internal_fixed_bridge_repair
source_bridge_gap=fixed
pair_id_removed=True
```

解释：

- 新模式把 `fixed=` 的生成桥接行加回语料。
- 同时保留 `loss=loss` 和 internal preference anchor。
- 仍然不引入 `pair=01`。

## 截图说明

- `图片/v627-loss-internal-fixed-bridge-corpus-contract.png`
  - Playwright MCP 截取的 corpus contract HTML。
  - 证明 fixed bridge mode 已就绪，真实 checkpoint evidence 从 v628 开始。

## 结论

v627 完成 fixed generation bridge corpus 的准备，下一版进入真实训练。

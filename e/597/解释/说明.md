# v597 required-term pair fixed-retention objective readiness 运行说明

## 本版目标

v597 合并 v594 route decision 和 v596 missed-seed diagnostic，生成 fixed-retention objective readiness gate。它明确下一轮不是继续加 loss 权重，而是设计 fixed first-token retention。

## 关键结果

```text
status=pass
decision=design_fixed_retention_objective_before_more_loss_branch_training
fixed_retention_objective_required=True
first_token_gap_confirmed=True
missed_seed_count=3
missed_first_token_gap_count=3
required_requirement_count=3
```

## 要求清单

```text
fixed_first_token_retention
loss_branch_no_extra_weight
pair_full_seed_gate
```

## 截图说明

- `图片/v597-fixed-retention-objective-readiness.png`
  - Playwright MCP 截取的 v597 readiness HTML。
  - 证明下一轮训练应转向 fixed-retention objective。

## 结论

v597 把本轮证据转为下一轮训练契约：必须增加 fixed first-token retention，不能继续单纯增加 loss 权重，并且新 objective 必须通过真实 seed gate。

# v677 运行截图与解释

## 本版目标

v677 对 v676 的三个 dual-boundary seed 同时运行 forced-choice 诊断，判断多 seed 下内部偏好是否也稳定。

本版不训练新模型，只读取 v676 每个 seed 的 refresh sidecar 和 checkpoint。

## 关键结果

- `status=pass`
- `decision=refresh_forced_choice_internal_pair_match`
- `expected_best_prompt_count=6`
- `forced_choice_full_match_source_count=3`
- `best_internal_sources=dual-boundary-seed-1535,dual-boundary-seed-2535,dual-boundary-seed-3535`
- `model_quality_claim=forced_choice_internal_signal_only`
- `next_action=compare internal preference with generation failures before more training`

## 解释

v676 显示 generation pair-full 是 2/3：seed 1535 和 3535 通过，seed 2535 未通过。

v677 显示 forced-choice internal pair match 是 3/3。这说明 seed 2535 的问题不是内部评分完全没学到 `fixed/loss`，而是自由生成表面表达没有稳定吐出完整 required-term pair。

因此下一步不该盲目加训练轮次，而应做一个收口判断：dual-boundary 是否已经把主要瓶颈从内部偏好转移到 generation surface stability。

## 截图说明

- `e/677/图片/v677-dual-boundary-multi-seed-forced-choice.png`: Playwright MCP 截取的 multi-seed forced-choice HTML 报告，显示 3 个 source 都内部 pair match。

一句话总结：v677 证明 dual-boundary 的内部偏好多 seed 稳定，但自由生成仍有 seed 级不稳定。

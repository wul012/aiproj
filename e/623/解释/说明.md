# v623 required-term pair loss-internal-preference objective comparison 运行说明

## 本版目标

v623 比较 v620-v622 三条真实 loss-internal-preference objective 训练结果。

本版不训练新 checkpoint；它把三条路线的生成结果归并成可决策的 comparison。

## 输入证据

```text
v620 loss-internal-preference seed 3535
v621 loss-internal first-token seed 3535
v622 loss-internal ranked-choice seed 3535
```

## 结果

```text
status=pass
decision=loss_internal_preference_objectives_confirm_branch_tradeoff
pair_full_report_count=0
fixed_only_tradeoff_report_count=2
loss_only_tradeoff_report_count=1
union_hit_terms=fixed,loss
```

解释：

- 没有路线达到 pair-full。
- v620、v622 是 fixed-only。
- v621 是 loss-only。
- 三条路线合起来证明 fixed/loss 都能被恢复，但不能同时稳定在一个 checkpoint。

## 截图说明

- `图片/v623-loss-internal-preference-objective-comparison.png`
  - Playwright MCP 截取的 comparison HTML。
  - 证明 decision、pair-full 计数、fixed-only/loss-only 计数可读。

## 结论

v623 证明本轮 objective 进入 branch tradeoff 状态，下一步应跑 forced-choice，判断生成失败背后是否已有内部偏好。

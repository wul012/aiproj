# v586 required-term pair target-anchor route decision 运行说明

## 本版目标

v585 确认 target-anchor 只恢复 fixed partial hit。v586 把这个结论固化成机器可读 route decision：保留 v584 为 residual evidence，但不推广、不继续无目的训练。

本版不训练、不改 corpus。它生成下一步路线约束。

## 运行命令

```powershell
python scripts\run_model_capability_required_term_pair_target_anchor_route_decision.py `
  e\585\解释\model-capability-required-term-pair-target-anchor-comparison `
  --out-dir e\586\解释\model-capability-required-term-pair-target-anchor-route-decision `
  --force `
  --require-pass
```

## 关键结果

```text
status=pass
decision=keep_target_anchor_as_residual_not_promoted
target_anchor_route_count=1
target_anchor_visible_hit_route_count=1
residual_signal_routes=v571-loss-balanced,v584-target-anchor
model_quality_claim=route_decision_only
```

## 截图说明

- `图片/v586-target-anchor-route-decision.png`
  - Playwright MCP 截取的 HTML decision 页面。
  - 证明 residual routes、decision 和 next action 可见。

## 结论

v586 把 v584 定位为 residual candidate，而不是 promotion candidate。下一步必须围绕 loss branch objective 做设计或收口。

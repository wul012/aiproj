# v710 运行截图和解释：pair-readiness loss-retention contract patch

## 本版目标

v710 把 v709 的 repair plan 落实为 contract patch。它读取 v705 base contract，新增 loss-retention rows，同时保持 heldout pair probe 不泄漏。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_loss_retention_contract_patch.py e\709\解释\model-capability-required-term-pair-readiness-loss-retention-repair-plan --base-contract e\705\解释\model-capability-required-term-pair-readiness-split-contract --out-dir e\710\解释\model-capability-required-term-pair-readiness-loss-retention-contract-patch --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_loss_retention_contract_patch_ready
patch_ready=True
base_training_row_count=12
patched_training_row_count=20
added_training_row_count=8
model_quality_claim=contract_patch_only
```

## 新增行

v710 新增 loss-retention rows：

```text
loss=l
loss=lo
loss=los
loss=loss
loss branch keeps l before fixed
loss branch resists fixed completion
loss prompt should not become fixed
loss direct retention beats fixed pollution
```

## 截图说明

- `e/710/图片/v710-pair-readiness-loss-retention-contract-patch.png`

截图展示 added rows、checks 和 next action。

## 结论

v710 将 v709 的计划变成可 materialize 的 patched contract。下一步可以直接用现有 corpus materialization 生成新训练语料。

一句话总结：v710 把 loss-retention 修复从计划推进到可检查 contract patch。

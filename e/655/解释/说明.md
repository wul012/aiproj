# v655 运行截图与解释

## 本版目标

v655 给 v651-v654 的 surface-first 负结果补一层失败分析，不再只写“失败”，而是把生成、内部评分、全矩阵和路线决策汇总成可复核结论。

## 关键结果

- `status=pass`
- `decision=surface_first_schedule_fixed_collapse_confirmed`
- `generation_hit_terms=fixed`
- `forced_choice_expected_terms=fixed`
- `alignment_class=fixed_only_aligned_partial`
- `fixed_collapse_confirmed=True`
- `next_objective=loss_guarded_surface_schedule_repair`

## 解释

分析显示 surface-first schedule 在两个视角都塌缩到 `fixed`：

- 自由生成只命中 `fixed`。
- forced-choice 内部偏好也只命中 `fixed`。
- 对比矩阵分类为 `fixed_only_aligned_partial`。
- 路线决策没有选它。

## 截图说明

- `e/655/图片/v655-surface-first-failure-analysis.png`：failure analysis HTML 截图，显示固定塌缩被确认，并给出下一步 loss-guarded schedule。

## 证据角色

本版是故障诊断和路线收束证据。它避免继续重复 surface-first 配置，也为 v656 的 loss-guarded corpus 提供直接理由。

一句话总结：v655 把 surface-first 失败从现象整理成原因链，下一步只值得尝试 loss-guarded 变体或停止该分支。

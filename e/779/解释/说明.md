# v779 maturity narrative release summary split

## 目标

v779 是拆分和维护优化版本，不新增功能链。目标是把 `maturity_narrative_summary.py` 中最长、最容易继续膨胀的 release readiness context 归一化逻辑拆出去。

## 关键变化

- 新增 `src/minigpt/maturity_narrative_release.py`。
- `src/minigpt/maturity_narrative_summary.py` 从 715 行降到 534 行。
- 新模块承接 `build_maturity_narrative_release_summary`，负责把 `release_readiness_context` 与 `maturity_summary` fallback 字段归一到同一 release summary shape。
- 主 summary 模块继续负责 portfolio summary、row extraction、status、recommendations 和 warnings。

## 验证

- `python -m py_compile src\minigpt\maturity_narrative_summary.py src\minigpt\maturity_narrative_release.py`
- `python -m pytest tests\test_maturity_narrative.py -q -o cache_dir=runs\pytest-cache-v779-focused`
- 结果：`20 passed`

## 截图

- `e/779/图片/v779-maturity-narrative-release-summary-split.png`

## 一句话总结

v779 把 maturity narrative 中的 release context 归一化长块拆出，让 portfolio narrative 主文件回到更清晰的汇总编排角色。

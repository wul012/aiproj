# v698 运行截图和解释：minimal prompt loss first-token repair plan

## 本版目标

v698 根据 v697 的 branch-bias 证据，新增并验证下一轮训练计划：用 `minimal_prompt_loss_first_token_repair_objective` 加强 loss 首 token 和 branch separation，而不是盲目增加训练轮数。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_minimal_prompt_loss_first_token_repair_plan.py e\697\解释\model-capability-required-term-pair-minimal-prompt-branch-bias-diagnostic --out-dir e\698\解释\model-capability-required-term-pair-minimal-prompt-loss-first-token-repair-plan --require-pass --force
```

结果：

```text
status=pass
decision=minimal_prompt_loss_first_token_repair_plan_ready
plan_ready=True
proposed_corpus_mode=minimal_prompt_loss_first_token_repair_objective
seed_to_rerun=3535
repair_focus=loss_first_token_and_branch_separation
model_quality_claim=repair_plan_only
```

focused tests：

```powershell
python -m pytest tests\test_model_capability_required_term_pair_minimal_prompt_loss_first_token_repair_plan.py tests\test_model_capability_required_term_pair_minimal_prompt_corpus_contract.py -q -o cache_dir=runs\pytest-cache-v698-focused
```

结果：

```text
9 passed
```

## 截图说明

- `e/698/图片/v698-minimal-prompt-loss-first-token-repair-plan.png`

截图展示 repair plan HTML 页面，能看到 proposed corpus mode、seed、repair focus、检查项和下一步训练命令方向。

## 结论

v698 新增的修复语料仍保持 minimal prompt 边界：

- `source_prompts` 仍是 `fixed=` / `loss=`。
- 不引入 contextual answer-bearing anchor。
- loss prefix 行和 loss direct target 权重强于 fixed。
- 下一版应 rerun seed `3535`。

一句话总结：v698 把 v697 的 fixed-dominant 诊断转换成可执行的 loss-first-token 修复计划。

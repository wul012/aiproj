# v632 required-term pair generation/internal alignment comparison 运行说明

## 本版目标

v632 把 v621、v628、v630 的 generation replay 和 forced-choice internal 结果放到同一张比较表里，避免只看单一指标就误判路线。

## 输入

```text
loss-internal-first-token: v621 refresh + v624 forced-choice
loss-internal-fixed-bridge: v628 refresh + v628 forced-choice
loss-internal-joint-cycle: v630 refresh + v631 forced-choice
```

## 比较结果

```text
status=pass
decision=keep_generation_pair_full_and_repair_internal_preference
generation_pair_full_count=1
internal_pair_full_count=1
aligned_pair_full_count=0
best_source_labels=loss-internal-first-token,loss-internal-joint-cycle
```

解释：

- v621：internal full，但 generation 只保 `loss`。
- v628：generation/internal 都偏 fixed。
- v630：generation full，但 internal 只保 `fixed`。

因此目前没有真正 aligned pair-full。

## 截图说明

- `图片/v632-generation-internal-alignment-comparison.png`
  - 比较 HTML 截图，证明 `Generation full=1`、`Internal full=1`、`Aligned full=0`。

## 结论

v632 给出下一步路线：优先以 v630 为主，因为它已经生成 pair-full；后续目标是在不破坏 generation pair-full 的前提下修 internal preference。

# v596 required-term pair loss-branch targeted missed-seed diagnostic 运行说明

## 本版目标

v596 对 v595 的 3 个 missed seed 做 first-token/logit 诊断，确认 targeted loss branch 为什么不能 pair-full。

## 关键结果

```text
status=pass
decision=required_term_pair_colon_immediate_first_token_gap
seed_count=3
missed_seed_count=3
missed_expected_top_count=0
missed_first_token_gap_count=3
```

CSV 关键行：

```text
3535 fixed_rank=3 loss_rank=2 top_token=space
4535 fixed_rank=3 loss_rank=1 top_token=l
5535 fixed_rank=2 loss_rank=1 top_token=l
```

## 截图说明

- `图片/v596-loss-branch-targeted-missed-seed-diagnostic.png`
  - Playwright MCP 截取的 v596 diagnostic HTML。
  - 证明 3 个 seed 都是 first-token gap，而不是训练失败。

## 结论

v596 把问题从“loss branch 还不够强”改写为“fixed first-token retention 不够强”。下一步应做 fixed-retention objective，而不是继续堆 loss 权重。

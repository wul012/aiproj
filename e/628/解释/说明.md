# v628 required-term pair loss-internal fixed-bridge seed 3535 运行说明

## 本版目标

v628 运行 v627 的 fixed-bridge corpus mode，验证它是否能把 v626 的 `fixed` generation gap 补上，同时保留 `loss`。

本版是真实 tiny 训练，并额外运行同版 forced-choice sidecar。

## 训练配置

```text
seed=3535
corpus_mode=equals_surface_no_pair_id_loss_internal_fixed_bridge_repair
repeat=320
bridge_repeat=24
max_iters=1800
block_size=16
n_embd=64
learning_rate=0.02
device=cpu
```

## 生成结果

```text
status=pass
decision=required_term_pair_coexistence_refresh_no_pair_full
training_status=pass
checkpoint_exists=True
pair_full_observed=False
```

replay 观察：

```text
fixed= -> fixed= fixe
loss=  -> fixed= fixe
```

解释：fixed bridge 的确恢复了 `fixed=`，但又把 `loss=` 拉回 fixed，仍然没有 pair-full。

## Forced-choice sidecar

```text
decision=refresh_forced_choice_partial_internal_match
expected_best_prompt_count=1
forced_choice_full_match_source_count=0
```

解释：内部偏好也从 v621 的 full match 退回 partial match，只保住了 fixed。

## 截图说明

- `图片/v628-loss-internal-fixed-bridge-seed-3535.png`
  - 训练/replay HTML 截图，证明 training pass 和 pair-full 为 `False`。
- `图片/v628-loss-internal-fixed-bridge-forced-choice.png`
  - forced-choice HTML 截图，证明 internal full match 降为 `0`。

## 结论

v628 证明简单 fixed bridge 会修复 fixed 但破坏 loss；下一批应该做联合约束，而不是单边桥接。

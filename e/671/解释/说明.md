# v671 运行截图与解释

## 本版目标

v671 按 v670 plan 注册 explicit dual-objective boundary corpus mode。它解决的问题是：把 fixed miss、loss retention、generation anchor、internal anchor 和 stop-naive-resume 边界写进真实训练语料构造器。

本版不训练模型；它是 corpus contract 版本。

## 新增 corpus mode

```text
equals_surface_no_pair_id_loss_internal_explicit_dual_boundary_repair
```

关键语料约束：

- `dual boundary surface fixed=fixed loss=loss`
- `dual boundary surface loss=loss fixed=fixed`
- `fixed retention after constrained miss fixed=fixed`
- `loss retention after constrained hit loss=loss`
- `do not accept fixed prefix without full fixed`
- `do not erase loss while repairing fixed`
- `the boundary rejects naive checkpoint continuation variants.`

## 验证

- `python -m pytest tests\test_model_capability_required_term_pair_coexistence_refresh.py -q -o cache_dir=runs\pytest-cache-v671`
- 结果：`25 passed`

## 截图说明

- `e/671/图片/v671-dual-boundary-corpus-contract.png`: Playwright MCP 截取的 corpus contract HTML。

一句话总结：v671 把 v670 的计划真正接入训练语料系统，为下一版真实训练做准备。

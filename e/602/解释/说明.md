# v602 required-term pair fixed-retention prompt-guard seed 3535 运行说明

## 本版目标

v602 使用 `equals_surface_no_pair_id_fixed_retention_prompt_guard_repair` 做真实 tiny training。目标是验证 prompt guard 是否能保留 v601 的 fixed improvement，同时避免覆盖 `loss=`。

## 关键结果

```text
status=pass
decision=required_term_pair_coexistence_refresh_no_pair_full
training_status=pass
checkpoint_exists=True
pair_full_observed=False
```

## 观察

prompt-guard route 回到 loss-only：

```text
fixed= -> losssssssss
loss= -> losssssssss
```

它没有保留 v601 的 fixed recovery，因此不是 promotion route。

## 截图说明

- `图片/v602-fixed-retention-prompt-guard-seed-3535.png`
  - Playwright MCP 截取的 v602 coexistence refresh HTML。
  - 证明 prompt-guard route 完成真实训练，但仍未达到 pair-full。

## 结论

v602 说明 prompt guard 不足以解决 fixed retention。v600-v602 三条 fixed-retention route 需要进入比较和 route decision。

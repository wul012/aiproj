# v747 运行截图和解释：fixed-preserving transfer contract patch

## 本版目标

v747 基于 v746 fixed-preserving transfer plan 和 v736 direct-completion surface contract，生成更轻量的 contract patch。

本版不训练模型；它只把 v746 的计划落成可检查 contract，并把新 decision 注册进 materializer。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_fixed_preserving_transfer_contract_patch.py --transfer-plan e\746\解释\model-capability-required-term-pair-readiness-fixed-preserving-transfer-plan --base-contract e\736\解释\model-capability-required-term-pair-readiness-direct-completion-surface-contract --out-dir e\747\解释\model-capability-required-term-pair-readiness-fixed-preserving-transfer-contract-patch --require-pass --force
```

输出摘要：

```text
status=pass
decision=pair_readiness_fixed_preserving_transfer_contract_patch_ready
failed_count=0
patch_ready=True
base_training_row_count=16
patched_training_row_count=20
fixed_preserving_transfer_row_count=4
model_quality_claim=contract_patch_only
```

## 截图说明

- `e/747/图片/v747-fixed-preserving-transfer-contract-patch.png`

截图显示 base rows 为 16、patched rows 为 20、transfer rows 为 4，并列出新增 fixed-preserving rows。

## 关键观察

- patch 保留 `fixed=fixed` 和 `loss=loss` exact direct-completion rows。
- patch 只新增 4 条 fixed-preserving transfer rows。
- patch 未复用 v742 的 broad `pair_transfer` rows。
- exact heldout pair prompt `fixed=|loss=` 仍不在训练 rows 中。
- materializer 已接入 `pair_readiness_fixed_preserving_transfer_contract_patch_ready`。

一句话总结：v747 将 fixed-preserving 计划落成 materializer-ready contract patch，但模型能力仍需 v748-v749 物化和训练验证。

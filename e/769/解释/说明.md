# v769 运行截图和解释：objective-level contrast seed 3737 pair-probe replay

## 本版目标

v769 消费 v768 seed `3737` training checkpoint，运行 heldout pair-probe replay。

本版不重新训练，不调整解码参数，也不接受 checkpoint。它只回答一个问题：seed `3737` 的 direct pair-full 是否能在独立 replay artifact 中复核。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_fixed_preserving_transfer_pair_probe_replay.py e\768\解释\model-capability-required-term-pair-readiness-objective-level-contrast-seed-3737-training-run --out-dir e\769\解释\model-capability-required-term-pair-readiness-objective-level-contrast-seed-3737-pair-probe-replay --require-pass --force
```

输出摘要：

```text
status=pass
decision=pair_readiness_fixed_preserving_transfer_pair_probe_replay_ready
failed_count=0
exact_heldout_pair_full=True
required_all_pair_full=True
pair_full_count=2
model_quality_claim=pair_probe_replay_ready
next_action=compare against prior routes and run stricter promotion guards before accepting the checkpoint
```

## 截图说明

- `e/769/图片/v769-objective-level-contrast-seed-3737-pair-probe-replay.png`

截图确认 HTML 页面可见：

- `Status pass`
- `Decision pair_readiness_fixed_preserving_transfer_pair_probe_replay_ready`
- `Exact pair True`
- `Required all True`
- `Pair-full count 2`

## 关键观察

- seed `3737` replay-ready，说明 objective-level contrast 信号不是 seed `3636` 的一次性训练内现象。
- `pair_full_count=2` 低于 v764 seed `3636` 的 `pair_full_count=3`，所以稳定性 rollup 应记录强度差异。
- 本版仍不是 acceptance。还需要 seed `3838` 训练、replay，再做 seed stability rollup。

一句话总结：v769 把 seed `3737` 从 direct training hit 推进为 replay-ready 证据，但保留强度差异等待 rollup。

# v704 运行截图和解释：pair-readiness split plan

## 本版目标

v704 承接 v703 closeout，不再继续同族 minimal prompt 训练，而是把下一步改成 pair-readiness train/eval split plan。

本版只规划训练前契约，不训练模型，也不宣称能力提升。

## 输入证据

输入来自：

```text
e/703/解释/model-capability-required-term-pair-minimal-prompt-batch-closeout/
```

v703 已确认：

```text
decision=minimal_prompt_batch_closed_without_pair_full
report_count=3
pair_full_report_count=0
fixed_only_report_count=1
loss_only_report_count=2
```

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_split_plan.py e\703\解释\model-capability-required-term-pair-minimal-prompt-batch-closeout --out-dir e\704\解释\model-capability-required-term-pair-readiness-split-plan --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_split_plan_ready
plan_ready=True
proposed_next_artifact=pair_readiness_split_contract
training_split_count=3
evaluation_split_count=3
model_quality_claim=plan_only
```

## 截图说明

- `e/704/图片/v704-pair-readiness-split-plan.png`

截图展示 pair-readiness split plan HTML，能看到 training split、evaluation split 和 promotion guard。

## 结论

v704 把下一步从“继续盲训”改成“先定义训练/评估分离”：

- training split：direct branch completion、anti-contamination、balanced prefix progression。
- evaluation split：`fixed=`、`loss=`、heldout pair probe。
- promotion guard：只有 heldout fixed 和 loss continuation 都命中，才能 claim pair capability。

一句话总结：v704 把 minimal prompt 失败收束成 pair-readiness split plan，为下一步 contract 化训练前检查做准备。

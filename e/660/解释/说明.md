# v660 运行截图与解释

## 本版目标

v660 从 v630 的 generation pair-full checkpoint 做真实续训，使用 `equals_surface_no_pair_id_loss_internal_joint_cycle_internal_repair` 语料尝试修内部偏好。

## 关键结果

- `status=pass`
- `training_status=pass`
- `training_mode=checkpoint_continuation`
- `resume_checkpoint_exists=True`
- `checkpoint_exists=True`
- `decision=required_term_pair_coexistence_refresh_no_pair_full`
- `pair_full_observed=False`

## Replay 观察

- `fixed=` 未命中，输出偏向 `loss`。
- `loss=` 命中，输出 `loss`。

这说明真实续训机制可用，但这组 internal-repair continuation 把 v630 的 pair-full 推向 loss-only，没保住 fixed。

## 截图说明

- `e/660/图片/v660-v630-to-internal-repair-resume.png`：refresh HTML 截图，显示 training mode 为 continuation、checkpoint 存在、pair-full 为 0。

## 证据角色

本版是第一条真实 checkpoint continuation 负证据。它比 v649-v658 的单语料近似更重要，因为它确认“从 v630 续训”可以运行，但 naive internal-repair continuation 会破坏生成 pair-full。

一句话总结：v660 把项目推进到真实续训实验层，但第一条 v630->internal-repair continuation 退化为 loss-only。

# v652 运行截图与解释

## 本版目标

v652 对 v651 的 surface-first schedule checkpoint 做 forced-choice 诊断，判断内部打分是否比自由生成更好。

## 关键结果

- `status=pass`
- `decision=refresh_forced_choice_partial_internal_match`
- `expected_best_prompt_count=1`
- `forced_choice_full_match_source_count=0`
- `best_internal_sources=[]`

## Prompt 观察

- `fixed` prompt：
  - `best_candidate=fixed`
  - `expected_best=True`
  - `expected_first_token_rank=1`
- `loss` prompt：
  - `best_candidate=fixed`
  - `expected_candidate=loss`
  - `expected_best=False`
  - `expected_first_token_rank=2`

这说明 v651 不只是自由生成偏向 `fixed`，内部 forced-choice 也没有保住 `loss` 偏好。

## 截图说明

- `e/652/图片/v652-surface-first-schedule-forced-choice.png`：forced-choice HTML 截图，显示 partial internal match、Expected-best 为 1、Full match 为 0。

## 证据角色

v652 把 v651 的负结果从生成侧扩展到内部评分侧：surface-first schedule approximation 是 fixed-only 倾向，不适合作为后续 pair-full 候选。

一句话总结：v652 证明 surface-first schedule checkpoint 的内部偏好同样没有通过，后续应转向对比收口或更强的阶段隔离设计。

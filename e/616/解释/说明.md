# v616 required-term pair refresh forced-choice diagnostic contract 运行说明

## 本版目标

v616 新增 refresh forced-choice diagnostic。它读取 coexistence refresh 报告中的 checkpoint/tokenizer，对 `fixed=` 和 `loss=` 分别 teacher-forced 打分 `fixed` / `loss` 候选。

本版只新增诊断能力，不跑真实 checkpoint；v617 会执行真实诊断。

## 验证

```text
python -m pytest tests\test_model_capability_required_term_pair_refresh_forced_choice_diagnostic.py -q -o cache_dir=runs\pytest-cache-v616-targeted
5 passed
```

## 截图说明

- `图片/v616-refresh-forced-choice-diagnostic-contract.png`
  - Playwright MCP 截取的 v616 contract HTML。
  - 证明 forced-choice diagnostic 的输入、候选项和测试状态已经归档。

## 结论

v616 把 v615 的下一步落实为可运行诊断入口：先看 checkpoint 内部偏好，再设计新语料。

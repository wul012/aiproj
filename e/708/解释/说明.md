# v708 运行截图和解释：pair-readiness heldout failure diagnostic

## 本版目标

v708 读取 v707 的 heldout direct replay，诊断为什么 `pair_readiness_training_no_pair_full`。

本版只做失败归因，不改 corpus、不训练模型。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_heldout_failure_diagnostic.py e\707\解释\model-capability-required-term-pair-readiness-training-run --out-dir e\708\解释\model-capability-required-term-pair-readiness-heldout-failure-diagnostic --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_loss_prompt_absorbed_by_fixed
dominant_failure=loss_prompt_absorbed_by_fixed
fixed_hit_count=1
loss_hit_count=0
loss_prompt_fixed_pollution_count=1
model_quality_claim=diagnostic_only
```

## 截图说明

- `e/708/图片/v708-pair-readiness-heldout-failure-diagnostic.png`

截图展示 dominant failure、分析表和 next action。

## 结论

v708 把 v707 的 no pair-full 明确归因：

- `fixed=` 命中 fixed。
- `loss=` 没命中 loss。
- `loss=` 的 continuation 出现 fixed。
- 当前失败不是“两个都不会”，而是 loss prompt 被 fixed 分支吸附。

一句话总结：v708 把 v707 的失败从 no pair-full 细化为 loss-prompt fixed-pollution，为下一步 loss-retention repair plan 提供依据。

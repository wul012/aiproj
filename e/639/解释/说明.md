# v639 required-term pair joint-cycle internal-repair corpus 运行说明

## 本版目标

v639 接在 v638 closeout 之后，新增 `joint-cycle internal-repair` corpus mode。目标是保留 v630 的 generation pair-full 路线，同时用 teacher-forced/internal ranking 行修复 v631 暴露的 `loss=` internal gap。

本版只做 corpus contract，不做真实训练；真实 seed 运行从 v640 开始。

## 新增模式

```text
equals_surface_no_pair_id_loss_internal_joint_cycle_internal_repair
```

## 合约结果

```text
status=pass
decision=joint_cycle_internal_repair_corpus_ready
registered_mode_count=7
pytest=8 passed in 0.11s
```

解释：新模式保留 `generation fixed= fixed` 和 `generation loss= loss`，同时增加 `teacher forced loss= loss`、`internal loss= candidate loss rank 1` 等内部修复锚点。

## 截图说明

- `图片/v639-joint-cycle-internal-repair-corpus-contract.png`
  - 合约 HTML 截图，证明 mode 已注册、边界仍是 corpus contract。

## 结论

v639 把 v638 的路线决策落成训练输入；后续要用真实 seed 验证它是否能同时保住 generation pair-full 和 internal preference。

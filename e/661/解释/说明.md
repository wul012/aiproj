# v661 运行截图与解释

## 本版目标

v661 对 v660 的真实续训 checkpoint 做 forced-choice 诊断，确认它是否在内部偏好上修好了 `loss`。

## 关键结果

- `status=pass`
- `decision=refresh_forced_choice_partial_internal_match`
- `expected_best_prompt_count=1`
- `forced_choice_full_match_source_count=0`
- `fixed` prompt 命中 expected best
- `loss` prompt 仍选择 `fixed`

## 解释

v660 的自由生成已经退成 loss-only，但 v661 显示内部评分并没有形成 `loss` pair-full：`loss=` 的 expected candidate 仍不是 best。也就是说，这条续训既没有保住生成 pair-full，也没有达成内部 pair-full。

## 截图说明

- `e/661/图片/v661-v630-internal-repair-resume-forced-choice.png`：forced-choice HTML 截图，显示 Expected-best=1、Full match=0。

一句话总结：v661 证明 v630->internal-repair continuation 不是有效候选，后续需要更温和的 continuation 或停止该续训配置。

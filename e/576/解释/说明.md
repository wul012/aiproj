# v576 required-term pair route fresh seed 3535 variable comparison 运行说明

## 本版目标

v571、v573、v575 分别测试了 seed `3535` 的三种变量：原始 loss-balanced、已有 first-token repair、wider embedding。v576 把三份真实训练报告放到同一个 comparison 中，形成 seed `3535` 的变量对比结论。

本版不重新训练、不新增代码。它是 v571-v575 的对比收口。

## 运行命令

```powershell
python scripts\run_model_capability_required_term_pair_equals_surface_repair_comparison.py `
  e\571\解释\model-capability-required-term-pair-route-fresh-seed-3535 `
  e\573\解释\model-capability-required-term-pair-route-fresh-seed-3535-first-token-repair `
  e\575\解释\model-capability-required-term-pair-route-fresh-seed-3535-wider-embd `
  --labels v571-loss-balanced v573-first-token v575-wider-embd `
  --out-dir e\576\解释\model-capability-required-term-pair-route-fresh-seed-3535-variable-comparison `
  --force `
  --require-pass
```

## 关键结果

```text
status=pass
decision=required_term_pair_equals_surface_repair_comparison_recorded
compared_report_count=3
term_case_row_count=12
branch_competition_seed_count=0
pair_full_profile_seed_count=0
union_hit_terms=fixed
model_quality_claim=comparison_only
```

三路变量没有任何 pair-full：

```text
v571-loss-balanced -> fixed partial only
v573-first-token   -> no visible hit
v575-wider-embd    -> no visible hit
```

## 截图说明

- `图片/v576-route-fresh-seed-3535-variable-comparison.png`
  - Playwright MCP 截取的 HTML 报告。
  - 证明三份 source report、summary 和 term evidence 可见。

## 归档文件

- `解释/model-capability-required-term-pair-route-fresh-seed-3535-variable-comparison/model_capability_required_term_pair_equals_surface_repair_comparison.json`
  - 主 comparison 报告。
- `解释/model-capability-required-term-pair-route-fresh-seed-3535-variable-comparison/model_capability_required_term_pair_equals_surface_repair_comparison.csv`
  - seed-level branch rows。
- `解释/model-capability-required-term-pair-route-fresh-seed-3535-variable-comparison/playwright-snapshot.md`
  - 页面快照旁证。

## 结论

v576 将 seed `3535` 的变量实验收口：first-token rows 和 width scaling 都没有修复 fresh seed，且都比 v571 的 partial fixed signal 更差。下一步要么设计新的 objective，要么先生成路线决策，停止在这三个变量上继续消耗版本。

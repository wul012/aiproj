# v700 运行截图和解释：minimal prompt tradeoff comparison

## 本版目标

v700 不新增代码，复用已有 `first_token_preference_diagnostic`，把 v696 和 v699 两次真实训练放在同一个对比报告里，确认它们是不是 fixed-only / loss-only 的 tradeoff。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_first_token_preference_diagnostic.py --report e\696\解释\model-capability-required-term-pair-minimal-prompt-training --label v696-fixed-dominant --report e\699\解释\model-capability-required-term-pair-minimal-prompt-loss-first-token-training --label v699-loss-dominant --out-dir e\700\解释\model-capability-required-term-pair-minimal-prompt-tradeoff-comparison --require-pass --force
```

结果：

```text
status=pass
decision=first_token_preference_tradeoff_confirmed
source_report_count=2
first_token_conflict_confirmed=True
mixed_branch_tradeoff_confirmed=True
other_term_start_count=4
model_quality_claim=diagnostic_only
```

## 截图说明

- `e/700/图片/v700-minimal-prompt-tradeoff-comparison.png`

截图展示 first-token preference diagnostic 页面，列出 v696/v699 两个 source 的 prompt rows。

## 结论

v700 确认：

- v696 是 fixed-dominant，loss prompt 以 fixed 开头。
- v699 是 loss-dominant，fixed prompt 以 loss 开头。
- 两者都不是 pair-full。
- 下一步应设计 balanced / contrast-free objective，而不是继续单边增强。

一句话总结：v700 把 v696/v699 的两次真实训练归纳为 first-token preference tradeoff，给 balanced repair 提供依据。

# v584 required-term pair target-anchor seed 3535 运行说明

## 本版目标

v583 停止了 branch-binding v1/v2，要求后续必须是更强 objective。v584 新增 target-anchor corpus：直接重复 `fixed=fixed` 和 `loss=loss` 等 exact equals continuations，减少自然语言解释句对生成路径的干扰。

本版是真实训练，但仍只跑 seed `3535`，不做泛化宣称。

## 运行命令

```powershell
python scripts\run_model_capability_required_term_pair_colon_immediate_stability.py `
  --out-dir e\584\解释\model-capability-required-term-pair-target-anchor-seed-3535 `
  --seeds 3535 `
  --corpus-mode equals_surface_no_pair_id_target_anchor_repair `
  --repeat 260 `
  --bridge-repeat 20 `
  --max-iters 1400 `
  --eval-iters 2 `
  --batch-size 16 `
  --block-size 16 `
  --n-layer 1 `
  --n-head 1 `
  --n-embd 64 `
  --learning-rate 0.02 `
  --max-new-tokens 12 `
  --temperature 0.8 `
  --top-k 2 `
  --device cpu `
  --force `
  --require-pass
```

## 关键结果

```text
status=pass
decision=required_term_pair_colon_immediate_not_stable
pair_full_seed_count=0
continuation_hit_count=1
```

v584 没有 pair-full，但恢复了 partial `fixed` hit；`loss=` 仍然生成 fixed 方向。

## 截图说明

- `图片/v584-target-anchor-seed-3535.png`
  - Playwright MCP 截取的 HTML stability 报告。
  - 证明 target-anchor 训练报告和 partial hit 结果可见。

## 结论

v584 比 v579/v581 更接近 v571 的 residual baseline，但仍不是成功路线。下一步应把 v584 加入 comparison，确认它是否只是回到 v571 水平。

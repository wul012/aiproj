# v791 运行截图和解释：model capability route promotion history

## 本版目标

v791 是功能版本，接在 v790 objective-level contrast promotion manifest 后面。v790 把 accepted route 变成单个 benchmark-history-ready promotion manifest；v791 再把它写入 route promotion history ledger，让模型能力阶段拥有可追加、可比较、可被后续 portfolio 或 regression review 消费的历史入口。

本版明确不做：

- 不重新训练。
- 不重新 replay。
- 不合并旧 scorecard benchmark history。
- 不把 tiny pair-probe promotion history 解释为生产模型质量。

## 运行命令和结果

```powershell
python -B scripts\build_model_capability_route_promotion_history.py e\790\解释\model-capability-required-term-pair-readiness-objective-level-contrast-promotion-manifest --names "objective-level contrast v790" --out-dir e\791\解释\model-capability-route-promotion-history --require-ready-history --force
```

输出摘要：

```text
status=pass
decision=model_capability_route_promotion_history_ready
entry_count=1
ready_count=1
blocked_count=0
boundary_mismatch_count=0
model_quality_claim=seed_stable_pair_probe_route_accepted
readiness_requirement_status=pass
readiness_requirement_failed_reasons=
```

## 关键观察

- ledger 当前包含一个 route promotion entry，来自 v790 promotion manifest。
- `ready_count=1`，说明这条 promotion manifest 可以被后续模型能力历史消费。
- `boundary_mismatch_count=0`，说明 tiny pair-probe 边界没有被篡改或放大。
- `model_quality_claim=seed_stable_pair_probe_route_accepted`，仍然保持 v789/v790 的受限声明。
- readiness requirement 为 pass，后续版本可以基于它做 portfolio summary 或 regression monitor。

## 截图说明

- `e/791/图片/v791-model-capability-route-promotion-history.png`

截图确认 HTML 页面可见：

- `Status pass`
- `Entries 1`
- `Ready 1`
- `Boundary mismatches 0`
- `Readiness pass`
- ledger 表格和 recommendations

一句话总结：v791 把单个 objective-level contrast promotion manifest 写成 route promotion history ledger，让模型能力阶段从单点接受证据推进到可持续追踪的路线履历。

# v730 运行截图和解释：surface mismatch diagnostic

## 本版目标

v730 同时读取 v727 objective-structure contract 和 v729 training run，诊断 direct probes 全部 miss 的原因。

目标是判断问题是否来自 raw direct prompt surface 与训练 row surface 不一致。结果是：检测到 direct surface mismatch。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_surface_mismatch_diagnostic.py --contract e\727\解释\model-capability-required-term-pair-readiness-objective-structure-contract --training-run e\729\解释\model-capability-required-term-pair-readiness-objective-structure-training-run --out-dir e\730\解释\model-capability-required-term-pair-readiness-surface-mismatch-diagnostic --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_direct_surface_mismatch_detected
surface_mismatch_detected=True
default_hit_terms=[]
default_missed_terms=['fixed', 'loss']
raw_surface_reference_count=0
recommended_next_artifact=pair_readiness_direct_prompt_bridge_contract_patch
model_quality_claim=diagnostic_only
```

## 截图说明

- `e/730/图片/v730-surface-mismatch-diagnostic.png`

截图显示 raw surface refs 为 0，`fixed` 和 `loss` 全部 miss，下一步是 direct prompt bridge contract patch。

## 关键观察

- v727 contract 没有训练 exact `fixed=` 或 `loss=` direct rows。
- v729 replay 中 `fixed=` 和 `loss=` 都生成 non-term surface。
- 这不是 fixed 吸收 loss，也不是 loss 吸收 fixed。
- 下一步应增加 direct prompt bridge rows，但仍不能加入 heldout pair probe。

一句话总结：v730 把 v729 的双 miss 归因为 direct surface mismatch，并给出 bridge patch 方向。

# v619 required-term pair loss-internal-preference corpus contract 运行说明

## 本版目标

v619 接续 v618 的 closeout 结论：contrast-free 变体已经不值得继续堆，本版改为定义 loss-internal-preference objective 语料。

本版不训练 checkpoint；它只确认三种新语料模式已经注册到现有 `coexistence_refresh` 入口。

## 新增模式

```text
equals_surface_no_pair_id_loss_internal_preference_repair
equals_surface_no_pair_id_loss_internal_first_token_repair
equals_surface_no_pair_id_loss_internal_ranked_choice_repair
```

## 结果

```text
status=pass
decision=loss_internal_preference_objective_corpus_modes_ready
new_mode_count=3
source_prompts=fixed= / loss=
```

解释：

- internal preference 模式把 forced-choice 的“候选偏好”写回训练文本。
- first-token 模式优先训练 `loss=` 后的 `l`。
- ranked-choice 模式把 v617 的 teacher-forced 诊断形式变成训练语料。

## 截图说明

- `图片/v619-loss-internal-preference-corpus-contract.png`
  - Playwright MCP 截取的 contract HTML。
  - 证明三种模式注册完成，并明确本版只是 corpus contract。

## 结论

v619 把下一批真实训练入口准备好，v620-v622 可以分别训练三种 loss-internal-preference objective。

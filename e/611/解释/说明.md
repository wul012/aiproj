# v611 required-term pair contrast-free seed 3535 运行说明

## 本版目标

v611 用 v610 的 `equals_surface_no_pair_id_fixed_retention_contrast_free_repair` 跑真实 seed `3535`。目标是验证“分离 branch rows、不直接提对侧 term”的语料是否能减少 v609 发现的 first-token 冲突。

本版只记录真实 tiny 训练结果，不宣称模型能力提升。

## 训练配置

```text
seed=3535
corpus_mode=equals_surface_no_pair_id_fixed_retention_contrast_free_repair
repeat=260
bridge_repeat=20
max_iters=1800
batch_size=16
block_size=16
n_layer=1
n_head=1
n_embd=64
learning_rate=0.02
temperature=0.2
top_k=1
device=cpu
```

## 结果

```text
status=pass
decision=required_term_pair_coexistence_refresh_no_pair_full
training_status=pass
checkpoint_exists=True
pair_full_observed=False
```

Replay 结果：

```text
fixed= -> lossssssss   miss fixed
loss=  -> fixed=fixed= miss loss
```

这说明 contrast-free rows 仍然产生 cross-branch 起始，而且两个 prompt 都没有命中目标 term。

## 截图说明

- `图片/v611-contrast-free-seed-3535.png`
  - Playwright MCP 截取的 v611 HTML 报告。
  - 证明训练流程通过、checkpoint 存在，但 pair-full 仍为 `False`。

## 结论

v611 是负结果：contrast-free row separation 没有解决 first-token 冲突，下一步继续验证 delimiter-span 路线。

# v615 required-term pair contrast-free route decision 运行说明

## 本版目标

v615 读取 v608 closeout、v609 first-token diagnostic 和 v614 comparison，做带历史约束的 route decision。目标是避免 v614 默认建议把项目再次带回已经失败的 loss-rebalance 分支。

本版不训练新 checkpoint，只做决策归档。

## 输入证据

```text
v608 fixed-retention batch closeout
v609 first-token preference diagnostic
v614 contrast-free objective comparison
```

## 结果

```text
status=pass
decision=stop_contrast_free_routes_and_run_forced_choice_diagnostic
pair_full_route_count=0
fixed_only_route_count=2
prior_loss_rebalance_stopped=True
requires_forced_choice_diagnostic=True
model_quality_claim=route_decision_only
```

解释：

- v614 没有 pair-full。
- v608 已经停止当前 loss-rebalance 分支。
- v609 确认 first-token conflict。
- 因此下一步不是继续训练相似语料，而是先做 teacher-forced / forced-choice 诊断。

## 截图说明

- `图片/v615-contrast-free-route-decision.png`
  - Playwright MCP 截取的 v615 HTML 报告。
  - 证明决策为 `stop_contrast_free_routes_and_run_forced_choice_diagnostic`。

## 结论

v615 把 v611-v614 的训练结果转成路线刹车：先诊断 checkpoint 内部偏好，再设计下一批语料。

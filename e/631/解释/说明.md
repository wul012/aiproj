# v631 required-term pair loss-internal joint-cycle forced-choice 运行说明

## 本版目标

v631 对 v630 的 pair-full checkpoint 跑 teacher-forced forced-choice 诊断，验证生成命中是否也对应内部偏好稳定。

## 输入

```text
source=e/630/解释/model-capability-required-term-pair-loss-internal-joint-cycle-seed-3535
label=loss-internal-joint-cycle
device=cpu
```

## 诊断结果

```text
status=pass
decision=refresh_forced_choice_partial_internal_match
expected_best_prompt_count=1
forced_choice_full_match_source_count=0
model_quality_claim=partial_internal_signal_only
```

prompt-level 观察：

```text
fixed= -> best fixed
loss=  -> best fixed, expected loss is not best
```

解释：v630 的生成结果已经 pair-full，但 teacher-forced 内部评分仍偏向 fixed，所以还不能把它提升为稳定能力。

## 截图说明

- `图片/v631-loss-internal-joint-cycle-forced-choice.png`
  - forced-choice HTML 截图，证明 `Expected-best=1`、`Full match=0`，并给出 prompt-level 摘要。

## 结论

v631 把 v630 的正结果降温成“生成 pair-full + internal partial”的状态；后续需要对齐比较和第二个 corpus 变体，而不是直接 promotion。

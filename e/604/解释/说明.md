# v604 required-term pair fixed-retention route decision 运行说明

## 本版目标

v604 读取 v603 comparison，做 fixed-retention route decision。目标是明确 v601 是否能 promotion，还是只能作为 loss rebalance 的依据。

## 关键结果

```text
status=pass
decision=select_fixed_recovery_route_for_loss_rebalance_not_promotion
route_count=3
pair_full_route_count=0
selected_route=v601-first-token
selected_corpus_mode=equals_surface_no_pair_id_fixed_retention_first_token_repair
loss_rebalance_objective_required=True
```

## 解释

v601 能恢复 `fixed=`，但覆盖了 `loss=`，所以它不是 promotion route。它的价值是证明 fixed first-token 可以被拉回，下一步需要围绕这个 route 做 loss rebalance。

## 截图说明

- `图片/v604-fixed-retention-route-decision.png`
  - Playwright MCP 截取的 v604 route decision HTML。
  - 证明 selected route、pair-full count 和 next action 可见。

## 结论

v604 把 v603 的比较结果转为明确路线：以 v601 为 fixed recovery 证据，设计 loss-rebalance objective。

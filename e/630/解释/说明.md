# v630 required-term pair loss-internal joint-cycle seed 3535 运行说明

## 本版目标

v630 运行 v629 新增的 `joint_cycle` corpus mode，验证它是否能同时保住 `fixed=` 和 `loss=` 的生成命中。

本版是真实 tiny 训练，不是静态合约。

## 训练配置

```text
seed=3535
corpus_mode=equals_surface_no_pair_id_loss_internal_joint_cycle_repair
repeat=320
bridge_repeat=24
max_iters=1800
block_size=16
n_embd=64
learning_rate=0.02
device=cpu
```

## 生成结果

```text
status=pass
decision=required_term_pair_coexistence_refresh_pair_full_observed
training_status=pass
checkpoint_exists=True
pair_full_observed=True
default_pair_full_variant_count=1
suppression_pair_full_variant_count=1
```

replay 观察：

```text
fixed= -> d= fixed= l
loss=  -> losssssssss
```

解释：两个 profile 都能同时命中 fixed/loss，所以 v630 是这一批路线里第一次出现 pair-full 的真实 checkpoint。

## 截图说明

- `图片/v630-loss-internal-joint-cycle-seed-3535.png`
  - 训练/replay HTML 截图，证明 training pass、checkpoint 存在，并且 `pair_full_observed=True`。

## 结论

v630 证明 joint-cycle corpus 比单边 fixed bridge 更有效；下一步需要 forced-choice sidecar 验证内部偏好是否也保持 fixed/loss 双命中。

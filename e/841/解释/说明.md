# v841 运行截图和解释

## 本版目标

v841 承接 v840 的 zero-hit diagnostic，验证一个更窄的问题：如果给 decoder 一个很小的输出锚点，v838 checkpoint 是否能补出 `fixed/loss`。

这版不做新训练，不改 objective contract，不把锚点补齐解释为普通生成能力恢复。

## 输入

- `e/839/解释/model-capability-route-promotion-bounded-objective-replay-comparison/`
  - v839 的真实 replay 行，包含 prompt、continuation、hit/missed terms。
- `e/840/解释/model-capability-route-promotion-bounded-objective-replay-zero-hit-diagnostic/`
  - v840 的 near-miss 和 zero-hit 诊断。
- `e/838/解释/model-capability-route-promotion-bounded-objective-training-run/run/`
  - v838 的真实 checkpoint 和 tokenizer。

## 输出

产物目录：

```text
e/841/解释/model-capability-route-promotion-bounded-objective-decoder-anchor-probe/
```

关键文件：

```text
model_capability_route_promotion_bounded_objective_decoder_anchor_probe.json
model_capability_route_promotion_bounded_objective_decoder_anchor_probe.csv
model_capability_route_promotion_bounded_objective_decoder_anchor_probe.txt
model_capability_route_promotion_bounded_objective_decoder_anchor_probe.md
model_capability_route_promotion_bounded_objective_decoder_anchor_probe.html
```

截图：

```text
e/841/图片/v841-bounded-objective-decoder-anchor-probe-html.png
```

## 真实结果

```text
status=pass
decision=model_capability_route_promotion_bounded_objective_decoder_anchor_probe_found_completion_signal
bounded_objective_decoder_anchor_probe_ready=True
case_count=3
probe_row_count=9
anchor_assisted_pass_count=9
completion_pass_count=9
new_text_pass_count=0
anchor_completion_success=True
promotion_ready=False
model_quality_claim=decoder_anchor_signal_only
next_action=build_bounded_objective_decoder_anchor_policy
```

结果说明：

- 三个 objective cases 都能在 anchor-assisted combined text 里补出 required terms。
- `new_text_pass_count=0` 说明普通 continuation 本身仍没有完整恢复。
- `promotion_ready=False` 保持保守边界，避免把锚点辅助当成模型能力晋级。

## 截图说明

HTML 截图展示了状态卡片和 probe rows，重点可见：

- `Status=pass`
- `Decision=model_capability_route_promotion_bounded_objective_decoder_anchor_probe_found_completion_signal`
- `Completion=9`
- `New text=0`
- `Claim=decoder_anchor_signal_only`
- `Promotion=False`

## 一句话总结

v841 证明 v838 checkpoint 在强约束锚点下存在 decoder completion signal，但普通 replay 仍未恢复，下一步应该把锚点信号收敛成受控 policy，而不是直接晋级模型能力。

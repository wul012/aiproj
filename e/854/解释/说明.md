# v854 运行截图和解释

## 本版目标

v854 承接 v853 replay comparison，诊断为什么 revised no-anchor checkpoint 已经能命中 `fixed`，但还不能完成 `fixed loss` contract。

本版不继续训练，不扩大模型，不做 promotion。

## 输入

- `e/853/解释/model-capability-route-promotion-bounded-objective-unassisted-repair-seed-revision-replay-comparison/`
- `e/851/解释/model-capability-route-promotion-bounded-objective-unassisted-repair-seed-revision/`
- `e/852/解释/model-capability-route-promotion-bounded-objective-unassisted-repair-seed-revision-training-run/`
- `e/851/解释/model-capability-route-promotion-bounded-objective-unassisted-repair-seed-revision/model_capability_route_promotion_bounded_objective_unassisted_repair_seed_revision_corpus.txt`

## 输出

产物目录：

```text
e/854/解释/model-capability-route-promotion-bounded-objective-unassisted-repair-seed-revision-partial-hit-diagnostic/
```

截图：

```text
e/854/图片/v854-bounded-objective-unassisted-repair-seed-revision-partial-hit-diagnostic-html.png
```

## 真实结果

```text
status=pass
decision=model_capability_route_promotion_bounded_objective_unassisted_repair_seed_revision_partial_hit_diagnostic_ready
bounded_objective_unassisted_repair_seed_revision_partial_hit_diagnostic_ready=True
partial_hit_case_count=2
zero_hit_case_count=1
hit_terms=['fixed']
missed_terms=['fixed', 'loss']
root_cause_count=6
model_quality_claim=partial_required_term_signal_diagnosed
next_action=build_bounded_objective_unassisted_repair_seed_revision_curriculum_patch
```

## 关键原因

- `first_term_only_uptake`：两个 `answer:` case 命中 `fixed`，但没有命中 `loss`。
- `loss_term_not_stabilized`：`loss` 在 corpus 中存在，但 replay continuation 仍然 miss。
- `prompt_surface_still_zero_hit`：`completion:` surface 仍然没有任何 required-term hit。
- `no_case_passed_contract`：没有 case 同时满足 `fixed` 和 `loss`。

## 验证

```text
py_compile: pass
focused pytest: 3 passed
full pytest: 1741 passed
source encoding: source_count=1276 clean_count=1276 bom_count=0 syntax_error_count=0
git diff --check: pass
```

## 一句话总结

v854 把 v853 的 partial signal 定位为 first-term-only uptake，下一步应补 curriculum patch，而不是宣布模型能力恢复。

# v647 required-term pair alignment comparison with light-merge 运行说明

## 本版目标

v647 将 v645/v646 light-merge 结果加入完整 alignment matrix，确认它是否改变路线排序。

## 比较结果

```text
status=pass
decision=keep_generation_pair_full_and_repair_internal_preference
generation_pair_full_count=1
internal_pair_full_count=2
aligned_pair_full_count=0
```

新增 light-merge 分类：

```text
joint-cycle-light-merge -> partial_tradeoff
```

解释：light-merge generation 命中 loss，但 internal 只保 fixed，因此不是候选主线。

## 截图说明

- `图片/v647-alignment-comparison-with-light-merge.png`
  - comparison HTML 截图，证明加入 light-merge 后 aligned full 仍为 `0`。

## 结论

v647 说明 v644-v646 没有找到 aligned route；v648 应关闭本批次，并把下一步转向更结构化的双阶段训练或参数/调度实验。

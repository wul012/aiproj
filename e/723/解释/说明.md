# v723 运行截图和解释：capacity-probe plan

## 本版目标

v723 读取 v722 四路比较，在单边 fixed/loss row patching 被关闭后，生成容量探针训练计划。

本版不训练模型；它只定义下一次更大 tiny 模型训练的边界和配置。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_capacity_probe_plan.py e\722\解释\model-capability-required-term-pair-readiness-four-route-comparison --out-dir e\723\解释\model-capability-required-term-pair-readiness-capacity-probe-plan --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_capacity_probe_plan_ready
failed_count=0
plan_ready=True
proposed_next_artifact=pair_readiness_capacity_probe_training_run
corpus_source=fixed_recovery_corpus_materialization
n_layer=2
n_head=2
n_embd=96
max_iters=1800
learning_rate=0.01
model_quality_claim=plan_only
```

## 截图说明

- `e/723/图片/v723-capacity-probe-plan.png`

截图展示 capacity training config、checks 和 next action。

## 关键结论

- v722 已关闭单边 fixed/loss row patching。
- v723 不再继续加 rows，而是计划测试稍大 tiny model。
- 计划配置为 2 层、2 头、96 embedding、1800 iters、learning rate 0.01。
- 下一版应在 v720 fixed-recovery corpus 上跑真实 capacity probe training。

一句话总结：v723 把后续路线从行级补丁转向容量探针，为 v724 真实训练定义了可复核配置。

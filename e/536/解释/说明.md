# v536 运行截图和解释：colon-immediate stability

## 本版目标

v536 复验 v535 的 colon-immediate 正向信号是否稳定。它不改变 corpus 和训练参数，只把 seed 从单个 `535` 扩展到 `535, 1535, 2535`，观察 fixed/loss pair-full 是否跨 seed 复现。

## 运行命令

```powershell
python -B scripts\run_model_capability_required_term_pair_colon_immediate_stability.py --out-dir e\536\解释\model-capability-required-term-pair-colon-immediate-stability --seeds 535 1535 2535 --repeat 260 --bridge-repeat 20 --max-iters 1400 --eval-iters 2 --batch-size 16 --block-size 16 --n-layer 1 --n-head 1 --n-embd 64 --learning-rate 0.02 --force --require-pass
```

## 结果摘要

```text
status=pass
decision=required_term_pair_colon_immediate_partial_stability
failed_count=0
seed_count=3
pair_full_seed_count=1
pair_full_seed_rate=0.3333
stable_pair_full=False
model_quality_claim=targeted_pair_refresh_partial_signal
next_action=inspect missed seeds before increasing corpus or model size
```

seed 明细：

```text
535  -> pair_full=True, continuation_hit_count=2
1535 -> pair_full=False, continuation_hit_count=0
2535 -> pair_full=False, continuation_hit_count=1
```

## 截图说明

- `e/536/图片/01-model-capability-required-term-pair-colon-immediate-stability.png`：Playwright MCP 打开的 v536 HTML 报告截图，展示 3 seed 中只有 1 个 seed 达到 pair-full。

## 结论

v536 保留 v535 的正向信号，但把它从“可能稳定”降级为“partial stability”。colon-immediate 的方向是有效线索，但还不能作为稳定 pair objective baseline。下一步应检查 missed seeds 的 first-token / continuation 差异，再决定是调 corpus 多样性还是训练参数。

一句话总结：v536 把 v535 的单 seed pair-full 信号升级为三 seed 复验，并确认它目前只是部分稳定。

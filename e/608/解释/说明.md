# v608 required-term pair fixed-retention batch closeout 运行说明

## 本版目标

v608 汇总 v599-v607 的 fixed-retention / loss-rebalance 批次，生成机器可读 closeout。目标是判断当前路线是否值得继续扩展，还是应该停止并转向新的 objective 设计。

本版不是新训练，也不是新治理链；它是对本批真实训练证据的收口。

## 输入证据

```text
v600 balanced fixed-retention seed 3535
v601 first-token fixed-retention seed 3535
v602 prompt-guard fixed-retention seed 3535
v603 fixed-retention objective comparison
v604 fixed-retention route decision
v606 loss-rebalance seed 3535
v607 dual-cycle seed 3535
```

## 结果

```text
status=pass
decision=close_fixed_retention_loss_rebalance_batch_before_new_design
failed_count=0
loss_rebalance_pair_full_count=0
loss_rebalance_tradeoff_confirmed=True
stop_current_loss_rebalance_routes=True
model_quality_claim=negative_tradeoff_evidence
```

## 验证

```text
python -m pytest -q -o cache_dir=runs\pytest-cache-v608-full
1208 passed

python -B scripts\check_source_encoding.py --out-dir runs\source-encoding-hygiene-local-v608
status=pass
clean_count=700
syntax_error_count=0
```

解释：

- v606 是 loss-only。
- v607 是 fixed-only。
- 两条 loss-rebalance 路线互补，但没有任何一条达到 pair-full。
- 当前分支应停止，下一步应检查 first-token preference 或设计新的 pair objective shape。

## 截图说明

- `图片/v608-fixed-retention-batch-closeout.png`
  - Playwright MCP 截取的 v608 closeout HTML。
  - 证明 `status=pass`、`decision=close_fixed_retention_loss_rebalance_batch_before_new_design`、loss pair-full 为 `0`。

## 结论

v608 把 v599-v607 的连续实验收束成明确决策：fixed-retention/loss-rebalance 不是稳定 pair-full 路线，下一批应换 objective 设计。

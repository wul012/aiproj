# v590 required-term pair loss-branch targeted seed 3535 运行说明

## 本版目标

v590 使用 v589 的 `equals_surface_no_pair_id_loss_branch_targeted_repair` 做真实 seed `3535` tiny 训练。目标是确认 targeted loss objective 是否能恢复 `loss`，以及是否能同时保留 `fixed`。

## 运行命令

```text
python -B scripts\run_model_capability_required_term_pair_coexistence_refresh.py ^
  --out-dir e\590\解释\model-capability-required-term-pair-loss-branch-targeted-seed-3535 ^
  --seed 3535 ^
  --corpus-mode equals_surface_no_pair_id_loss_branch_targeted_repair ^
  --repeat 260 ^
  --bridge-repeat 20 ^
  --max-iters 1800 ^
  --device cpu ^
  --force
```

## 关键结果

```text
status=pass
training_status=pass
checkpoint_exists=True
pair_full_observed=False
default_pair_full_variant_count=0
suppression_pair_full_variant_count=0
default_continuation_hit_count=1
suppression_continuation_hit_count=1
hit_terms=loss
missed_terms=fixed
```

## 截图说明

- `图片/v590-loss-branch-targeted-seed-3535.png`
  - Playwright MCP 截取的 v590 HTML 报告。
  - 证明本版真实训练完成，但只有 `loss` 命中，没有 pair-full。

## 结论

v590 证明 targeted loss objective 能把 seed `3535` 从 fixed residual 推向 loss residual，但它造成 fixed 掉线，暂时只能视为 branch tradeoff 证据。

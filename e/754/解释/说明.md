# v754 运行截图和解释：exact-surface repair corpus materialization

## 本版目标

v754 将 v753 exact-surface repair contract patch 物化成训练 corpus 和 heldout eval fixture。

本版不训练模型；它只验证 24 条 training rows 能被稳定展开，并确认 exact heldout prompt `fixed=|loss=` 没有进入训练 corpus。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_corpus_materialization.py e\753\解释\model-capability-required-term-pair-readiness-exact-surface-repair-contract-patch --out-dir e\754\解释\model-capability-required-term-pair-readiness-exact-surface-repair-corpus-materialization --repeat 320 --require-pass --force
```

输出摘要：

```text
status=pass
decision=pair_readiness_corpus_materialized
failed_count=0
materialization_ready=True
training_line_count=7680
evaluation_probe_count=3
model_quality_claim=data_artifact_only
```

## 截图说明

- `e/754/图片/v754-exact-surface-repair-corpus-materialization.png`

截图显示 `Train lines=7680`、`Eval probes=3`，并确认 source contract decision 为 `pair_readiness_exact_surface_repair_contract_patch_ready`。

## 关键观察

- 24 条 training rows 乘以 repeat 320，得到 7680 行训练文本。
- `heldout_not_in_training_rows` 和 `heldout_not_in_corpus` 均通过。
- 输出包含 `pair_readiness_training_corpus.txt` 和 `pair_readiness_heldout_eval_fixture.json`，可直接供 v755 训练。

一句话总结：v754 将 exact-surface repair contract patch 变成可训练 corpus，但模型效果要等后续训练和 replay 验证。

# v598 required-term pair loss-branch batch closeout 运行说明

## 本版目标

v598 收尾 v589-v598 十版 loss-branch 批次。它把 v589 语料契约、v590-v592 三条真实训练、v593 比较、v594 route、v595 三 seed 稳定性、v596 missed-seed diagnostic 和 v597 readiness 合成一个 closeout artifact。

本版不宣称模型能力已经提升到 pair-full。它的结论是：loss 分支已经被模型学到，但 fixed retention 仍然掉落，因此下一步应关闭继续加 loss 权重的路线，转向 fixed-retention objective。

## 关键结果

```text
status=pass
decision=close_loss_branch_batch_and_start_fixed_retention_objective
version_range=v589-v598
source_report_count=9
single_seed_pair_full_count=0
stability_pair_full_seed_count=0
diagnostic_first_token_gap_count=3
readiness_ready_for_design=True
```

## 证据链

```text
v589: loss-branch corpus modes ready
v590: targeted seed 3535 loss-only tradeoff
v591: dual-anchor seed 3535 loss-only tradeoff
v592: micro-span seed 3535 loss-only tradeoff
v593: comparison confirms 3 loss-only tradeoff routes
v594: route selects targeted only for stability replay
v595: 3-seed stability still pair-full 0/3
v596: all 3 missed seeds confirm first-token gap
v597: fixed-retention objective readiness pass
```

## 截图说明

- `图片/v598-loss-branch-batch-closeout.png`
  - Playwright MCP 截取的 v598 closeout HTML。
  - 页面展示 `status=pass`、批次范围、pair-full seed 数、first-token gap 数和下一步动作。

## 验证

```text
python -m pytest tests\test_model_capability_required_term_pair_coexistence_refresh.py tests\test_model_capability_required_term_pair_loss_branch_objective_comparison.py tests\test_model_capability_required_term_pair_loss_branch_route_decision.py tests\test_model_capability_required_term_pair_fixed_retention_objective_readiness.py tests\test_model_capability_required_term_pair_loss_branch_batch_closeout.py -q -o cache_dir=runs\pytest-cache-v598-targeted
42 passed

python -m pytest -q -o cache_dir=runs\pytest-cache-v598-full
1187 passed

python -B scripts\check_source_encoding.py --out-dir runs\source-encoding-hygiene-local-v598
status=pass
clean_count=684
```

## 结论

v598 把“继续 loss-branch 已经没有意义”收敛成明确工程决策：下一轮应设计并训练 fixed-retention objective，而不是继续堆 loss-only 语料。

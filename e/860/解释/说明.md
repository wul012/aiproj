# v860 运行截图和解释

## 本版目标

v860 根据 v859 profile sweep 的 `loss` signal，生成一份 no-anchor loss-signal bridge corpus，供下一版训练使用。

本版不训练，不做 replay，不声称模型能力提升；它只是把“loss 能出现但 fixed/loss 不共现”的证据转成训练数据。

## 输入

- `e/859/解释/bounded-objective-curriculum-patch-profile-sweep/`
- `e/855/解释/model-capability-route-promotion-bounded-objective-unassisted-repair-seed-revision-curriculum-patch/model_capability_route_promotion_bounded_objective_unassisted_repair_seed_revision_curriculum_patch_corpus.txt`

## 输出

产物目录：

```text
e/860/解释/bounded-objective-loss-signal-bridge/
```

截图：

```text
e/860/图片/v860-bounded-objective-loss-signal-bridge-html.png
```

## 真实结果

```text
status=pass
decision=bounded_objective_loss_signal_bridge_ready
bounded_objective_loss_signal_bridge_ready=True
bridge_example_count=16
loss_signal_bridge_example_count=6
fixed_signal_bridge_example_count=6
pair_reinforcement_example_count=4
bridged_corpus_char_count=3908
model_quality_claim=bridge_corpus_only
next_step=train_bounded_objective_loss_signal_bridge
```

## 样本来源

- `longer-top20/minimal_direct_completion` 贡献 loss-only bridge。
- `longer-open/canonical_direct_completion` 贡献 loss-only bridge。
- `longer-open/completion_label_surface` 贡献 loss-only bridge。
- `v857-baseline`、`top1-low-temp`、`top3-low-temp`、`longer-top20` 的 fixed-only 行贡献 fixed-to-pair bridge。
- 额外加入 4 条 pair reinforcement。

## 边界

这些样本全部 `decoder_anchor=False`。v860 只生成 corpus，不证明 checkpoint 已提升。能力判断必须等下一版真实训练后再 replay。

## 验证

```text
py_compile: pass
focused pytest: 3 passed
Playwright MCP: snapshot and full-page screenshot saved
```

后续还会跑全量 pytest、source encoding 和 `git diff --check` 后再提交。

## 一句话总结

v860 把 v859 的 loss-only/fixed-only 信号转成可训练的 no-anchor bridge corpus。

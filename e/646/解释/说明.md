# v646 required-term pair joint-cycle light-merge forced-choice 运行说明

## 本版目标

v646 对 v645 light-merge checkpoint 跑 forced-choice 诊断，确认它的 internal preference 是否随 loss generation 一起改善。

## 输入

```text
source=e/645/解释/model-capability-required-term-pair-joint-cycle-light-merge-seed-3535
label=joint-cycle-light-merge
device=cpu
```

## 诊断结果

```text
status=pass
decision=refresh_forced_choice_partial_internal_match
expected_best_prompt_count=1
forced_choice_full_match_source_count=0
```

prompt-level 观察：

```text
fixed= -> best fixed
loss=  -> best fixed, expected loss is not best
```

解释：light-merge 在 generation 上偏 loss，但 internal forced-choice 仍偏 fixed，说明两层目标没有对齐。

## 截图说明

- `图片/v646-joint-cycle-light-merge-forced-choice.png`
  - forced-choice HTML 截图，证明 `Expected-best=1`、`Full match=0`。

## 结论

v646 排除了 light-merge 作为直接候选；下一步应做总比较和 closeout，而不是继续堆相似 corpus。

# v625 required-term pair loss-internal-preference route decision 运行说明

## 本版目标

v625 结合 v623 generation comparison 和 v624 forced-choice diagnostic 做路线决策。

本版不训练新 checkpoint；它判断 v621 是否可以 promotion，或者只能作为 decode bridge 候选。

## 输入证据

```text
v623 loss-internal-preference objective comparison
v624 loss-internal forced-choice diagnostic
```

## 结果

```text
status=pass
decision=select_loss_internal_first_token_for_decode_bridge_not_promotion
forced_choice_full_match_source_count=1
decode_bridge_candidate_count=1
selected_decode_bridge_source=loss-internal-first-token
internal_to_generation_bridge_required=True
```

解释：

- v621 内部 forced-choice 已经 pair match。
- 但 v621 generation replay 不是 pair-full。
- 因此 v621 不能 promotion，只能进入 internal-to-generation bridge 检查。

## 截图说明

- `图片/v625-loss-internal-preference-route-decision.png`
  - Playwright MCP 截取的 route decision HTML。
  - 证明 decision、selected route 和 bridge candidate 计数。

## 结论

v625 把下一步路线从“继续训练新语料”改为“围绕 v621 做 decode bridge”。

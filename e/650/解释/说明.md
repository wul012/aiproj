# v650 运行截图与解释

## 本版目标

v650 把 v649 的 two-stage schedule plan 落成一个可运行的语料近似模式：`equals_surface_no_pair_id_loss_internal_surface_first_schedule_repair`。它先给模型看到 surface 生成目标，再加入 soft internal rank 行，目标是在不伪装 checkpoint resume 的前提下测试“先保生成，再修内部偏好”的方向。

## 关键结果

- 新增 corpus mode 已注册到 `PAIR_LOSS_INTERNAL_PREFERENCE_OBJECTIVE_CORPUS_MODES`。
- `source_prompts(mode)=("fixed=", "loss=")` 保持不变。
- contract 结果：
  - `status=pass`
  - `decision=surface_first_schedule_corpus_contract_ready`
  - `failed_count=0`
  - `boundary=not_checkpoint_resume`

## 截图说明

- `e/650/图片/v650-surface-first-schedule-corpus-contract.png`：Playwright MCP 打开的 corpus contract HTML 截图，能看到 mode ready、边界、检查项和样例语料行。

## 证据角色

本版还不是训练结果，只是把 v649 的 schedule plan 变成可供 v651 真实训练调用的 corpus mode。它明确不使用 `teacher forced loss= loss` 这种重内部强制行，避免再次复现 v640 的生成侧回退。

一句话总结：v650 让两阶段方向有了真实可运行的语料入口，下一版可以直接跑 seed 3535。

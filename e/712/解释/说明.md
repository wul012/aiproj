# v712 运行截图和解释：loss-retention training run

## 本版目标

v712 使用 v711 loss-retention patched corpus 做真实训练，验证 v710/v711 的修复是否改善 heldout direct probes。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_training_run.py e\711\解释\model-capability-required-term-pair-readiness-loss-retention-corpus-materialization --out-dir e\712\解释\model-capability-required-term-pair-readiness-loss-retention-training-run --seed 3535 --max-iters 1400 --eval-iters 2 --batch-size 16 --block-size 16 --n-layer 1 --n-head 1 --n-embd 64 --learning-rate 0.02 --max-new-tokens 12 --temperature 0.2 --top-k 1 --device cpu --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_training_no_pair_full
training_status=pass
checkpoint_exists=True
pair_full_observed=False
default_continuation_hit_count=0
model_quality_claim=not_claimed
```

关键样本：

```text
fixed= -> fixed=los=los=los=
loss=  -> loss=los=los=los=
```

## 截图说明

- `e/712/图片/v712-loss-retention-training-run.png`

截图展示 v712 training run HTML，明确 pair-full 仍为 false。

## 结论

loss-retention patch 没有带来能力提升：

- v707 至少 fixed direct hit。
- v712 两个 direct probe 都没有完整命中。
- 输出稳定漂移到 `los` 前缀循环，而不是完整 `loss`。

一句话总结：v712 证明当前 loss-retention patch 过度强化了 `los` 前缀，未恢复 heldout direct pair capability。

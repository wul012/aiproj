# v581 required-term pair branch-binding no-space seed 3535 运行说明

## 本版目标

v580 发现 v579 的 branch-binding v1 在 `fixed=` 和 `loss=` 后都先漂到空格。v581 因此新增 no-space branch-binding 变体，把训练样本改成 equals 后直接接目标 term。

本版做真实训练，但不做 seed sweep；它只验证 seed `3535` 是否被 no-space objective 修复。

## 运行命令

```powershell
python scripts\run_model_capability_required_term_pair_colon_immediate_stability.py `
  --out-dir e\581\解释\model-capability-required-term-pair-branch-binding-no-space-seed-3535 `
  --seeds 3535 `
  --corpus-mode equals_surface_no_pair_id_branch_binding_no_space_repair `
  --repeat 260 `
  --bridge-repeat 20 `
  --max-iters 1400 `
  --eval-iters 2 `
  --batch-size 16 `
  --block-size 16 `
  --n-layer 1 `
  --n-head 1 `
  --n-embd 64 `
  --learning-rate 0.02 `
  --max-new-tokens 12 `
  --temperature 0.8 `
  --top-k 2 `
  --device cpu `
  --force `
  --require-pass
```

## 关键结果

```text
status=pass
decision=required_term_pair_colon_immediate_not_stable
seed_count=1
pair_full_seed_count=0
stable_pair_full=False
continuation_hit_count=0
```

no-space branch-binding 没有修复 seed `3535`。

## 截图说明

- `图片/v581-branch-binding-no-space-seed-3535.png`
  - Playwright MCP 截取的 HTML stability 报告。
  - 证明 no-space branch-binding 的真实训练结果可见。

## 结论

v581 证明仅移除 whitespace 诱导句还不够。下一步应把 v571、v579、v581 放到同一 comparison 里，确认 branch-binding 是否相对旧 loss-balanced 有任何增量。

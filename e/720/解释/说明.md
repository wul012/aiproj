# v720 运行截图和解释：fixed-recovery corpus materialization

## 本版目标

v720 把 v719 fixed-recovery contract patch materialize 成真实训练 corpus 和 heldout fixture。

本版不训练模型；它只落地 v721 的训练输入。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_corpus_materialization.py e\719\解释\model-capability-required-term-pair-readiness-fixed-recovery-contract-patch --out-dir e\720\解释\model-capability-required-term-pair-readiness-fixed-recovery-corpus-materialization --repeat 320 --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_corpus_materialized
failed_count=0
materialization_ready=True
training_line_count=7040
evaluation_probe_count=3
model_quality_claim=data_artifact_only
```

## 截图说明

- `e/720/图片/v720-fixed-recovery-corpus-materialization.png`

截图展示 training line count、materialized paths、checks 和 corpus preview。

## 关键结论

- v719 的 22 条 patched rows 重复 320 次，形成 7040 行 corpus。
- heldout fixture 仍是 3 个 probes。
- `fixed=|loss=` 没有作为 training row 或 corpus line 出现。

一句话总结：v720 把 fixed-recovery contract patch 落成可训练数据，为 v721 真实训练准备好输入。

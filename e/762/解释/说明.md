# v762 运行截图和解释：objective-level contrast corpus materialization

## 本版目标

v762 消费 v761 objective-level contrast contract，复用已有 pair-readiness corpus materialization 能力，生成训练 corpus 和 heldout eval fixture。

本版不训练模型。它只证明新 contract 已经能进入真实训练输入链路，并且 heldout pair prompt 没有泄漏进训练 corpus。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_corpus_materialization.py e\761\解释\model-capability-required-term-pair-readiness-objective-level-contrast-contract --out-dir e\762\解释\model-capability-required-term-pair-readiness-objective-level-contrast-corpus-materialization --repeat 320 --require-pass --force
```

输出摘要：

```text
status=pass
decision=pair_readiness_corpus_materialized
materialization_ready=True
training_line_count=8320
evaluation_probe_count=3
model_quality_claim=data_artifact_only
```

## 截图说明

- `e/762/图片/v762-objective-level-contrast-corpus-materialization.png`

截图确认 HTML 页面可见：

- `Decision pair_readiness_corpus_materialized`
- `Train lines 8320`
- `Eval probes 3`
- training corpus 和 heldout fixture 输出路径
- corpus preview 以 objective-level rows 开头

## 关键观察

- v761 的 26 行 contract rows 使用 `repeat=320` 后生成 8320 行训练 corpus。
- heldout fixture 保留 `fixed=`、`loss=` 和 `fixed=|loss=` 三个 probes。
- materializer 通过 `heldout_not_in_training_rows` 和 `heldout_not_in_corpus` 检查。
- 下一版可以直接复用 pair-readiness training runner 训练 fresh tiny checkpoint。

一句话总结：v762 把 objective-level contrast contract 物化成真实训练输入，完成从 contract 到 corpus 的闭环。

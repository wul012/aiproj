# v711 运行截图和解释：loss-retention corpus materialization

## 本版目标

v711 使用 v706 的 materializer，将 v710 patched contract 落成新的训练语料和 heldout eval fixture。

本版不写新代码，不训练模型，只生成 loss-retention 修复后的数据输入。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_corpus_materialization.py e\710\解释\model-capability-required-term-pair-readiness-loss-retention-contract-patch\model_capability_required_term_pair_readiness_loss_retention_contract_patch.json --out-dir e\711\解释\model-capability-required-term-pair-readiness-loss-retention-corpus-materialization --repeat 320 --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_corpus_materialized
materialization_ready=True
training_line_count=6400
evaluation_probe_count=3
model_quality_claim=data_artifact_only
```

## 截图说明

- `e/711/图片/v711-loss-retention-corpus-materialization.png`

截图展示 materialized paths、checks 和 corpus preview。

## 结论

v711 相比 v706 的差异：

- v706 基于 12 行 base contract，展开后 3840 行。
- v711 基于 20 行 patched contract，展开后 6400 行。
- heldout eval probes 仍为 3 个。
- heldout pair probe 仍不进入训练行。

一句话总结：v711 把 v710 的 loss-retention contract patch 转成真实训练 corpus，为下一版重新训练做准备。

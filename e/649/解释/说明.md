# v649 运行截图与解释

## 本版目标

v649 补上 `required-term pair` 路线的两阶段 schedule plan。它不是新训练结果，也不是 checkpoint resume；它把 v630/v631 的生成 pair-full 证据和 v640/v641 的内部 forced-choice pair-full 证据放到同一张计划表里，确认下一步可以做 `surface-first` 的可运行近似实验。

## 运行命令

```powershell
python -B scripts\build_model_capability_required_term_pair_two_stage_schedule_plan.py `
  --surface-source loss-internal-joint-cycle e\630\解释\model-capability-required-term-pair-loss-internal-joint-cycle-seed-3535 e\631\解释\model-capability-required-term-pair-loss-internal-joint-cycle-forced-choice `
  --internal-source joint-cycle-internal-repair e\640\解释\model-capability-required-term-pair-joint-cycle-internal-repair-seed-3535 e\641\解释\model-capability-required-term-pair-joint-cycle-internal-repair-forced-choice `
  --out-dir e\649\解释\model-capability-required-term-pair-two-stage-schedule-plan `
  --require-pass --force
```

## 关键结果

- `status=pass`
- `decision=two_stage_surface_internal_schedule_ready`
- `failed_count=0`
- `stage_gate_pass_count=2`
- `guardrail_fail_count=0`
- `schedule_boundary.training_semantics=not_checkpoint_resume`

## 截图说明

- `e/649/图片/v649-two-stage-schedule-plan.png`：Playwright MCP 打开的 HTML 报告截图，证明页面可读，能看到 `status=pass`、`decision=two_stage_surface_internal_schedule_ready`、`Boundary=not_checkpoint_resume` 和两类 stage/guardrail。

## 证据角色

本版产物是计划证据，不是模型质量提升证明。它的价值在于把“生成 pair-full”和“内部 pair-full”分离在两条路线上的事实写成可审计输入，避免下一版把单语料近似误说成真实 checkpoint 续训。

一句话总结：v649 把 v648 选出的两阶段方向落成可复核 plan，为 v650 的 surface-first corpus approximation 提供边界清楚的入口。

# v937 randomized holdout publication registry lookup index review 运行说明

## 本版目标

v937 消费 v936 lookup index，确认该 index 只可用于 downstream governance lookup：

```text
v936 lookup index
  -> v937 lookup index review
```

它是 downstream guard 之前的只读评审层。

明确不做：

- 不重新训练。
- 不重新 replay。
- 不改变 v936 index。
- 不批准 production promotion。

## 输入和输出

输入：

```text
e/936/解释/randomized-holdout-publication-registry-lookup-index
```

输出：

```text
e/937/解释/randomized-holdout-publication-registry-lookup-index-review
```

截图：

```text
e/937/图片/v937-randomized-holdout-publication-registry-lookup-index-review.png
```

## 实际结果

真实 v936 输入运行结果：

```text
status=pass
decision=randomized_holdout_publication_registry_lookup_index_review_ready
failed_count=0
randomized_holdout_publication_registry_lookup_index_review_ready=True
review_id=randomized-holdout-publication-registry-lookup-index-review-v937
review_status=approved_for_downstream_governance_lookup_only
entry_count=1
downstream_ready=True
lookup_ready=True
contract_check_ready=True
promotion_ready=False
allowed_use=downstream_governance_lookup_only
rejected_use=production_promotion
next_step=build_randomized_holdout_publication_registry_downstream_guard
passed_check_count=16
failed_check_count=0
```

## 截图验证

Playwright MCP 已保存 HTML 快照和截图。页面可见：

- `Status = pass`
- `Review ready = True`
- `Downstream = True`
- `Lookup = True`
- `Contract = True`
- `Failed = 0`

## 一句话总结

v937 把 v936 lookup index 复核为 downstream governance lookup only，为后续 downstream guard 提供了已审阅的 index 输入。

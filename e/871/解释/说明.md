# v871 运行截图和解释：bounded objective loss signal bridge single-line surface zero-hit diagnostic

## 本版目标

v871 接在 v870 后面。v870 已经证明 v869 checkpoint 在 unchanged v836 objective contract 上仍然是 zero-hit。v871 的目标不是继续训练，而是解释这个 zero-hit 到底是什么形态。

本版把 continuation 分成：

- `exact_label_echo`：例如 `answer:`
- `label_prefix_fragment`：例如 `answeti`
- `non_target_fragment`

同时记录 `loss_improved_without_required_term_uptake=True`，说明 v869 的 loss 下降没有转换成 required-term 命中。

## 输入

- `e/870/解释/bounded-objective-loss-signal-bridge-single-line-surface-replay-comparison/bounded_objective_loss_signal_bridge_single_line_surface_replay_comparison.json`

## 真实诊断结果

```text
status=pass
decision=bounded_objective_loss_signal_bridge_single_line_surface_zero_hit_label_echo_persisted
zero_hit_diagnostic_ready=True
case_count=3
exact_label_echo_case_count=2
label_prefix_fragment_case_count=1
zero_hit_case_count=3
loss_improved_without_required_term_uptake=True
model_quality_claim=label_echo_persisted_after_single_line_training
next_action=build_target_only_completion_memory_patch
```

case 级别：

```text
canonical_direct_completion -> exact_label_echo      -> answer:
minimal_direct_completion   -> exact_label_echo      -> answer:
completion_label_surface    -> label_prefix_fragment -> answeti
```

## Root causes

v871 识别六个原因：

```text
label_echo_persisted_after_single_line_patch
answer_label_echo_still_dominant
completion_label_fragment
loss_improved_without_required_term_uptake
no_anchor_surface_failure
short_decode_budget_consumed_by_label
```

其中最关键的是 `loss_improved_without_required_term_uptake`：训练指标改善是真实的，但 objective replay 没有任何 required-term hit。

## 输出产物

- `e/871/解释/bounded-objective-loss-signal-bridge-single-line-surface-zero-hit-diagnostic/bounded_objective_loss_signal_bridge_single_line_surface_zero_hit_diagnostic.json`
- `e/871/解释/bounded-objective-loss-signal-bridge-single-line-surface-zero-hit-diagnostic/bounded_objective_loss_signal_bridge_single_line_surface_zero_hit_diagnostic.csv`
- `e/871/解释/bounded-objective-loss-signal-bridge-single-line-surface-zero-hit-diagnostic/bounded_objective_loss_signal_bridge_single_line_surface_zero_hit_diagnostic.html`
- `e/871/图片/v871-bounded-objective-loss-signal-bridge-single-line-surface-zero-hit-diagnostic-html.png`

## 截图说明

Playwright MCP 截图确认 HTML 报告能打开。页面展示了诊断摘要、Case Diagnostics 表格和 Root Causes 列表。

## 一句话总结

v871 把 v870 的 zero-hit 失败解释为“低 loss 后仍持续标签回声/标签碎片”，下一步应构造 target-only completion memory patch，而不是继续重复 single-line surface 训练。

# v866 运行截图和解释

## 本版目标

v866 使用 v865 pair-binding checkpoint 回放 v836 bounded objective contract。

本版不训练、不 promotion，也不更换 contract；它只判断 v865 checkpoint 是否真实恢复 `fixed loss`。

## 输入

```text
objective contract:
e/836/解释/model-capability-route-promotion-bounded-objective-contract/

training run:
e/865/解释/bounded-objective-loss-signal-bridge-pair-binding-training-run/
```

## 输出

```text
e/866/解释/bounded-objective-loss-signal-bridge-pair-binding-replay-comparison/
```

截图：

```text
e/866/图片/v866-bounded-objective-loss-signal-bridge-pair-binding-replay-comparison-html.png
```

## 真实结果

```text
status=pass
decision=bounded_objective_loss_signal_bridge_pair_binding_replay_zero_hit
bounded_objective_loss_signal_bridge_pair_binding_replay_comparison_ready=True
objective_contract_recovered=False
canonical_case_pass=False
case_count=3
passed_case_count=0
any_hit_case_count=0
zero_hit_case_count=3
pass_rate=0.0
promotion_ready=False
model_quality_claim=not_improved
next_action=diagnose_bounded_objective_loss_signal_bridge_pair_binding_zero_hit_before_more_training
```

## Case 结果

| Case | Continuation | 结论 |
| --- | --- | --- |
| `canonical_direct_completion` | `answer:` | zero-hit |
| `minimal_direct_completion` | `answer:` | zero-hit |
| `completion_label_surface` | `ans` | zero-hit |

v865 pair-binding 训练没有恢复目标词，反而从 v862 的 partial signal 退回到 3 个 zero-hit。

## 边界解释

这不是程序失败：`status=pass` 表示 replay 执行和证据链成功。

模型能力结论是：

```text
not_improved
```

下一步应该做 zero-hit diagnostic，定位为什么 pair-binding patch 没有转化成 required-term 输出。

## 验证

```text
py_compile: pass
focused pytest: 4 passed
real replay: pass, require-comparison-ready
Playwright MCP: snapshot and full-page screenshot saved
```

后续还会跑全量 pytest、source encoding 和 `git diff --check` 后再提交。

## 一句话总结

v866 证明 pair-binding patch 训练未改善 bounded objective replay，结果从 partial signal 退回到 zero-hit。

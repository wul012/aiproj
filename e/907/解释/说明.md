# v907 运行截图与解释

## 本版目标

v907 对 v906 semantic target-hidden holdout suite 做 dry-run，确认 scoring contract 在更少任务提示的新 suite 上仍然可执行。

本版不运行模型、不读取 checkpoint、不声明模型能力提升。它只验证正例/负例 continuation 的评分行为。

## 输入证据

- v906 semantic target-hidden suite：
  `e/906/解释/target-hidden-semantic-holdout-suite`

## 输出目录

- JSON/CSV/TXT/Markdown/HTML：
  `e/907/解释/target-hidden-semantic-holdout-dry-run`
- 截图：
  `e/907/图片/v907-target-hidden-semantic-holdout-dry-run.png`

## 关键结果

```text
status=pass
decision=target_hidden_semantic_holdout_dry_run_passed
failed_count=0
target_hidden_semantic_holdout_dry_run_ready=True
case_count=5
positive_passed_case_count=5
negative_passed_case_count=0
negative_control_passed=False
promotion_ready=False
model_quality_claim=dry_run_only
next_step=run_target_hidden_semantic_holdout_real_replay
```

## 结果解释

- 正例 continuation：` fixed loss`
- 负例 continuation：` fixed only`
- 正例 5 个 case 全部通过。
- 负例 0 个 case 通过。
- v907 证明 v906 suite 的 scoring contract 没有被 semantic prompt 改写破坏。

## 截图说明

HTML 截图重点确认：

- `Status=pass`
- `Positive passed=5`
- `Negative passed=0`
- `Negative control=False`
- `Next=run_target_hidden_semantic_holdout_real_replay`

## 验证命令

```powershell
python -m py_compile src\minigpt\target_hidden_semantic_holdout_dry_run.py src\minigpt\target_hidden_semantic_holdout_dry_run_artifacts.py scripts\dry_run_target_hidden_semantic_holdout.py tests\test_target_hidden_semantic_holdout_dry_run.py
python -m pytest tests\test_target_hidden_semantic_holdout_dry_run.py -q -o cache_dir=runs\pytest-cache-v907-focused
python scripts\dry_run_target_hidden_semantic_holdout.py --holdout-suite e\906\解释\target-hidden-semantic-holdout-suite --out-dir e\907\解释\target-hidden-semantic-holdout-dry-run --require-dry-run-ready --force
```

## 一句话总结

v907 证明 v906 semantic target-hidden suite 的评分契约可用，为下一版真实 checkpoint replay 清除了评分器层面的疑问。

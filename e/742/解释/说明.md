# v742 运行截图和解释：pair prompt transfer contract patch

## 本版目标

v742 基于 v741 repair plan 和 v736 direct-completion surface contract，生成 `pair_readiness_pair_prompt_transfer_contract_patch`。

本版不训练，只把非泄漏 pair-transfer rows 加入 contract，为 v743 materialization 做准备。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_pair_prompt_transfer_contract_patch.py --repair-plan e\741\解释\model-capability-required-term-pair-readiness-pair-prompt-transfer-repair-plan --base-contract e\736\解释\model-capability-required-term-pair-readiness-direct-completion-surface-contract --out-dir e\742\解释\model-capability-required-term-pair-readiness-pair-prompt-transfer-contract-patch --require-pass --force
```

输出摘要：

```text
status=pass
decision=pair_readiness_pair_prompt_transfer_contract_patch_ready
failed_count=0
patch_ready=True
base_training_row_count=16
patched_training_row_count=24
pair_transfer_row_count=8
model_quality_claim=contract_patch_only
```

## 截图说明

- `e/742/图片/v742-pair-prompt-transfer-contract-patch.png`

截图显示 base rows 为 16、patched rows 为 24、新增 transfer rows 为 8。

## 关键观察

- patch 保留 `fixed=fixed` / `loss=loss` direct-completion rows。
- patch 加入 8 行 surrogate pair-transfer rows。
- patch 没有把 exact heldout pair prompt `fixed=|loss=` 放入训练行。
- materializer 已接入新 decision，下一版可以直接物化。

一句话总结：v742 将 pair prompt transfer 修复计划落成非泄漏 contract patch，但模型能力仍需下一轮训练验证。

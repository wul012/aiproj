# v727 运行截图和解释：objective-structure contract

## 本版目标

v727 将 v726 objective-structure plan 落成可物化的 pair-readiness contract。

它新增 task-id direct rows、paired block rows、boundary contrast rows，并保留 `fixed=`、`loss=`、`fixed=|loss=` 作为 heldout probes。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_objective_structure_contract.py e\726\解释\model-capability-required-term-pair-readiness-objective-structure-plan --out-dir e\727\解释\model-capability-required-term-pair-readiness-objective-structure-contract --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_objective_structure_contract_ready
failed_count=0
contract_ready=True
training_row_count=18
row_family_count=5
evaluation_probe_count=3
paired_block_row_count=6
model_quality_claim=contract_only
```

## 截图说明

- `e/727/图片/v727-objective-structure-contract.png`

截图显示 5 个 row family、18 条训练行、6 条 paired block row，并且所有 contract checks 通过。

## 关键观察

- fixed direct 和 loss direct row family 均为 4 条，保持直接目标平衡。
- paired block 同时包含 forward 和 reverse 两个方向。
- exact heldout probes 没有进入 training rows。
- materializer 已接入新 decision 和 JSON filename，v728 可以直接物化。

一句话总结：v727 把 objective-structure 计划转成可检查、可物化的训练契约。

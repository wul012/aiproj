# v746 运行截图和解释：fixed-preserving transfer plan

## 本版目标

v746 基于 v745 的回归诊断，生成 fixed-preserving transfer objective 计划。

本版不训练，也不直接改 contract；它只把“full surrogate transfer patch 已回退”的结论转换成下一版可检查的 contract patch 目标。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_fixed_preserving_transfer_plan.py e\745\解释\model-capability-required-term-pair-readiness-pair-prompt-transfer-regression-diagnostic --out-dir e\746\解释\model-capability-required-term-pair-readiness-fixed-preserving-transfer-plan --require-pass --force
```

输出摘要：

```text
status=pass
decision=pair_readiness_fixed_preserving_transfer_plan_ready
failed_count=0
plan_ready=True
proposed_next_artifact=pair_readiness_fixed_preserving_transfer_contract_patch
closed_route=pair_prompt_transfer_full_surrogate_patch
transfer_row_budget=4
model_quality_claim=plan_only
```

## 截图说明

- `e/746/图片/v746-fixed-preserving-transfer-plan.png`

截图显示 `Ready=True`、`Next artifact=pair_readiness_fixed_preserving_transfer_contract_patch`、`Row budget=4`，并列出 patch strategy。

## 关键观察

- v746 明确关闭上一轮 8 行 full surrogate transfer patch。
- 新计划把 transfer row budget 限制为 4。
- 新计划要求保留 `fixed=fixed` 和 `loss=loss` exact direct-completion rows。
- 新计划继续禁止 exact heldout pair prompt `fixed=|loss=` 进入训练数据。

一句话总结：v746 把 v745 的回归结论转成更轻的 fixed-preserving contract patch 计划，为下一版可检查补丁做准备。

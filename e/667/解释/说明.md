# v667 运行截图与解释

## 本版目标

v667 沿着 v666 closeout 的 next action，检查 v630 generation anchor 在 constrained decode 下是否能恢复 fixed/loss pair-full。

本版不重新训练模型，不修改 checkpoint；它只做 decode-only feasibility。

## 输入与输出

输入：

- `e/630/解释/model-capability-required-term-pair-loss-internal-joint-cycle-seed-3535`

输出：

- `e/667/解释/model-capability-required-term-pair-v630-constrained-decode-feasibility/`
- `e/667/图片/v667-v630-constrained-decode-feasibility.png`

## 关键结果

- `status=pass`
- `decision=required_term_pair_constrained_decode_partial_gain`
- `default_hit_count=0`
- `constrained_hit_count=1`
- `hit_delta=1`
- `constrained_pair_full=False`
- `fixed_constrained_hit=False`

## 解释

constrained decode 能把 `loss=` 拉回 loss 命中，但 `fixed=` 仍没有命中 fixed。也就是说，这条路线有一点 decoding gain，但不能作为 pair-full profile 晋升。

这个结果支持 v666 的判断：下一步不能只靠 naive resume，也不能只靠当前 block-competing-initial decode profile。更合理的是先分析 fixed 仍 miss 的原因，再设计更明确的 dual-objective boundary。

## 截图说明

- `e/667/图片/v667-v630-constrained-decode-feasibility.png`: Playwright MCP 截取的 HTML feasibility report，显示 partial gain、profile rows 和 case rows。

一句话总结：v667 确认 constrained decode 只能恢复 loss 单侧，仍不能解决 required-term pair-full。

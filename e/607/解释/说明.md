# v607 required-term pair fixed-retention dual-cycle seed 3535 运行说明

## 本版目标

v607 用 v605 的 `equals_surface_no_pair_id_fixed_retention_dual_cycle_repair` 跑真实 seed `3535`。它是 v606 loss-rebalance 的对照路线：通过交替 fixed/loss rows 检查能否避免单边覆盖。

本版只记录真实 tiny 训练结果，不新增治理链，也不宣称模型能力提升。

## 训练配置

```text
seed=3535
corpus_mode=equals_surface_no_pair_id_fixed_retention_dual_cycle_repair
repeat=260
bridge_repeat=20
max_iters=1800
batch_size=16
block_size=16
n_layer=1
n_head=1
n_embd=64
learning_rate=0.02
temperature=0.2
top_k=1
device=cpu
```

## 结果

```text
status=pass
decision=required_term_pair_coexistence_refresh_no_pair_full
training_status=pass
checkpoint_exists=True
pair_full_observed=False
```

Replay 结果：

```text
fixed= -> fixed=fixed=  hit fixed
loss=  -> fixed=los=fi  miss loss
```

这说明 dual-cycle route 恢复了 fixed branch，但仍然没有保住 loss branch。

## 截图说明

- `图片/v607-fixed-retention-dual-cycle-seed-3535.png`
  - Playwright MCP 截取的 v607 HTML 报告。
  - 证明 `status=pass`、`pair_full_observed=False`，并保存本版训练证据的可浏览版本。

## 结论

v607 与 v606 形成互补负结果：一个 loss-only，一个 fixed-only。两者都没有把 required-term pair 合成稳定 pair-full。

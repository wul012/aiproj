# v673 运行截图与解释

## 本版目标

v673 对 v672 dual-boundary checkpoint 运行 forced-choice 诊断，判断 pair-full 是否只发生在自由生成表面，还是内部偏好也匹配 fixed/loss。

## 关键结果

- `status=pass`
- `decision=refresh_forced_choice_internal_pair_match`
- `expected_best_prompt_count=2`
- `forced_choice_full_match_source_count=1`
- `best_internal_sources=dual-boundary-seed-3535`

## 解释

v673 是比 v672 更强的证据：v672 显示自由生成 pair-full，v673 显示内部 forced-choice 也对 fixed/loss 两个 prompt 选择了预期 term。

边界仍然存在：这是单 seed，因此下一步应放入 alignment matrix，再做 route decision 和后续 seed stability。

## 截图说明

- `e/673/图片/v673-dual-boundary-forced-choice.png`: Playwright MCP 截取的 forced-choice HTML 报告，显示 internal pair match。

一句话总结：v673 证明 v672 checkpoint 同时具备生成 pair-full 和内部 pair match 的单 seed 信号。

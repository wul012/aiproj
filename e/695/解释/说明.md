# v695 运行截图和解释：minimal prompt corpus contract

## 本版目标

v695 承接 v694 readiness，把推荐的 `minimal_prompt_equals_surface_objective` 落成可检查语料契约。它仍然不训练模型，目标是保证下一版真实训练使用的 corpus mode 已注册、prompt surface 是 `fixed=` / `loss=`，且不再依赖 contextual answer-bearing anchor。

## 验证命令和结果

生成 corpus contract 证据：

```powershell
python -B scripts\run_model_capability_required_term_pair_minimal_prompt_corpus_contract.py e\694\解释\model-capability-required-term-pair-minimal-prompt-objective-readiness --out-dir e\695\解释\model-capability-required-term-pair-minimal-prompt-corpus-contract --require-pass --force
```

结果：

```text
status=pass
decision=minimal_prompt_equals_surface_corpus_contract_ready
failed_count=0
contract_ready=True
corpus_mode=minimal_prompt_equals_surface_objective
source_prompts=['fixed=', 'loss=']
anchor_hit_count=0
model_quality_claim=corpus_contract_only
```

focused tests：

```powershell
python -m pytest tests\test_model_capability_required_term_pair_minimal_prompt_objective_readiness.py tests\test_model_capability_required_term_pair_minimal_prompt_corpus_contract.py -q -o cache_dir=runs\pytest-cache-v695-focused
```

结果：

```text
10 passed
```

## 截图说明

- `e/695/图片/v695-minimal-prompt-corpus-contract.png`

截图展示 corpus contract HTML 页面，能看到 contract ready、source prompts、anchor hits、sample lines 和 next action。

## 产物说明

正式产物目录：

```text
e/695/解释/model-capability-required-term-pair-minimal-prompt-corpus-contract/
```

主要文件：

- `model_capability_required_term_pair_minimal_prompt_corpus_contract.json`
- `model_capability_required_term_pair_minimal_prompt_corpus_contract.csv`
- `model_capability_required_term_pair_minimal_prompt_corpus_contract.txt`
- `model_capability_required_term_pair_minimal_prompt_corpus_contract.md`
- `model_capability_required_term_pair_minimal_prompt_corpus_contract.html`

这些产物用于保护 v696 真实训练输入，不是训练结果。

## 链路结论

v695 确认：

- corpus mode 已注册。
- `source_prompts` 是 `fixed=` 和 `loss=`。
- 语料含直接目标 `fixed=fixed` / `loss=loss`。
- 语料含 prefix spans `fixed=fix` / `loss=los`。
- `anchor_hit_count=0`，没有沿用上一分支的 answer-bearing contextual anchor。

一句话总结：v695 把 minimal-prompt 训练输入变成可审计 contract，为 v696 真跑 checkpoint 做准备。

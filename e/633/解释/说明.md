# v633 required-term pair generation/internal alignment route decision 运行说明

## 本版目标

v633 基于 v632 比较报告，生成下一步路线约束单：不直接 promotion，而是以 v630 的 generation pair-full 为主线，修复 internal preference。

## 输入

```text
e/632/解释/model-capability-required-term-pair-generation-internal-alignment-comparison/
```

## 决策结果

```text
status=pass
decision=repair_internal_preference_preserve_generation_pair_full
selected_generation_route=loss-internal-joint-cycle
internal_anchor_route=loss-internal-first-token
direct_promotion_ready=False
```

解释：v630 的 generation 最强，v621 的 internal 最强，但没有一条路线同时满足两者。

## 约束

```text
preserve_generation_pair_full
repair_loss_internal_preference
avoid_pair_id_shortcut
```

这些约束会指导后续 corpus/seed 版本：必须保住 v630 的生成 pair-full，同时补回 `loss=` 的 internal expected-best。

## 截图说明

- `图片/v633-generation-internal-alignment-route-decision.png`
  - route decision HTML 截图，证明 selected route、internal anchor 和 promotion boundary。

## 结论

v633 将 v632 的比较结论转成后续开发约束；下一步应训练第二个 joint variant 或内部修复 variant，而不是直接发布能力结论。

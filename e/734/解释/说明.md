# v734 运行截图和解释：bridge comparison

## 本版目标

v734 比较 v729 objective-structure training 和 v733 direct-prompt bridge training。

目标是判断 bridge patch 是否改善 direct hit，并观察 failure shape 是否变化。结果是：没有改善，且引入 loss prompt fixed pollution。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_bridge_comparison.py --objective e\729\解释\model-capability-required-term-pair-readiness-objective-structure-training-run --bridge e\733\解释\model-capability-required-term-pair-readiness-direct-prompt-bridge-training-run --out-dir e\734\解释\model-capability-required-term-pair-readiness-bridge-comparison --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_bridge_no_improvement_introduced_fixed_pollution
objective_default_hit_count=0
bridge_default_hit_count=0
default_hit_delta=0
bridge_pollution_introduced=True
model_quality_claim=comparison_only
```

## 截图说明

- `e/734/图片/v734-bridge-comparison.png`

截图显示 objective 和 bridge 两条路线 hit count 都是 0，但 bridge route 出现 loss->fixed pollution。

## 关键观察

- v731/v732/v733 这条 bridge route 没有提升 direct hits。
- v733 相比 v729 改变了 failure shape。
- `loss=` 重新生成 fixed，说明继续堆 bridge rows 风险较高。
- 下一步应关闭当前 bridge patch，改用另一种 objective surface。

一句话总结：v734 关闭当前 direct-prompt bridge 路线，避免继续在无收益且污染加重的方向上迭代。

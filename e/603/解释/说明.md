# v603 required-term pair fixed-retention objective comparison 运行说明

## 本版目标

v603 对 v600-v602 三条 fixed-retention objective route 做结构化比较，判断哪条路线值得进入后续决策。

## 关键结果

```text
status=pass
decision=fixed_retention_objectives_confirm_branch_tradeoff
compared_report_count=3
pair_full_report_count=0
fixed_only_tradeoff_report_count=1
loss_only_tradeoff_report_count=2
union_hit_terms=fixed,loss
fixed_recovery_route=v601-first-token
```

## 解释

- v600 balanced：loss-only / no pair-full。
- v601 first-token：fixed-only / no pair-full。
- v602 prompt-guard：loss-only / no pair-full。

v601 是唯一恢复 `fixed=` 的路线，但它覆盖了 `loss=`，所以不能直接 promotion。

## 截图说明

- `图片/v603-fixed-retention-objective-comparison.png`
  - Playwright MCP 截取的 v603 comparison HTML。
  - 证明三条真实训练 route 已合并成可审计比较。

## 结论

v603 证明 fixed-retention objective 有效果但仍是 tradeoff。下一步应做 route decision，选择 v601 作为 fixed recovery route，并要求 loss rebalance。

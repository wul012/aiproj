# v591 required-term pair loss-branch dual-anchor seed 3535 运行说明

## 本版目标

v591 使用 `equals_surface_no_pair_id_loss_branch_dual_anchor_repair` 做真实 seed `3535` tiny 训练，验证 dual-anchor clean record 是否能在恢复 `loss` 的同时保留 `fixed`。

## 关键结果

```text
status=pass
training_status=pass
checkpoint_exists=True
pair_full_observed=False
default_continuation_hit_count=1
suppression_continuation_hit_count=1
hit_terms=loss
missed_terms=fixed
```

## 运行解释

dual-anchor corpus 使用：

```text
loss=loss|fixed=fixed
fixed=fixed|loss=loss
```

它比 v590 更强调同一 clean record 内的双分支关系，但 replay 仍然表现为 `loss` 单分支命中。

## 截图说明

- `图片/v591-loss-branch-dual-anchor-seed-3535.png`
  - Playwright MCP 截取的 v591 HTML 报告。
  - 证明 dual-anchor 训练通过，但没有 pair-full。

## 结论

v591 排除了一个简单方向：仅靠 dual-anchor 记录不能在 seed `3535` 上同时保住 `fixed` 和 `loss`。

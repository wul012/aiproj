# v645 required-term pair joint-cycle light-merge seed 3535 运行说明

## 本版目标

v645 运行 v644 的 light-merge corpus，验证它是否能比 v640 heavy internal-repair 更好地保住 generation pair-full。

## 训练配置

```text
seed=3535
corpus_mode=equals_surface_no_pair_id_loss_internal_joint_cycle_light_merge_repair
repeat=320
bridge_repeat=24
max_iters=1800
block_size=16
n_embd=64
learning_rate=0.02
device=cpu
```

## 生成结果

```text
status=pass
decision=required_term_pair_coexistence_refresh_no_pair_full
training_status=pass
checkpoint_exists=True
pair_full_observed=False
```

replay 观察：

```text
fixed= -> los=loss=los
loss=  -> loss=loss=lo
```

解释：light-merge 保住了 loss generation，但丢失 fixed generation，因此是 loss-only，不是 aligned pair-full。

## 截图说明

- `图片/v645-joint-cycle-light-merge-seed-3535.png`
  - 训练/replay HTML 截图，证明 training pass、checkpoint 存在，但 `Pair full observed=False`。

## 结论

v645 说明轻量合并比 heavy repair 更温和，但仍造成 fixed/loss tradeoff；下一步检查 forced-choice internal 是否保持 full match。

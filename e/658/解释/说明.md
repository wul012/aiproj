# v658 运行截图与解释

## 本版目标

v658 收口 v649-v658 这一轮 schedule approximation 批次。它完成三件事：

- 对 v657 loss-guarded checkpoint 做 forced-choice。
- 把 loss-guarded 路线纳入 alignment comparison 和 route decision。
- 生成批次 closeout，决定停止单语料 schedule approximation 分支。

## 关键结果

forced-choice：

- `decision=refresh_forced_choice_partial_internal_match`
- `expected_best_prompt_count=1`
- `forced_choice_full_match_source_count=0`
- `loss` prompt 仍选择 `fixed`

alignment/route：

- `generation_pair_full_count=1`
- `internal_pair_full_count=2`
- `aligned_pair_full_count=0`
- `selected_generation_route=loss-internal-joint-cycle`
- `internal_anchor_route=joint-cycle-internal-repair`

closeout：

- `decision=stop_single_corpus_schedule_approximations`
- next action：停止这条单语料近似分支，后续只考虑真实 checkpoint continuation 或 constrained decoding。

## 截图说明

- `e/658/图片/v658-schedule-approximation-batch-closeout.png`：批次 closeout HTML 截图，显示 stop decision、保留的 generation route 和 internal anchor。

## 证据角色

本版是批次收口，不是新模型能力提升。它把 surface-first 和 loss-guarded 两条单语料近似都记录为负例，并保留 v630/v640 作为分离证据锚点。

一句话总结：v658 明确停止单语料 schedule approximation，项目下一步应转向真正续训能力或解码约束，而不是继续微调类似语料。

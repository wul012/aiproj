# v753 运行截图和解释：exact-surface repair contract patch

## 本版目标

v753 基于 v752 exact-surface repair plan 和 v747 fixed-preserving transfer contract patch，生成 checked exact-surface repair contract patch。

本版不训练模型；它只新增 4 条 near-exact bridge rows，并验证 exact heldout prompt `fixed=|loss=` 没有进入训练 rows。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_exact_surface_repair_contract_patch.py --repair-plan e\752\解释\model-capability-required-term-pair-readiness-exact-surface-repair-plan --base-contract e\747\解释\model-capability-required-term-pair-readiness-fixed-preserving-transfer-contract-patch --out-dir e\753\解释\model-capability-required-term-pair-readiness-exact-surface-repair-contract-patch --require-pass --force
```

输出摘要：

```text
status=pass
decision=pair_readiness_exact_surface_repair_contract_patch_ready
patch_ready=True
base_training_row_count=20
patched_training_row_count=24
exact_surface_repair_row_count=4
model_quality_claim=contract_patch_only
```

## 截图说明

- `e/753/图片/v753-exact-surface-repair-contract-patch.png`

截图显示 base rows 为 20、patched rows 为 24、repair rows 为 4，并列出 heldout prompt 不泄漏和 eval overlap 为空的检查。

## 关键观察

- patch 保留 v747 的 fixed-preserving transfer rows。
- patch 新增 4 条 near-exact pipe/equals bridge rows。
- patch 没有把 `fixed=|loss=` 加入训练 rows。
- materializer 已注册 `pair_readiness_exact_surface_repair_contract_patch_ready`，下一版可以直接 materialize。

一句话总结：v753 将 exact-surface repair plan 落成可物化 contract patch，但模型效果仍需后续 materialization、training 和 independent replay 验证。

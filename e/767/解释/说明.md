# v767 运行截图和解释：objective-level contrast seed stability plan

## 本版目标

v767 消费 v766 promotion guard，生成 objective-level contrast 的 seed stability plan。

本版不训练、不 replay、不接受 checkpoint。它只把 v766 的下一步要求转成可执行计划：继续沿用 v762 corpus 和 v763/v764 的路线，用两个补充 seed 检查 pair-full replay 是否稳定。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_objective_level_contrast_seed_stability_plan.py e\766\解释\model-capability-required-term-pair-readiness-objective-level-contrast-promotion-guard --out-dir e\767\解释\model-capability-required-term-pair-readiness-objective-level-contrast-seed-stability-plan --require-pass --force
```

输出摘要：

```text
status=pass
decision=pair_readiness_objective_level_contrast_seed_stability_plan_ready
failed_count=0
seed_stability_plan_ready=True
source_seed=3636
additional_seed_count=2
minimum_ready_replay_count=2
model_quality_claim=plan_only
next_action=run seed 3737 training and replay first, then seed 3838
```

## 截图说明

- `e/767/图片/v767-objective-level-contrast-seed-stability-plan.png`

截图确认 HTML 页面可见：

- `Status pass`
- `Decision pair_readiness_objective_level_contrast_seed_stability_plan_ready`
- `Source seed 3636`
- `Extra seeds 2`
- `Required Next Artifacts`

## 关键观察

- v767 继承 v766 的保守判断：objective-level contrast 只能进入 seed stability，不能直接接受。
- 计划明确补充 seed 为 `3737` 和 `3838`。
- acceptance rule 要求至少两个 seed replay pair-full，且不能出现 seed 退化到零 exact hit。
- non-goals 明确禁止复用 v763 checkpoint、只看 training direct hit、或在稳定性验证中改 corpus/model size。

一句话总结：v767 把 objective-level contrast 的后续验证从口头建议落成可执行 seed stability plan。

# v629 required-term pair loss-internal joint-constraint corpus 运行说明

## 本版目标

v629 接在 v628 之后，针对 “fixed bridge 修复 `fixed=` 但破坏 `loss=`” 的问题，新增两个 joint constraint corpus mode。

本版只做 corpus contract，不做真实训练；真实 seed 运行从 v630 开始。

## 新增模式

```text
equals_surface_no_pair_id_loss_internal_joint_cycle_repair
equals_surface_no_pair_id_loss_internal_balanced_anchor_repair
```

## 合约结果

```text
status=pass
decision=loss_internal_joint_constraint_corpus_modes_ready
registered_mode_count=6
new_joint_modes=2
pair_id_removed=True
```

解释：新模式把 `fixed=` / `loss=` 生成锚点和 internal preference 锚点放回同一类干净样本里，避免 v627/v628 的单边 fixed bridge 继续制造 tradeoff。

## 截图说明

- `图片/v629-loss-internal-joint-constraint-corpus-contract.png`
  - 合约 HTML 截图，证明两个 joint constraint mode 已注册，并明确边界：这只是 corpus contract，不是 checkpoint 结果。

## 结论

v629 把下一轮训练的候选 corpus 从单边桥接推进到联合约束；后续要用 v630/v631 验证它是否同时保住 generation 和 forced-choice internal preference。

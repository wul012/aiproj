# v624 required-term pair loss-internal forced-choice diagnostic 运行说明

## 本版目标

v624 对 v620-v622 三个真实 checkpoint 运行 teacher-forced forced-choice scoring。

本版不训练新 checkpoint；目标是区分“生成没有 pair-full”和“模型内部完全没有固定/损失偏好”这两种情况。

## 输入证据

```text
v620 loss-internal-preference seed 3535
v621 loss-internal first-token seed 3535
v622 loss-internal ranked-choice seed 3535
```

## 结果

```text
status=pass
decision=refresh_forced_choice_internal_pair_match
expected_best_prompt_count=4
forced_choice_full_match_source_count=1
best_internal_sources=loss-internal-first-token
```

解释：

- v621 在生成 replay 上是 loss-only tradeoff。
- 但 forced-choice 内部评分显示，v621 对 `fixed=` 和 `loss=` 都偏好正确候选。
- 这说明下一步应该做 internal preference -> generation 的桥接，而不是继续盲目换语料。

## 截图说明

- `图片/v624-loss-internal-forced-choice-diagnostic.png`
  - Playwright MCP 截取的 forced-choice HTML。
  - 证明 `decision=refresh_forced_choice_internal_pair_match` 和 `Full match=1`。

## 结论

v624 是本批次第一个强信号：模型内部已经出现 pair match，只是生成路径没有释放出来。

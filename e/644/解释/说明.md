# v644 required-term pair joint-cycle light-merge corpus 运行说明

## 本版目标

v644 根据 v643 路线决策新增 `joint-cycle light-merge` corpus mode。它比 v639 的 internal-repair 更轻：先保 generation rows，再少量加入 internal rank rows。

本版只做 corpus contract，不做真实训练；真实 seed 运行从 v645 开始。

## 新增模式

```text
equals_surface_no_pair_id_loss_internal_joint_cycle_light_merge_repair
```

## 合约结果

```text
status=pass
decision=joint_cycle_light_merge_corpus_ready
registered_mode_count=8
pytest=9 passed in 0.08s
```

解释：light-merge 明确移除了 v639 里较重的 teacher-forced rows，避免再次破坏 v630 的 generation surface。

## 截图说明

- `图片/v644-joint-cycle-light-merge-corpus-contract.png`
  - 合约 HTML 截图，证明新 mode 已注册，真实 checkpoint 证据从 v645 开始。

## 结论

v644 是 v643 路线决策的轻量实现；下一步要用真实训练验证它是否比 v639 更能保 generation。

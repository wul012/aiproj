# v901 运行截图与解释

## 本版目标

v901 review v900 的 tokenizer-covered holdout replay 结果。v900 真实 checkpoint 5/5 通过，但 v898 suite 的 prompt 明确包含 `fixed/loss`，因此本版检查 target leakage，判断能否批准 promotion。

## 关键结论

- `status=pass`
- `source_holdout_model_quality_ready=True`
- `case_count=5`
- `target_leakage_case_count=5`
- `target_hidden_case_count=0`
- `approved_for_promotion=False`
- `promotion_ready=False`
- `model_quality_claim=target_leaked_holdout_pass_review_only`
- `next_step=build_target_hidden_tokenizer_covered_holdout_suite`

## 解释

v900 的 5/5 replay 结果有效，但它只能说明模型在包含目标词的 tokenizer-covered prompts 上能延续生成 `fixed/loss`。由于 5 条 prompt 都泄露 expected terms，本版不能把它批准为 route promotion 证据。

下一步应构建 target-hidden tokenizer-covered holdout suite：prompt 仍然必须被 v890 tokenizer 覆盖，但不能直接包含 `fixed` 或 `loss`。

## 验证命令

```powershell
python scripts\review_bounded_objective_loss_signal_bridge_target_only_memory_tokenizer_coverage_aware_holdout_replay.py --real-replay e\900\解释\bounded-objective-loss-signal-bridge-target-only-memory-tokenizer-coverage-aware-holdout-real-replay --holdout-suite e\898\解释\bounded-objective-loss-signal-bridge-target-only-memory-tokenizer-coverage-aware-holdout-suite --out-dir e\901\解释\bounded-objective-loss-signal-bridge-target-only-memory-tokenizer-coverage-aware-holdout-replay-review --require-review-ready --force
```

## 一句话总结

v901 保留 v900 的 5/5 正向事实，但因 prompt target leakage 阻断 promotion。

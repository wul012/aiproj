# v934 randomized holdout publication registry lookup packet 运行说明

## 本版目标

v934 消费 v933 manifest review，生成 governance lookup packet：

```text
v933 manifest review
  -> v934 registry lookup packet
```

它把已复核的 bounded publication entry 转成后续查询入口可消费的 lookup packet，同时继续拒绝 production promotion。

明确不做：

- 不重新训练。
- 不重新 replay。
- 不改变 v933 review 结论。
- 不批准 production promotion。

## 输入和输出

输入：

```text
e/933/解释/randomized-holdout-publication-registry-manifest-review
```

输出：

```text
e/934/解释/randomized-holdout-publication-registry-lookup-packet
```

截图：

```text
e/934/图片/v934-randomized-holdout-publication-registry-lookup-packet.png
```

## 实际结果

真实 v933 输入运行结果：

```text
status=pass
decision=randomized_holdout_publication_registry_lookup_packet_ready
failed_count=0
randomized_holdout_publication_registry_lookup_packet_ready=True
lookup_packet_id=randomized-holdout-publication-registry-lookup-packet-v934
lookup_scope=governance_lookup_only
entry_count=1
lookup_ready=True
bounded_publication_accepted=True
promotion_ready=False
consumer_boundary=governance_lookup_only
allowed_use=governance_lookup_only
rejected_use=production_promotion
next_step=check_randomized_holdout_publication_registry_lookup_packet
passed_check_count=18
failed_check_count=0
```

## 截图验证

Playwright MCP 已保存 HTML 快照和截图。页面可见：

- `Status = pass`
- `Packet ready = True`
- `Lookup = True`
- `Promotion = False`
- `Failed = 0`
- lookup key 为 `publication:randomized-holdout-publication-v928`

## 一句话总结

v934 把 v933 的 lookup-only review 转成可查询 packet，让 bounded randomized holdout publication 有了稳定 lookup key，同时仍然不允许 production promotion。

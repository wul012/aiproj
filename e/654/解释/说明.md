# v654 运行截图与解释

## 本版目标

v654 基于 v653 的全矩阵重新生成路线决策，判断 surface-first schedule 是否改变主线选择。

## 关键结果

- `status=pass`
- `decision=repair_internal_preference_preserve_generation_pair_full`
- `selected_generation_route=loss-internal-joint-cycle`
- `internal_anchor_route=joint-cycle-internal-repair`
- `direct_promotion_ready=False`

## 解释

surface-first schedule 没有进入 selected route。路线仍然是：

- 用 v630 `loss-internal-joint-cycle` 保留生成 pair-full。
- 用 v640/v641 `joint-cycle-internal-repair` 作为内部 forced-choice 锚点。
- 不直接 promotion，因为 aligned pair-full 仍为 0。

## 截图说明

- `e/654/图片/v654-route-decision-with-surface-first-schedule.png`：route decision HTML 截图，显示 generation route、internal anchor 和 promotion-ready 状态。

## 证据角色

本版把 v650-v652 的探索收束到路线层：surface-first 近似是负分支，不应继续作为主线扩展；后续更有价值的是做失败原因汇总和阶段隔离建议。

一句话总结：v654 保持 v648 的主线判断不变，surface-first schedule 被记录为实验负例而非新候选。

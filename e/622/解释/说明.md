# v622 required-term pair loss-internal ranked-choice seed 3535 运行说明

## 本版目标

v622 运行 v619 的第三条 objective：`equals_surface_no_pair_id_loss_internal_ranked_choice_repair`。

本版是真实 tiny 训练，目标是验证把 forced-choice candidate ranking 写进训练语料后，是否能同时保住 `fixed=` 和 `loss=`。

## 训练配置

```text
seed=3535
corpus_mode=equals_surface_no_pair_id_loss_internal_ranked_choice_repair
repeat=320
bridge_repeat=24
max_iters=1800
block_size=16
n_embd=64
learning_rate=0.02
device=cpu
```

## 结果

```text
status=pass
decision=required_term_pair_coexistence_refresh_no_pair_full
training_status=pass
checkpoint_exists=True
pair_full_observed=False
```

replay 观察：

```text
fixed= -> fixed= fixe
loss=  -> fixed= fixe
```

解释：ranked-choice 版本保住 fixed，但没有恢复 loss；它与 v620 类似，仍是 fixed-only tradeoff。

## 截图说明

- `图片/v622-loss-internal-ranked-choice-seed-3535.png`
  - Playwright MCP 截取的训练/replay HTML。
  - 证明 training pass、checkpoint 存在、pair-full 仍为 `False`。

## 结论

v622 证明“候选排序文本”没有直接转化为 `loss=` 生成能力，下一步需要比较 v620-v622 的 tradeoff。

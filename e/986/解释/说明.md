# v986 运行截图和解释

## 本版目标

v986 审阅 v985 的 receipt index，确认它可以进入后续 lookup-only receipt flow。它解决的是“v985 index 虽然 ready，但是否仍保留源证据、digest、路径和 no-promotion 边界”的问题。

本版不做模型训练，不改变 checkpoint，不扩大模型能力声明，也不允许 production promotion。

## 输入与输出

输入：

```text
e/985/解释/publication-receipt-index-v985/
```

输出：

```text
e/986/解释/publication-receipt-index-review-v986/
```

关键产物：

```text
randomized_holdout_publication_receipt_packet_index_publication_receipt_index_review_v986.json
randomized_holdout_publication_receipt_packet_index_publication_receipt_index_review_v986.csv
randomized_holdout_publication_receipt_packet_index_publication_receipt_index_review_v986.txt
randomized_holdout_publication_receipt_packet_index_publication_receipt_index_review_v986.md
randomized_holdout_publication_receipt_packet_index_publication_receipt_index_review_v986.html
```

## 关键结果

```text
status=pass
decision=randomized_holdout_publication_receipt_packet_index_publication_receipt_index_review_v986_ready
review_ready=True
review_status=approved_for_publication_receipt_index_receipt_lookup_only
receipt_index_row_count=1
source_evidence_count=2
lookup_key_count=1
receipt_ready=True
lookup_ready=True
contract_check_ready=True
promotion_ready=False
passed_check_count=25
failed_check_count=0
next_step=record_randomized_holdout_publication_receipt_packet_index_publication_receipt_index_receipt_v986
```

## 运行截图

Playwright MCP 截图：

```text
e/986/图片/v986-randomized-holdout-publication-receipt-packet-index-publication-receipt-index-review.png
```

Playwright 快照：

```text
e/986/解释/v986-playwright-snapshot.md
```

截图用于确认 HTML 报告可打开，并且页面上能看到 review ready、receipt ready、lookup key count、source evidence、failed count 和 checks 表格。

## 验证命令

本版使用：

```text
python -m py_compile src/minigpt/randomized_holdout_publication_receipt_packet_index_publication_receipt_index_review_v986.py src/minigpt/randomized_holdout_publication_receipt_packet_index_publication_receipt_index_review_v986_artifacts.py scripts/review_randomized_holdout_publication_receipt_packet_index_publication_receipt_index_v986.py tests/test_randomized_holdout_publication_receipt_packet_index_publication_receipt_index_review_v986.py
python -m pytest tests/test_randomized_holdout_publication_receipt_packet_index_publication_receipt_index_v985.py tests/test_randomized_holdout_publication_receipt_packet_index_publication_receipt_index_review_v986.py -q -o cache_dir=runs/pytest-cache-v986-focus
python -B scripts/check_source_encoding.py --out-dir runs/source-encoding-hygiene-v986
python -m pytest -q -o cache_dir=runs/pytest-cache-v986-full
git diff --check
```

验证结果：

```text
focused tests: 12 passed
source encoding: status=pass, source_count=1806, syntax_error_count=0
full pytest: 2300 passed in 236.58s
git diff --check: pass
```

## 一句话总结

v986 把 v985 receipt index 从“可查索引”推进为“经过 review 批准、仍保持 lookup-only 与 no-promotion 的后续 receipt 入口”。

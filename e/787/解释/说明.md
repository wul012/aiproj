# v787 pair capacity sweep summary split

## 目标

v787 是维护拆分版本，不新增 capacity sweep 功能。目标是把 `model_capability_required_term_pair_capacity_sweep.py` 中的 summary、decision 和 interpretation helper 拆到独立模块，让原文件专注输入选择、训练执行和 probe 编排。

## 关键变化

- 新增 `src/minigpt/model_capability_required_term_pair_capacity_sweep_summary.py`。
- `src/minigpt/model_capability_required_term_pair_capacity_sweep.py` 从 616 行降到 417 行。
- 原模块保留 corpus 写入、训练调用、probe 调用和输入问题检查。
- 新模块承接 variant/pair summary、capacity sweep decision、source baseline、model quality claim、interpretation reason 和 next action。

## 验证

- `python -m py_compile src\minigpt\model_capability_required_term_pair_capacity_sweep.py src\minigpt\model_capability_required_term_pair_capacity_sweep_summary.py`
- `python -m pytest tests\test_model_capability_required_term_pair_capacity_sweep.py -q -o cache_dir=runs\pytest-cache-v787-focused`
- `python -B scripts\check_source_encoding.py --out-dir runs\source-encoding-hygiene-v787`
- `git diff --check`

结果：focused 测试 `4 passed`，source encoding `status=pass`，diff check 无错误。

## 截图

- `e/787/图片/v787-pair-capacity-sweep-summary-split.png`

## 一句话总结

v787 把 pair capacity sweep 的解释和决策层拆出，保留训练副作用在原编排模块内，降低实验文件维护风险。

# v626 required-term pair loss-internal decode bridge check 运行说明

## 本版目标

v626 检查 v621 的“内部 pair match 但生成未 pair-full”到底缺在哪个 term。

本版不训练新 checkpoint；它为 v627 的 bridge corpus 提供精确目标。

## 输入证据

```text
v621 loss-internal first-token refresh
v624 loss-internal forced-choice diagnostic
v625 loss-internal route decision
```

## 结果

```text
status=pass
decision=loss_internal_decode_bridge_gap_confirmed
selected_source=loss-internal-first-token
generation_hit_terms=loss
forced_choice_expected_best_terms=fixed,loss
bridge_gap_terms=fixed
decode_bridge_ready=True
```

解释：

- v621 内部对 fixed/loss 都有正确偏好。
- 生成上只命中 loss。
- 缺口是 `fixed`，不是 `loss`。

## 截图说明

- `图片/v626-loss-internal-decode-bridge-check.png`
  - Playwright MCP 截取的 decode bridge check HTML。
  - 证明 selected route、gap terms 和 ready 状态。

## 结论

v626 把 bridge objective 的目标收窄为：在保留 loss internal preference 的同时恢复 fixed generation。

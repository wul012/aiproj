# v737 运行截图和解释：direct-completion surface corpus materialization

## 本版目标

v737 将 v736 的 direct-completion surface contract 物化为真实训练 corpus 和 heldout eval fixture。

本版不新增代码、不训练模型，只验证 v736 的 contract 能进入现有 materializer，并产出下一版训练所需输入。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_corpus_materialization.py e\736\解释\model-capability-required-term-pair-readiness-direct-completion-surface-contract --out-dir e\737\解释\model-capability-required-term-pair-readiness-direct-completion-surface-corpus-materialization --repeat 320 --require-pass --force
```

输出摘要：

```text
status=pass
decision=pair_readiness_corpus_materialized
failed_count=0
materialization_ready=True
training_line_count=5120
evaluation_probe_count=3
model_quality_claim=data_artifact_only
```

## 截图说明

- `e/737/图片/v737-direct-completion-surface-corpus-materialization.png`

截图显示 materialized paths、`Train lines=5120`、`Eval probes=3` 和 corpus preview。

## 关键观察

- 16 行 contract 以 `repeat=320` 展开为 5120 行训练 corpus。
- preview 的前两行是 `fixed=fixed` 与 `loss=loss`，说明 v736 的 direct-completion surface 已经进入真实语料。
- heldout fixture 继续保留 `fixed=`、`loss=`、`fixed=|loss=` 三个 eval probes。

一句话总结：v737 将 direct-completion surface contract 落成可训练输入，为 v738 真实 tiny 训练提供数据闭环。

# v621 required-term pair loss-internal first-token seed 3535 运行说明

## 本版目标

v621 运行 v619 的第二条 objective：`equals_surface_no_pair_id_loss_internal_first_token_repair`。

本版是真实 tiny 训练，目标是验证更直接的 first-token 训练是否能让 `loss=` 先选择 `l`。

## 训练配置

```text
seed=3535
corpus_mode=equals_surface_no_pair_id_loss_internal_first_token_repair
repeat=320
bridge_repeat=24
max_iters=1800
block_size=16
n_embd=64
learning_rate=0.02
device=cpu
```

## 结果

```text
status=pass
decision=required_term_pair_coexistence_refresh_no_pair_full
training_status=pass
checkpoint_exists=True
pair_full_observed=False
```

replay 观察：

```text
fixed= -> l=lossssssss
loss=  -> loss=losssss
```

解释：first-token objective 成功恢复 `loss=`，但把 `fixed=` 拉坏成 loss 分支；这是 loss-only tradeoff，不是 pair-full。

## 截图说明

- `图片/v621-loss-internal-first-token-seed-3535.png`
  - Playwright MCP 截取的训练/replay HTML。
  - 证明 training pass、checkpoint 存在、pair-full 仍为 `False`。

## 结论

v621 说明 first-token 训练能改变 loss 分支，但仍需要后续版本比较它与 v620、v622 的 tradeoff。

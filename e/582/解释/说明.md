# v582 required-term pair branch-binding comparison 运行说明

## 本版目标

v579 和 v581 都是真实训练，但都没有恢复 seed `3535`。v582 把它们和 v571 的旧 loss-balanced route 放到同一个 comparison 中，判断新 branch-binding 是否至少保留了旧路线的 partial signal。

本版不训练、不改代码。它只读三份稳定性报告。

## 运行命令

```powershell
python scripts\run_model_capability_required_term_pair_equals_surface_repair_comparison.py `
  e\571\解释\model-capability-required-term-pair-route-fresh-seed-3535 `
  e\579\解释\model-capability-required-term-pair-branch-binding-seed-3535 `
  e\581\解释\model-capability-required-term-pair-branch-binding-no-space-seed-3535 `
  --labels v571-loss-balanced v579-branch-binding v581-branch-binding-no-space `
  --out-dir e\582\解释\model-capability-required-term-pair-branch-binding-comparison `
  --force `
  --require-pass
```

## 关键结果

```text
status=pass
decision=required_term_pair_equals_surface_repair_comparison_recorded
compared_report_count=3
pair_full_profile_seed_count=0
union_hit_terms=fixed
fixed_hit_reports=v571-loss-balanced
loss_hit_reports=[]
```

v579/v581 没有超过 v571，甚至没有保住 v571 的 partial fixed hit。

## 截图说明

- `图片/v582-branch-binding-comparison.png`
  - Playwright MCP 截取的 comparison HTML。
  - 证明三份 source report、summary 和 branch row 可见。

## 结论

v582 把 branch-binding v1/v2 定性为当前回退路线。下一步应生成 route decision：停止这两条 branch-binding 变体，除非新增更强约束的 objective。

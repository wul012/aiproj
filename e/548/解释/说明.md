# v548 required-term pair seed config selection 运行说明

## 本版目标

v548 承接 v547 的 seed coverage tradeoff，不重新训练，而是把 union coverage 转成明确的 per-seed config-selection policy。

本版重点不是证明模型已经稳定，而是确认：如果允许按 seed 选择配置，是否每个 seed 都能找到一个在源报告里真实 `pair_full=True` 的配置。

## 输入证据

- `e/547/解释/model-capability-required-term-pair-seed-coverage-tradeoff/model_capability_required_term_pair_seed_coverage_tradeoff.json`

## 运行命令

```powershell
python -B scripts\run_model_capability_required_term_pair_seed_config_selection.py `
  e\547\解释\model-capability-required-term-pair-seed-coverage-tradeoff `
  --out-dir e\548\解释\model-capability-required-term-pair-seed-config-selection `
  --force `
  --require-pass
```

## 核心结果

```text
status=pass
decision=required_term_pair_seed_config_selection_multi_config_ready
seed_count=3
selection_ready_seed_count=3
selected_config_count=2
requires_multi_config_policy=True
```

seed 选择策略：

| Seed | Selected config | Ready |
| ---: | --- | --- |
| 535 | v544-topk2-t080 | true |
| 1535 | v546-loss-calibrated-topk2-t080 | true |
| 2535 | v544-topk2-t080 | true |

## 输出产物

- `model_capability_required_term_pair_seed_config_selection.json`
- `model_capability_required_term_pair_seed_config_selection.csv`
- `model_capability_required_term_pair_seed_config_selection.txt`
- `model_capability_required_term_pair_seed_config_selection.md`
- `model_capability_required_term_pair_seed_config_selection.html`
- `playwright-snapshot.md`

## 截图说明

- `e/548/图片/01-model-capability-required-term-pair-seed-config-selection.png`
  - 展示每个 seed 的 selected config、ready 状态和需要两个配置共同完成 coverage。
  - 证明 policy 不是口头 fallback，而是可以被审阅和后续脚本读取的 artifact。

## 验证说明

验证包括：

- 新增 policy builder、artifact writer、CLI 单测。
- 真实读取 v547 tradeoff report 并生成 v548 policy。
- Playwright MCP HTML 截图和 snapshot。
- 全量 pytest、source encoding、`git diff --check` 和 GitHub Actions。

一句话总结：v548 把 v547 的 seed-level tradeoff 转成了显式 per-seed config-selection policy，下一步应验证该 policy 是否能迁移到 held-out prompts 或 fresh seeds。

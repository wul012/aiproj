# v709 运行截图和解释：pair-readiness loss-retention repair plan

## 本版目标

v709 根据 v708 的诊断结果制定 loss-retention repair plan。它不训练模型，只把下一步应该 patch contract 的方向写清楚。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_loss_retention_repair_plan.py e\708\解释\model-capability-required-term-pair-readiness-heldout-failure-diagnostic --out-dir e\709\解释\model-capability-required-term-pair-readiness-loss-retention-repair-plan --require-pass --force
```

输出：

```text
status=pass
decision=pair_readiness_loss_retention_repair_plan_ready
plan_ready=True
proposed_next_artifact=pair_readiness_loss_retention_contract_patch
repair_focus=restore loss direct retention without sacrificing fixed direct retention
model_quality_claim=plan_only
```

## 计划内容

contract patch 方向：

```text
duplicate loss prefix progression rows
add loss anti-fixed contamination rows
keep fixed direct rows unchanged
keep heldout pair probe excluded from training
```

## 截图说明

- `e/709/图片/v709-pair-readiness-loss-retention-repair-plan.png`

截图展示 checks、contract patch 和 next action。

## 结论

v709 不是继续盲训，而是把 v708 的明确失败归因转成修复计划：保住 fixed 的同时，增强 loss retention，下一步生成 contract patch。

一句话总结：v709 把 loss prompt fixed-pollution 诊断转成 loss-retention contract patch 计划。

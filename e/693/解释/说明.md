# v693 运行截图和解释：surface branch final verification

## 本版目标

v693 是 v679-v692 surface-policy 批次的终检版。它不新增模型功能，不训练 checkpoint，不再扩展报告链，只做三件事：

- 跑全量测试。
- 跑 source encoding hygiene。
- 归档本批次最终验证结果。

## 验证命令和结果

全量测试：

```powershell
python -m pytest -q -o cache_dir=runs\pytest-cache-v693-full
```

结果：

```text
1335 passed in 166.24s (0:02:46)
```

源码编码检查：

```powershell
python -B scripts\check_source_encoding.py --out-dir runs\source-encoding-hygiene-v693
```

结果：

```text
status=pass
decision=continue_with_clean_sources
source_count=815
clean_count=815
bom_count=0
syntax_error_count=0
compatibility_error_count=0
target_python=3.11
```

Git whitespace 检查：

```powershell
git diff --check
```

结果：通过。

## 截图说明

- `e/693/图片/v693-final-verification.png`

截图展示 v693 final verification HTML 页面，集中列出 pytest、source encoding、diff check 和本批次结论。

## 产物说明

正式产物目录：

```text
e/693/解释/
```

主要文件：

- `v693-final-verification.html`：浏览器截图入口。
- `说明.md`：本说明文件。

临时验证输出位于：

```text
runs/source-encoding-hygiene-v693/
runs/pytest-cache-v693-full/
```

这些属于本地验证缓存/报告，不作为主要归档证据提交；最终提交只保留 `e/693` 的验证说明和截图。

## 批次结论

v679-v692 已经把 required-term pair surface 分支收口为：

- contextual decode aid 可用。
- 默认推荐 variant 是 `space_context_control`。
- 最小稳定 budget 是 8。
- 五个 contextual surface variants 全稳定。
- non-leaking baseline 仍不稳定。
- 不允许 promotion。
- 如果继续追模型能力，应新开 minimal-prompt objective。

一句话总结：v693 用全量测试和编码检查确认本批次代码与证据链可收口，最终结论仍是 contextual decode aid，而不是模型 baseline。

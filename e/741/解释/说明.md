# v741 运行截图和解释：pair prompt transfer repair plan

## 本版目标

v741 读取 v740 pair-probe replay 的 not-ready 结果，生成下一步 pair prompt transfer repair plan。

本版不训练、不写 contract patch，只规划一个非泄漏修复方向：用 surrogate pair-transfer rows 修复 pair prompt transfer，但不把 exact heldout `fixed=|loss=` 放进训练集。

## 运行命令和结果

```powershell
python -B scripts\run_model_capability_required_term_pair_readiness_pair_prompt_transfer_repair_plan.py e\740\解释\model-capability-required-term-pair-readiness-direct-completion-pair-probe-replay --out-dir e\741\解释\model-capability-required-term-pair-readiness-pair-prompt-transfer-repair-plan --require-pass --force
```

输出摘要：

```text
status=pass
decision=pair_readiness_pair_prompt_transfer_repair_plan_ready
failed_count=0
plan_ready=True
proposed_next_artifact=pair_readiness_pair_prompt_transfer_contract_patch
repair_requirement_count=5
model_quality_claim=plan_only
```

## 截图说明

- `e/741/图片/v741-pair-prompt-transfer-repair-plan.png`

截图显示本版 plan ready，并列出了 5 条修复要求。

## 关键观察

- v741 关闭了“v738 可直接 promotion”的说法。
- 下一步 contract patch 必须保留 direct rows，同时加入非 heldout 的 pair-transfer rows。
- `fixed=|loss=` 继续作为 heldout pair prompt，不能作为训练行。

一句话总结：v741 将 v740 的负结果转为非泄漏 pair-transfer contract patch 计划，为下一轮训练修复 pair prompt 迁移做准备。

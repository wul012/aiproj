# v662 运行截图与解释

## 本版目标

v662 从 v630 checkpoint 做第二条真实续训，这次使用更温和的 light-merge 语料、较低学习率和更少 continuation 步数，测试是否能少破坏 v630 的 generation pair-full。

## 关键结果

- `status=pass`
- `training_mode=checkpoint_continuation`
- `resume_checkpoint_exists=True`
- `decision=required_term_pair_coexistence_refresh_no_pair_full`
- `pair_full_observed=False`

## Replay 观察

- `fixed=` 未命中，输出 `=lossssssss`。
- `loss=` 命中，输出 `lossssssssss`。

较温和的 continuation 仍退化为 loss-only，没有保住 `fixed`。

## 截图说明

- `e/662/图片/v662-v630-light-merge-resume.png`：refresh HTML 截图，显示续训通过但 pair-full 为 0。

一句话总结：v662 证明降低学习率和使用 light-merge 仍无法保住 v630 的 pair-full，续训分支需要收束。

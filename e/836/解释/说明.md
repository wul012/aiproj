# v836 运行截图和解释：bounded objective contract

## 本版目标

v836 接住 v835 的计划产物，把“先定义目标契约再训练”的要求落成可被后续 seed builder 消费的 contract JSON。它仍然不训练模型，不声明能力提升，只把下一版 seed 的边界、case、holdout 和禁止路径固定下来。

## 运行证据

- 输入：`e/835/解释/model-capability-route-promotion-bounded-objective-intervention-plan`
- 输出目录：`e/836/解释/model-capability-route-promotion-bounded-objective-contract`
- 截图：`e/836/图片/v836-bounded-objective-contract-html.png`

## 关键结果

```text
status=pass
decision=model_capability_route_promotion_bounded_objective_contract_ready
bounded_objective_contract_ready=True
contract_id=bounded_fixed_loss_direct_completion_contract
target_terms=fixed,loss
contract_case_count=3
planned_seed_example_count=18
unchanged_suite_check_required=True
promotion_claim_allowed=False
proposed_next_artifact=model_capability_route_promotion_bounded_objective_seed
model_quality_claim=contract_only
next_action=model_capability_route_promotion_bounded_objective_seed
```

## 截图说明

Playwright MCP 打开的 HTML 页面展示了 Objective Contract、Contract Cases、Seed Blueprint、Holdout Rule 和 Blocked Surfaces。截图证明浏览器可读的证据链已生成，而不是只在 JSON 中存在。

## 边界

v836 只定义契约。它阻断 forced-prefix、carry-forward fragment 和无目标加 epoch 的路线，并要求保留 unchanged v803 bounded suite 作为后续 dual replay 的 holdout。

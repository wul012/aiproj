# v867 运行截图和解释

## 本版目标

v867 诊断 v866 的 pair-binding zero-hit 回退，确认模型为什么没有输出 `fixed` 或 `loss`。

本版不训练、不 replay 新 checkpoint、不 promotion。

## 输入

```text
e/866/解释/bounded-objective-loss-signal-bridge-pair-binding-replay-comparison/
```

## 输出

```text
e/867/解释/bounded-objective-loss-signal-bridge-pair-binding-zero-hit-diagnostic/
```

截图：

```text
e/867/图片/v867-bounded-objective-loss-signal-bridge-pair-binding-zero-hit-diagnostic-html.png
```

## 真实结果

```text
status=pass
decision=bounded_objective_loss_signal_bridge_pair_binding_zero_hit_label_echo
zero_hit_diagnostic_ready=True
case_count=3
label_echo_case_count=3
zero_hit_case_count=3
all_cases_label_echo=True
model_quality_claim=label_echo_regression
next_action=build_single_line_completion_surface_patch
```

## Case 诊断

| Case | Continuation | 诊断 |
| --- | --- | --- |
| `canonical_direct_completion` | `answer:` | label echo |
| `minimal_direct_completion` | `answer:` | label echo |
| `completion_label_surface` | `ans` | label echo |

三个 case 都先输出标签碎片，而不是目标词。

## Root causes

```text
label_echo_over_target_terms
partial_signal_regressed_to_zero_hit
no_anchor_failure_needs_surface_repair
short_decode_label_fragment
```

## 下一步

下一步不是继续堆 pair-binding 样本，而是构建 single-line completion surface patch，减少多行标签结构对模型的干扰。

## 验证

```text
py_compile: pass
focused pytest: 3 passed
real diagnostic: pass, require-diagnostic-ready
Playwright MCP: snapshot and full-page screenshot saved
```

后续还会跑全量 pytest、source encoding 和 `git diff --check` 后再提交。

## 一句话总结

v867 把 v866 的 zero-hit 回退诊断为 label echo regression，下一步应修 surface 格式而不是继续同类训练。

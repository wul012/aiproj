# v592 required-term pair loss-branch micro-span seed 3535 运行说明

## 本版目标

v592 使用 `equals_surface_no_pair_id_loss_branch_micro_span_repair` 做真实 seed `3535` tiny 训练。它验证 first-token/micro-span 提示是否能恢复 `loss` 并保留 `fixed`。

## 关键结果

```text
status=pass
training_status=pass
checkpoint_exists=True
pair_full_observed=False
default_continuation_hit_count=1
suppression_continuation_hit_count=1
hit_terms=loss
missed_terms=fixed
```

生成预览：

```text
fixed=los=los=los=
loss=los=loss=los
```

## 截图说明

- `图片/v592-loss-branch-micro-span-seed-3535.png`
  - Playwright MCP 截取的 v592 HTML 报告。
  - 证明 micro-span 训练通过，但仍然没有 pair-full。

## 结论

v592 说明 first-token/micro-span 可以强化 loss continuation，但仍然会把 fixed 压掉，当前三条 loss-branch objective 都是 tradeoff 证据。

# v620 required-term pair loss-internal-preference seed 3535 运行说明

## 本版目标

v620 运行 v619 的第一条 objective：`equals_surface_no_pair_id_loss_internal_preference_repair`。

本版是真实 tiny 训练，不只是 contract。目标是观察“forced-choice 偏好行写进语料”是否能让 `loss=` 生成 loss。

## 训练配置

```text
seed=3535
corpus_mode=equals_surface_no_pair_id_loss_internal_preference_repair
repeat=320
bridge_repeat=24
max_iters=1800
block_size=16
n_embd=64
learning_rate=0.02
device=cpu
```

## 结果

```text
status=pass
decision=required_term_pair_coexistence_refresh_no_pair_full
training_status=pass
checkpoint_exists=True
default_pair_full_variant_count=0
suppression_pair_full_variant_count=0
pair_full_observed=False
```

replay 观察：

```text
fixed= -> fixed= fixe
loss=  -> fixed=fixed=
```

解释：训练本身成功，但 `loss=` 仍然漂到 fixed，说明只加入“内部偏好描述行”还不足以改变 tiny checkpoint 的生成偏向。

## 截图说明

- `图片/v620-loss-internal-preference-seed-3535.png`
  - Playwright MCP 截取的训练/replay HTML。
  - 证明 training pass、checkpoint 存在、pair-full 为 `False`。

## 结论

v620 给出第一条 loss-internal-preference objective 的真实负结果：它保住 fixed，但没有恢复 loss。

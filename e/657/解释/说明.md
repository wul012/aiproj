# v657 运行截图与解释

## 本版目标

v657 用 v656 的 loss-guarded schedule corpus 做真实 seed 3535 训练，验证 loss guard 能否修正 v655 的 fixed-only collapse。

## 关键结果

- `status=pass`
- `training_status=pass`
- `checkpoint_exists=True`
- `decision=required_term_pair_coexistence_refresh_no_pair_full`
- `pair_full_observed=False`
- `default_pair_full_variant_count=0`
- `suppression_pair_full_variant_count=0`

## Replay 观察

- `fixed=` 未命中：输出 `fixed= lixelixelxe`。
- `loss=` 未命中：输出 `loss=ssssssssssss`。

loss guard 没有把 `loss` 修回来，也破坏了原有的 `fixed` 命中。它不是 fixed-only，而是进入了更差的 no-hit/字符漂移状态。

## 截图说明

- `e/657/图片/v657-loss-guarded-schedule-seed-3535.png`：HTML 报告截图，显示训练通过、checkpoint 存在，但 pair-full 仍为 0。

## 证据角色

本版是第二个负向训练实验。它说明在当前 tiny GPT/语料长度/训练参数下，简单加权 loss guard 不能解决 required-term pair 对齐问题。

一句话总结：v657 证明 loss-guarded schedule 比 surface-first 更不稳定，后续应该停止这条单语料近似分支并收口。
